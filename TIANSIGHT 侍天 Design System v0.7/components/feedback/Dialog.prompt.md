Paper panel over a charcoal scrim; the only surface allowed a shadow (p2, --shadow-3).

```jsx
<Dialog open title="确认提交" actions={<><Button variant="secondary">取消</Button><Button>提交</Button></>}>…</Dialog>
```
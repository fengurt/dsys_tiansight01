The published mark (brand/logo.png), never recoloured. Optional wordmark lockup carries only the Latin caption "Tiansight": the mark itself reads 侍天, so the CN name is never repeated beside it.

```jsx
<Mark size={36} wordmark src="../brand/logo.png" />
```
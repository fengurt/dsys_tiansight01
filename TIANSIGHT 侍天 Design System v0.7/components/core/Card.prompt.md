Content surface: paper, 1px hairline, radius 2px, pad 24. Never rounded, never a coloured left strip.

```jsx
<Card index="壹" caption="Chart" title="海图">把收银、平台、会员、门店数据合成一本账。</Card>
```

Variants: field (recommended tier), charcoal (one card per view).
/* @ds-bundle: {"format":4,"namespace":"TIANSIGHTDesignSystem_bb63dc","components":[{"name":"Donut","sourcePath":"components/charts/Donut.jsx"},{"name":"Funnel","sourcePath":"components/charts/Funnel.jsx"},{"name":"Gauge","sourcePath":"components/charts/Gauge.jsx"},{"name":"Heatmap","sourcePath":"components/charts/Heatmap.jsx"},{"name":"Sparkline","sourcePath":"components/charts/Sparkline.jsx"},{"name":"StackedBars","sourcePath":"components/charts/StackedBars.jsx"},{"name":"TrendLine","sourcePath":"components/charts/TrendLine.jsx"},{"name":"Waterfall","sourcePath":"components/charts/Waterfall.jsx"},{"name":"CHART_TONES","sourcePath":"components/charts/chartKit.jsx"},{"name":"CHART_HEX","sourcePath":"components/charts/chartKit.jsx"},{"name":"CHART_MARGIN","sourcePath":"components/charts/chartKit.jsx"},{"name":"AXIS_LABEL","sourcePath":"components/charts/chartKit.jsx"},{"name":"VALUE_LABEL","sourcePath":"components/charts/chartKit.jsx"},{"name":"ChartFrame","sourcePath":"components/charts/chartKit.jsx"},{"name":"ChartLegend","sourcePath":"components/charts/chartKit.jsx"},{"name":"Axes","sourcePath":"components/charts/chartKit.jsx"},{"name":"Badge","sourcePath":"components/core/Badge.jsx"},{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"Card","sourcePath":"components/core/Card.jsx"},{"name":"Divider","sourcePath":"components/core/Divider.jsx"},{"name":"Eyebrow","sourcePath":"components/core/Eyebrow.jsx"},{"name":"Icon","sourcePath":"components/core/Icon.jsx"},{"name":"IconButton","sourcePath":"components/core/IconButton.jsx"},{"name":"PlanCard","sourcePath":"components/core/PlanCard.jsx"},{"name":"Quote","sourcePath":"components/core/Quote.jsx"},{"name":"Wordmark","sourcePath":"components/core/Seal.jsx"},{"name":"Seal","sourcePath":"components/core/Seal.jsx"},{"name":"SectionHeading","sourcePath":"components/core/SectionHeading.jsx"},{"name":"Stat","sourcePath":"components/core/Stat.jsx"},{"name":"StepMarker","sourcePath":"components/core/StepMarker.jsx"},{"name":"Tag","sourcePath":"components/core/Tag.jsx"},{"name":"BarSeries","sourcePath":"components/data/BarSeries.jsx"},{"name":"LedgerTable","sourcePath":"components/data/LedgerTable.jsx"},{"name":"Matrix2x2","sourcePath":"components/data/Matrix2x2.jsx"},{"name":"MetricRow","sourcePath":"components/data/MetricRow.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"Input","sourcePath":"components/forms/Input.jsx"},{"name":"Radio","sourcePath":"components/forms/Radio.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"ExpertCard","sourcePath":"components/people/ExpertCard.jsx"},{"name":"PartnerCase","sourcePath":"components/people/PartnerCase.jsx"},{"name":"ChartSlide","sourcePath":"slides/ChartSlide.jsx"},{"name":"ContactSlide","sourcePath":"slides/ContactSlide.jsx"},{"name":"ExpertTeamSlide","sourcePath":"slides/ExpertTeamSlide.jsx"},{"name":"FiveQuestionsSlide","sourcePath":"slides/FiveQuestionsSlide.jsx"},{"name":"LadderSlide","sourcePath":"slides/LadderSlide.jsx"},{"name":"PartnerWallSlide","sourcePath":"slides/PartnerWallSlide.jsx"},{"name":"PromiseSlide","sourcePath":"slides/PromiseSlide.jsx"},{"name":"ProofSlide","sourcePath":"slides/ProofSlide.jsx"},{"name":"SectionSlide","sourcePath":"slides/SectionSlide.jsx"},{"name":"SlideFrame","sourcePath":"slides/SlideFrame.jsx"},{"name":"SlideTitle","sourcePath":"slides/SlideFrame.jsx"},{"name":"SlidePlaceholder","sourcePath":"slides/SlideFrame.jsx"},{"name":"TitleSlide","sourcePath":"slides/TitleSlide.jsx"}],"sourceHashes":{"components/charts/Donut.jsx":"9325a1e2e7f0","components/charts/Funnel.jsx":"45d9c91e0122","components/charts/Gauge.jsx":"58279ab30d9a","components/charts/Heatmap.jsx":"86290e8d0a51","components/charts/Sparkline.jsx":"079ed4f38924","components/charts/StackedBars.jsx":"f53b6d78c3a8","components/charts/TrendLine.jsx":"2efd8243b78a","components/charts/Waterfall.jsx":"110a405d352e","components/charts/chartKit.jsx":"cb605a37cab0","components/core/Badge.jsx":"3868b3efd818","components/core/Button.jsx":"2a38388bec08","components/core/Card.jsx":"f10e5e85df22","components/core/Divider.jsx":"ddcf90b37364","components/core/Eyebrow.jsx":"a6d1652abcd6","components/core/Icon.jsx":"9df1164d2bb9","components/core/IconButton.jsx":"e980f3ae5ff7","components/core/PlanCard.jsx":"5cb5875b2771","components/core/Quote.jsx":"284949a31609","components/core/Seal.jsx":"9b5227a91624","components/core/SectionHeading.jsx":"393969ae835a","components/core/Stat.jsx":"c2c655986c7a","components/core/StepMarker.jsx":"af18357d3e4a","components/core/Tag.jsx":"334405f72db1","components/data/BarSeries.jsx":"553708210d17","components/data/LedgerTable.jsx":"fafdc2698c44","components/data/Matrix2x2.jsx":"36470aa3e0e0","components/data/MetricRow.jsx":"245f7e628168","components/forms/Checkbox.jsx":"9d4135ca7b90","components/forms/Input.jsx":"20e1d3e577a4","components/forms/Radio.jsx":"8532f7c0133d","components/forms/Select.jsx":"699837d8c515","components/forms/Switch.jsx":"c1f822a60d67","components/people/ExpertCard.jsx":"77f510f8adb8","components/people/PartnerCase.jsx":"1597f23562dc","slides/ChartSlide.jsx":"8735e6dcef31","slides/ContactSlide.jsx":"a2b947a52030","slides/ExpertTeamSlide.jsx":"149b8c23c594","slides/FiveQuestionsSlide.jsx":"40efe82fef88","slides/LadderSlide.jsx":"88b8907ab42d","slides/PartnerWallSlide.jsx":"c76fd1573dea","slides/PromiseSlide.jsx":"b467144c0538","slides/ProofSlide.jsx":"2144162fd803","slides/SectionSlide.jsx":"e159e29dc625","slides/SlideFrame.jsx":"95bd8a404aa1","slides/TitleSlide.jsx":"2b2c6f07a752","slides/deck-stage.js":"f3d3d0a662c0","ui_kits/report/AnalysisPage.jsx":"9b51eb790f9f","ui_kits/report/DecisionPage.jsx":"afa6c84af79c","ui_kits/report/MethodMapPage.jsx":"d28fa98949a8","ui_kits/report/ReportShell.jsx":"6f77e573ff52","ui_kits/report/TrendPage.jsx":"5a19db64674c","ui_kits/website/ClosingCTA.jsx":"08333e1f5183","ui_kits/website/FiveQuestions.jsx":"796c3e084fdd","ui_kits/website/HomeHero.jsx":"54775c8fac79","ui_kits/website/Partners.jsx":"cae67b06fee9","ui_kits/website/PartnersPage.jsx":"ba07357eb280","ui_kits/website/ReportProof.jsx":"ff393d51cd0f","ui_kits/website/ServiceLadder.jsx":"a7a9111b5315","ui_kits/website/SiteHeader.jsx":"9d8be097d5d7"},"inlinedExternals":[],"unexposedExports":[{"name":"dsAsset","sourcePath":"slides/SlideFrame.jsx"},{"name":"isDarkFill","sourcePath":"components/charts/chartKit.jsx"},{"name":"tone","sourcePath":"components/charts/chartKit.jsx"},{"name":"useD3","sourcePath":"components/charts/chartKit.jsx"},{"name":"useWidth","sourcePath":"components/charts/chartKit.jsx"}]} */

(() => {

const __ds_ns = (window.TIANSIGHTDesignSystem_bb63dc = window.TIANSIGHTDesignSystem_bb63dc || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/charts/chartKit.jsx
try { (() => {
/* 侍天 chart grammar. Every d3 chart in this system draws through these helpers so
   axes, margins, tones and type are identical across the report, the site and slides.
   d3 is expected on window (CDN, v7) — see charts.card.html for the script tag. */

const CHART_TONES = {
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  datum: "var(--datum-500)",
  muted: "var(--parchment-400)"
};

/* Literal hex for d3 interpolation, which cannot read CSS custom properties. */
const CHART_HEX = {
  bronze: "#76551F",
  growth: "#4E6B3F",
  loss: "#A34A2C",
  caution: "#B98B2A",
  datum: "#3F5A6B",
  muted: "#D6C6A4",
  grid: "rgba(118,85,31,.16)",
  rule: "rgba(118,85,31,.28)",
  ink: "#4A4136",
  mutedInk: "#8B8072"
};
const CHART_MARGIN = {
  top: 16,
  right: 20,
  bottom: 34,
  left: 52
};
const AXIS_LABEL = {
  fontFamily: "var(--font-body)",
  fontSize: 11,
  letterSpacing: ".02em",
  fill: CHART_HEX.mutedInk
};
const VALUE_LABEL = {
  fontFamily: "var(--font-numeral)",
  fontSize: 11,
  fill: CHART_HEX.ink
};
function tone(name) {
  return CHART_HEX[name] || CHART_HEX.bronze;
}

/* Resolve d3, waiting for a CDN script that may still be loading. */
function useD3() {
  const [d3, setD3] = React.useState(function () {
    return typeof window !== "undefined" ? window.d3 : null;
  });
  React.useEffect(function () {
    if (d3) return;
    var t = setInterval(function () {
      if (window.d3) {
        setD3(window.d3);
        clearInterval(t);
      }
    }, 60);
    return function () {
      clearInterval(t);
    };
  }, [d3]);
  return d3;
}

/* Measure the container so charts fill their column without a hard-coded width. */
function useWidth(fallback) {
  const ref = React.useRef(null);
  const [w, setW] = React.useState(fallback || 560);
  React.useEffect(function () {
    var el = ref.current;
    if (!el) return;
    var fit = function () {
      var b = el.clientWidth;
      if (b > 80) setW(b);
    };
    fit();
    var ro = typeof ResizeObserver !== "undefined" ? new ResizeObserver(fit) : null;
    if (ro) ro.observe(el);
    window.addEventListener("resize", fit);
    return function () {
      window.removeEventListener("resize", fit);
      if (ro) ro.disconnect();
    };
  }, []);
  return [ref, w];
}

/* Chart frame: caption, measured SVG, optional source note. */
function ChartFrame({
  caption,
  note,
  height,
  children,
  svgRef,
  width
}) {
  return React.createElement("figure", {
    style: {
      margin: 0,
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)",
      minWidth: 0
    }
  }, caption ? React.createElement("figcaption", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, caption) : null, React.createElement("svg", {
    ref: svgRef,
    width: width,
    height: height,
    style: {
      display: "block",
      overflow: "visible"
    }
  }, children), note ? React.createElement("span", {
    style: {
      fontSize: "var(--text-3xs)",
      color: "var(--text-muted)"
    }
  }, note) : null);
}

/* Legend as HTML, NOT inside the SVG: an SVG legend with a hard-coded stride overflows
   narrow containers (svg uses overflow:visible, so it spills past the panel). Flex + wrap
   degrades correctly at any width. */
function ChartLegend({
  items = [],
  style: extra
}) {
  if (!items.length) return null;
  return React.createElement("div", {
    style: Object.assign({
      display: "flex",
      flexWrap: "wrap",
      gap: "6px 18px",
      paddingTop: 2
    }, extra)
  }, items.map(function (it, i) {
    return React.createElement("span", {
      key: i,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 7,
        minWidth: 0,
        fontFamily: "var(--font-body)",
        fontSize: 11,
        color: "var(--ink-600)",
        whiteSpace: "nowrap"
      }
    }, it.dashed ? React.createElement("span", {
      style: {
        width: 16,
        height: 0,
        borderTop: "2px dashed " + tone(it.tone),
        flexShrink: 0
      }
    }) : React.createElement("span", {
      style: {
        width: 10,
        height: 10,
        borderRadius: 1,
        background: tone(it.tone),
        flexShrink: 0
      }
    }), React.createElement("span", null, it.label));
  }));
}

/* Relative luminance of any CSS color d3 can parse — used to pick light vs dark ink on a
   filled cell. Both ends of a diverging scale are dark, so a mode flag is not enough. */
function isDarkFill(d3, color) {
  try {
    var c = d3.color(color);
    if (!c) return false;
    var rgb = c.rgb();
    var f = function (v) {
      v /= 255;
      return v <= .03928 ? v / 12.92 : Math.pow((v + .055) / 1.055, 2.4);
    };
    return 0.2126 * f(rgb.r) + 0.7152 * f(rgb.g) + 0.0722 * f(rgb.b) < 0.42;
  } catch (e) {
    return false;
  }
}

/* Bottom + left axes drawn as hairlines with mono ticks. No gridlines by default. */
function Axes({
  x,
  y,
  width,
  height,
  margin = CHART_MARGIN,
  xTicks = 6,
  yTicks = 4,
  grid = false,
  xFormat,
  yFormat,
  band = false
}) {
  const iw = width - margin.left - margin.right;
  const ih = height - margin.top - margin.bottom;
  const yt = y && y.ticks ? y.ticks(yTicks) : y && y.domain ? y.domain() : [];
  const xt = band ? x ? x.domain() : [] : x && x.ticks ? x.ticks(xTicks) : [];
  return React.createElement("g", null, grid ? yt.map(function (v, i) {
    return React.createElement("line", {
      key: "g" + i,
      x1: margin.left,
      x2: margin.left + iw,
      y1: y(v),
      y2: y(v),
      stroke: CHART_HEX.grid,
      strokeWidth: 1
    });
  }) : null, React.createElement("line", {
    x1: margin.left,
    x2: margin.left + iw,
    y1: margin.top + ih,
    y2: margin.top + ih,
    stroke: CHART_HEX.rule,
    strokeWidth: 1
  }), yt.map(function (v, i) {
    return React.createElement("text", Object.assign({
      key: "y" + i,
      x: margin.left - 10,
      y: y(v),
      dy: "0.32em",
      textAnchor: "end"
    }, VALUE_LABEL, {
      fontSize: 10
    }), yFormat ? yFormat(v) : v);
  }), xt.map(function (v, i) {
    const cx = band ? x(v) + x.bandwidth() / 2 : x(v);
    return React.createElement("text", Object.assign({
      key: "x" + i,
      x: cx,
      y: margin.top + ih + 18,
      textAnchor: "middle"
    }, AXIS_LABEL), xFormat ? xFormat(v) : v);
  }));
}
Object.assign(__ds_scope, { CHART_TONES, CHART_HEX, CHART_MARGIN, AXIS_LABEL, VALUE_LABEL, tone, useD3, useWidth, ChartFrame, ChartLegend, isDarkFill, Axes });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/chartKit.jsx", error: String((e && e.message) || e) }); }

// components/charts/Donut.jsx
try { (() => {
function Donut({
  data = [],
  size = 220,
  thickness = 26,
  caption,
  note,
  centerValue,
  centerLabel,
  unit = "%",
  showLegend = true
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(size + 200);
  let body = null;
  if (d3 && data.length) {
    const r = size / 2;
    const arcs = d3.pie().sort(null).padAngle(.012).value(function (d) {
      return d.value;
    })(data);
    const arc = d3.arc().innerRadius(r - thickness).outerRadius(r).cornerRadius(1);
    body = React.createElement("g", {
      transform: "translate(" + r + "," + r + ")"
    }, arcs.map(function (a, i) {
      return React.createElement("path", {
        key: i,
        d: arc(a),
        fill: __ds_scope.tone(data[i].tone),
        stroke: "var(--surface-card)",
        strokeWidth: 1
      });
    }), centerValue != null ? React.createElement("text", Object.assign({
      x: 0,
      y: -2,
      textAnchor: "middle"
    }, __ds_scope.VALUE_LABEL, {
      fontSize: 30,
      fill: __ds_scope.CHART_HEX.ink
    }), centerValue) : null, centerLabel ? React.createElement("text", Object.assign({
      x: 0,
      y: 20,
      textAnchor: "middle"
    }, __ds_scope.AXIS_LABEL, {
      fontSize: 11
    }), centerLabel) : null);
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0,
      display: "flex",
      alignItems: "center",
      gap: "var(--space-7)",
      flexWrap: "wrap"
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: size,
    height: size
  }, body), showLegend ? React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 10,
      minWidth: 0
    }
  }, data.map(function (d, i) {
    return React.createElement("div", {
      key: i,
      style: {
        display: "flex",
        alignItems: "center",
        gap: 10,
        fontSize: "var(--text-xs)",
        color: "var(--ink-700)"
      }
    }, React.createElement("span", {
      style: {
        width: 10,
        height: 10,
        borderRadius: 1,
        background: __ds_scope.tone(d.tone),
        flexShrink: 0
      }
    }), React.createElement("span", {
      style: {
        flex: 1
      }
    }, d.label), React.createElement("span", {
      style: {
        fontFamily: "var(--font-numeral)",
        color: "var(--ink-900)"
      }
    }, d.value + unit));
  })) : null);
}
Object.assign(__ds_scope, { Donut });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Donut.jsx", error: String((e && e.message) || e) }); }

// components/charts/Funnel.jsx
try { (() => {
/* 转化漏斗: each stage's width is proportional to its value; drop-off is labelled. */
function Funnel({
  stages = [],
  height,
  caption,
  note,
  unit = "",
  showDrop = true
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(560);
  const rowH = 52;
  const h = height || stages.length * rowH + 20;
  let body = null;
  if (d3 && stages.length) {
    const max = d3.max(stages, function (s) {
      return s.value;
    }) || 1;
    // Gutters are PROPORTIONAL, never a fixed 240px: a fixed reserve exceeds a narrow
    // two-column container and collapses every bar to the minimum width.
    const labelG = Math.min(120, Math.max(52, width * 0.2));
    const valueG = Math.min(84, Math.max(44, width * 0.15));
    const wideEnough = width > 320;
    const dropG = showDrop && wideEnough ? Math.min(56, width * 0.11) : 0;
    const usable = Math.max(width * 0.3, width - labelG - valueG - dropG);
    body = stages.map(function (s, i) {
      const w = Math.max(2, s.value / max * usable);
      const prev = i > 0 ? stages[i - 1].value : null;
      const drop = prev ? (s.value - prev) / prev * 100 : null;
      const cx = labelG + usable / 2;
      return React.createElement("g", {
        key: i,
        transform: "translate(0," + (i * rowH + 8) + ")"
      }, React.createElement("rect", {
        x: cx - w / 2,
        y: 0,
        width: w,
        height: rowH - 16,
        rx: 1,
        fill: __ds_scope.tone(s.tone || (i === 0 ? "bronze" : i === stages.length - 1 ? "growth" : "datum")),
        opacity: .92
      }), React.createElement("text", Object.assign({
        x: 0,
        y: (rowH - 16) / 2,
        dy: "0.32em"
      }, __ds_scope.AXIS_LABEL, {
        fill: __ds_scope.CHART_HEX.ink,
        fontSize: 12
      }), s.label), React.createElement("text", Object.assign({
        x: Math.min(cx + w / 2 + 10, width - dropG - 6),
        y: (rowH - 16) / 2,
        dy: "0.32em"
      }, __ds_scope.VALUE_LABEL, {
        fontSize: 12
      }), s.value + unit), dropG && drop != null ? React.createElement("text", Object.assign({
        x: width - 2,
        y: (rowH - 16) / 2,
        dy: "0.32em",
        textAnchor: "end"
      }, __ds_scope.VALUE_LABEL, {
        fontSize: 11,
        fill: __ds_scope.CHART_HEX.loss
      }), drop.toFixed(1) + "%") : null);
    });
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: width,
    height: h
  }, body));
}
Object.assign(__ds_scope, { Funnel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Funnel.jsx", error: String((e && e.message) || e) }); }

// components/charts/Gauge.jsx
try { (() => {
/* 指标达成: a 200° arc with the target marked. Bronze by default; tone by attainment. */
function Gauge({
  value = 0,
  target = 100,
  max,
  size = 190,
  label,
  unit = "%",
  caption,
  note,
  autoTone = true,
  toneName
}) {
  const d3 = __ds_scope.useD3();
  const top = max != null ? max : Math.max(target * 1.25, value * 1.1);
  let body = null;
  if (d3) {
    const r = size / 2;
    const a0 = -Math.PI * 0.62,
      a1 = Math.PI * 0.62;
    const s = d3.scaleLinear().domain([0, top]).range([a0, a1]).clamp(true);
    const track = d3.arc().innerRadius(r - 20).outerRadius(r - 4).startAngle(a0).endAngle(a1).cornerRadius(2);
    const fill = d3.arc().innerRadius(r - 20).outerRadius(r - 4).startAngle(a0).endAngle(s(value)).cornerRadius(2);
    const pct = target ? value / target : 0;
    const c = __ds_scope.tone(toneName || (!autoTone ? "bronze" : pct >= 1 ? "growth" : pct >= .85 ? "caution" : "loss"));
    const tx = Math.sin(s(target)) * (r - 12),
      ty = -Math.cos(s(target)) * (r - 12);
    const tx2 = Math.sin(s(target)) * (r - 26),
      ty2 = -Math.cos(s(target)) * (r - 26);
    body = React.createElement("g", {
      transform: "translate(" + r + "," + (r + 6) + ")"
    }, React.createElement("path", {
      d: track(),
      fill: "var(--parchment-300)"
    }), React.createElement("path", {
      d: fill(),
      fill: c
    }), React.createElement("line", {
      x1: tx2,
      y1: ty2,
      x2: tx,
      y2: ty,
      stroke: __ds_scope.CHART_HEX.ink,
      strokeWidth: 1.5
    }), React.createElement("text", Object.assign({
      x: 0,
      y: 4,
      textAnchor: "middle"
    }, __ds_scope.VALUE_LABEL, {
      fontSize: 34,
      fill: c
    }), value + unit), label ? React.createElement("text", Object.assign({
      x: 0,
      y: 26,
      textAnchor: "middle"
    }, __ds_scope.AXIS_LABEL, {
      fontSize: 11
    }), label) : null, React.createElement("text", Object.assign({
      x: 0,
      y: 46,
      textAnchor: "middle"
    }, __ds_scope.AXIS_LABEL, {
      fontSize: 10
    }), "目标 " + target + unit));
  }
  return React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: size,
    height: size * 0.82 + 20
  }, body);
}
Object.assign(__ds_scope, { Gauge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Gauge.jsx", error: String((e && e.message) || e) }); }

// components/charts/Heatmap.jsx
try { (() => {
/* 逐店逐月: rows = 门店, columns = 月份. Parchment → bronze ramp, or growth↔loss diverging. */
function Heatmap({
  rows = [],
  columns = [],
  values = {},
  height,
  caption,
  note,
  diverging = false,
  unit = "",
  cellGap = 2,
  labelWidth = 92
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(560);
  const rowH = 30;
  const h = height || rows.length * rowH + 46;
  let body = null;
  if (d3 && rows.length && columns.length) {
    const all = [];
    rows.forEach(function (r) {
      columns.forEach(function (c) {
        const v = values[r + "|" + c];
        if (v != null) all.push(v);
      });
    });
    const ext = d3.extent(all);
    const scale = diverging ? d3.scaleDiverging().domain([ext[0], 0, ext[1]]).interpolator(d3.interpolateRgbBasis([__ds_scope.CHART_HEX.loss, "#F6F1E4", __ds_scope.CHART_HEX.growth])) : d3.scaleSequential().domain(ext).interpolator(d3.interpolateRgbBasis(["#F6F1E4", "#D2BD8E", __ds_scope.CHART_HEX.bronze]));
    const cw = (width - labelWidth - 8) / columns.length;
    body = React.createElement(React.Fragment, null, columns.map(function (c, ci) {
      return React.createElement("text", Object.assign({
        key: "c" + ci,
        x: labelWidth + ci * cw + cw / 2,
        y: 12,
        textAnchor: "middle"
      }, __ds_scope.AXIS_LABEL, {
        fontSize: 10
      }), c);
    }), rows.map(function (r, ri) {
      return React.createElement("g", {
        key: "r" + ri
      }, React.createElement("text", Object.assign({
        x: labelWidth - 12,
        y: 24 + ri * rowH + rowH / 2,
        dy: "0.32em",
        textAnchor: "end"
      }, __ds_scope.AXIS_LABEL, {
        fill: __ds_scope.CHART_HEX.ink
      }), r), columns.map(function (c, ci) {
        const v = values[r + "|" + c];
        const fill = v == null ? "var(--parchment-100)" : scale(v);
        // Luminance of the RESOLVED fill, in both modes: a diverging scale is dark at
        // BOTH ends, so a mode flag would leave the deepest red and green unreadable.
        const dark = v != null && __ds_scope.isDarkFill(d3, fill);
        return React.createElement("g", {
          key: ci
        }, React.createElement("rect", {
          x: labelWidth + ci * cw + cellGap / 2,
          y: 24 + ri * rowH + cellGap / 2,
          width: Math.max(1, cw - cellGap),
          height: rowH - cellGap,
          rx: 1,
          fill: fill,
          stroke: v == null ? __ds_scope.CHART_HEX.grid : "none",
          strokeDasharray: v == null ? "2 2" : null
        }), v != null ? React.createElement("text", Object.assign({
          x: labelWidth + ci * cw + cw / 2,
          y: 24 + ri * rowH + rowH / 2,
          dy: "0.32em",
          textAnchor: "middle"
        }, __ds_scope.VALUE_LABEL, {
          fontSize: 10,
          fill: dark ? "#FBF8F1" : __ds_scope.CHART_HEX.ink
        }), v + unit) : null);
      }));
    }));
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: width,
    height: h
  }, body));
}
Object.assign(__ds_scope, { Heatmap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Heatmap.jsx", error: String((e && e.message) || e) }); }

// components/charts/Sparkline.jsx
try { (() => {
/* 表内微图: inline trend for a table cell or MetricRow. No axes, no labels. */
function Sparkline({
  values = [],
  width = 84,
  height = 22,
  toneName = "datum",
  area = true,
  showLast = true
}) {
  const d3 = __ds_scope.useD3();
  if (!d3 || values.length < 2) return React.createElement("svg", {
    width: width,
    height: height,
    style: {
      display: "block"
    }
  });
  const ext = d3.extent(values);
  const pad = (ext[1] - ext[0] || 1) * .12;
  const x = d3.scaleLinear().domain([0, values.length - 1]).range([1, width - 1]);
  const y = d3.scaleLinear().domain([ext[0] - pad, ext[1] + pad]).range([height - 2, 2]);
  const pts = values.map(function (v, i) {
    return {
      i: i,
      v: v
    };
  });
  const c = __ds_scope.tone(toneName);
  return React.createElement("svg", {
    width: width,
    height: height,
    style: {
      display: "block",
      overflow: "visible"
    }
  }, area ? React.createElement("path", {
    d: d3.area().x(function (p) {
      return x(p.i);
    }).y0(height).y1(function (p) {
      return y(p.v);
    }).curve(d3.curveMonotoneX)(pts),
    fill: c,
    opacity: .12
  }) : null, React.createElement("path", {
    d: d3.line().x(function (p) {
      return x(p.i);
    }).y(function (p) {
      return y(p.v);
    }).curve(d3.curveMonotoneX)(pts),
    fill: "none",
    stroke: c,
    strokeWidth: 1.4,
    strokeLinecap: "round"
  }), showLast ? React.createElement("circle", {
    cx: x(values.length - 1),
    cy: y(values[values.length - 1]),
    r: 2.2,
    fill: c
  }) : null);
}
Object.assign(__ds_scope, { Sparkline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Sparkline.jsx", error: String((e && e.message) || e) }); }

// components/charts/StackedBars.jsx
try { (() => {
function StackedBars({
  data = [],
  keys = [],
  tones = {},
  height = 280,
  caption,
  note,
  unit = "%",
  normalize = false,
  grid = true
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(560);
  const m = __ds_scope.CHART_MARGIN;
  let body = null;
  if (d3 && data.length && keys.length) {
    const rows = normalize ? data.map(function (d) {
      const t = keys.reduce(function (a, k) {
        return a + (d[k] || 0);
      }, 0) || 1;
      const o = {
        label: d.label
      };
      keys.forEach(function (k) {
        o[k] = (d[k] || 0) / t * 100;
      });
      return o;
    }) : data;
    const stack = d3.stack().keys(keys)(rows);
    const x = d3.scaleBand().domain(rows.map(function (d) {
      return d.label;
    })).range([m.left, width - m.right]).padding(0.34);
    const top = d3.max(stack[stack.length - 1], function (d) {
      return d[1];
    }) || 1;
    const y = d3.scaleLinear().domain([0, top]).nice().range([height - m.bottom, m.top]);
    body = React.createElement(React.Fragment, null, React.createElement(__ds_scope.Axes, {
      x: x,
      y: y,
      width: width,
      height: height,
      grid: grid,
      band: true,
      yFormat: function (v) {
        return Math.round(v) + unit;
      }
    }), stack.map(function (layer, li) {
      return React.createElement("g", {
        key: li,
        fill: __ds_scope.tone(tones[keys[li]])
      }, layer.map(function (d, di) {
        const h = Math.max(0, y(d[0]) - y(d[1]));
        return React.createElement("rect", {
          key: di,
          x: x(rows[di].label),
          y: y(d[1]),
          width: x.bandwidth(),
          height: h,
          rx: 1
        });
      }));
    }));
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0,
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: width,
    height: height
  }, body), React.createElement(__ds_scope.ChartLegend, {
    items: keys.map(function (k) {
      return {
        label: k,
        tone: tones[k]
      };
    })
  }));
}
Object.assign(__ds_scope, { StackedBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/StackedBars.jsx", error: String((e && e.message) || e) }); }

// components/charts/TrendLine.jsx
try { (() => {
function TrendLine({
  series = [],
  height = 260,
  caption,
  note,
  unit = "",
  yFormat,
  showPoints = true,
  grid = true
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(560);
  const m = __ds_scope.CHART_MARGIN;
  const flat = series.reduce(function (a, s) {
    return a.concat(s.points || []);
  }, []);
  let body = null;
  if (d3 && flat.length) {
    const labels = (series[0].points || []).map(function (p) {
      return p.label;
    });
    const x = d3.scalePoint().domain(labels).range([m.left, width - m.right]).padding(0.5);
    const ext = d3.extent(flat, function (p) {
      return p.value;
    });
    const pad = (ext[1] - ext[0] || 1) * 0.18;
    const y = d3.scaleLinear().domain([Math.min(ext[0] - pad, ext[0] * 0.96), ext[1] + pad]).nice().range([height - m.bottom, m.top]);
    const line = d3.line().x(function (p) {
      return x(p.label);
    }).y(function (p) {
      return y(p.value);
    }).curve(d3.curveMonotoneX);
    body = React.createElement(React.Fragment, null, React.createElement(__ds_scope.Axes, {
      x: x,
      y: y,
      width: width,
      height: height,
      grid: grid,
      band: false,
      yFormat: yFormat || function (v) {
        return v + unit;
      },
      xTicks: labels.length
    }), labels.map(function (l, i) {
      return React.createElement("text", Object.assign({
        key: "xl" + i,
        x: x(l),
        y: height - m.bottom + 18,
        textAnchor: "middle"
      }, __ds_scope.AXIS_LABEL), l);
    }), series.map(function (s, si) {
      const c = __ds_scope.tone(s.tone);
      return React.createElement("g", {
        key: si
      }, s.area ? React.createElement("path", {
        d: d3.area().x(function (p) {
          return x(p.label);
        }).y0(height - m.bottom).y1(function (p) {
          return y(p.value);
        }).curve(d3.curveMonotoneX)(s.points),
        fill: c,
        opacity: .10
      }) : null, React.createElement("path", {
        d: line(s.points),
        fill: "none",
        stroke: c,
        strokeWidth: s.emphasis ? 2.25 : 1.5,
        strokeDasharray: s.dashed ? "5 4" : null,
        strokeLinecap: "round"
      }), showPoints ? s.points.map(function (p, pi) {
        return React.createElement("circle", {
          key: pi,
          cx: x(p.label),
          cy: y(p.value),
          r: 3.2,
          fill: "var(--surface-card)",
          stroke: c,
          strokeWidth: 1.5
        });
      }) : null);
    }));
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0,
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: width,
    height: height
  }, body), series.length > 1 ? React.createElement(__ds_scope.ChartLegend, {
    items: series.filter(function (s) {
      return s.name;
    }).map(function (s) {
      return {
        label: s.name,
        tone: s.tone,
        dashed: s.dashed
      };
    })
  }) : null);
}
Object.assign(__ds_scope, { TrendLine });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/TrendLine.jsx", error: String((e && e.message) || e) }); }

// components/charts/Waterfall.jsx
try { (() => {
/* 利润流失归因: start → signed steps → end. Bars are placed cumulatively. */
function Waterfall({
  start,
  steps = [],
  endLabel = "当期",
  startLabel = "上期",
  height = 300,
  caption,
  note,
  unit = "万"
}) {
  const d3 = __ds_scope.useD3();
  const [wrapRef, width] = __ds_scope.useWidth(560);
  const m = {
    top: 22,
    right: 20,
    bottom: 46,
    left: 56
  };
  let body = null;
  if (d3 && steps.length) {
    const bars = [];
    let run = start;
    bars.push({
      label: startLabel,
      y0: 0,
      y1: start,
      kind: "total",
      value: start
    });
    steps.forEach(function (s) {
      const next = run + s.value;
      bars.push({
        label: s.label,
        y0: Math.min(run, next),
        y1: Math.max(run, next),
        kind: s.value >= 0 ? "growth" : "loss",
        value: s.value
      });
      run = next;
    });
    bars.push({
      label: endLabel,
      y0: 0,
      y1: run,
      kind: "total",
      value: run
    });
    const x = d3.scaleBand().domain(bars.map(function (b) {
      return b.label;
    })).range([m.left, width - m.right]).padding(0.32);
    const y = d3.scaleLinear().domain([0, d3.max(bars, function (b) {
      return b.y1;
    }) * 1.12]).nice().range([height - m.bottom, m.top]);
    body = React.createElement(React.Fragment, null, React.createElement(__ds_scope.Axes, {
      x: x,
      y: y,
      width: width,
      height: height,
      grid: true,
      band: true,
      yFormat: function (v) {
        return v + "";
      }
    }), bars.map(function (b, i) {
      const c = b.kind === "total" ? __ds_scope.tone("bronze") : __ds_scope.tone(b.kind);
      const h = Math.max(1, y(b.y0) - y(b.y1));
      return React.createElement("g", {
        key: i
      }, React.createElement("rect", {
        x: x(b.label),
        y: y(b.y1),
        width: x.bandwidth(),
        height: h,
        fill: c,
        opacity: b.kind === "total" ? 1 : .92,
        rx: 1
      }), React.createElement("text", Object.assign({
        x: x(b.label) + x.bandwidth() / 2,
        y: y(b.y1) - 7,
        textAnchor: "middle"
      }, __ds_scope.VALUE_LABEL, {
        fill: c,
        fontSize: 11
      }), (b.kind === "total" ? "" : b.value > 0 ? "+" : "") + b.value), i < bars.length - 1 ? React.createElement("line", {
        x1: x(b.label) + x.bandwidth(),
        x2: x(bars[i + 1].label),
        y1: y(b.kind === "loss" ? b.y0 : b.y1),
        y2: y(b.kind === "loss" ? b.y0 : b.y1),
        stroke: __ds_scope.CHART_HEX.rule,
        strokeWidth: 1,
        strokeDasharray: "3 3"
      }) : null);
    }), React.createElement("text", Object.assign({
      x: m.left,
      y: 10
    }, __ds_scope.AXIS_LABEL), "单位：" + unit));
  }
  return React.createElement("div", {
    ref: wrapRef,
    style: {
      width: "100%",
      minWidth: 0
    }
  }, React.createElement(__ds_scope.ChartFrame, {
    caption: caption,
    note: note,
    width: width,
    height: height
  }, body));
}
Object.assign(__ds_scope, { Waterfall });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/charts/Waterfall.jsx", error: String((e && e.message) || e) }); }

// components/core/Badge.jsx
try { (() => {
const TONES = {
  bronze: ["var(--bronze-100)", "var(--bronze-600)", "var(--bronze-500)"],
  neutral: ["var(--parchment-100)", "var(--ink-600)", "var(--line-rule)"],
  growth: ["var(--growth-200)", "var(--growth-500)", "var(--growth-500)"],
  loss: ["var(--loss-200)", "var(--loss-500)", "var(--loss-500)"],
  caution: ["var(--caution-200)", "var(--caution-500)", "var(--caution-500)"]
};
function Badge({
  children,
  tone = "bronze",
  variant = "soft",
  size = "md",
  ...rest
}) {
  const t = TONES[tone] || TONES.bronze;
  const solid = variant === "solid";
  const outline = variant === "outline";
  return React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "6px",
      whiteSpace: "nowrap",
      flexShrink: 0,
      padding: size === "sm" ? "2px 7px" : "3px 10px",
      fontFamily: "var(--font-body)",
      fontSize: size === "sm" ? "var(--text-3xs)" : "var(--text-2xs)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: ".04em",
      lineHeight: 1.5,
      borderRadius: "var(--radius-xs)",
      background: solid ? t[2] : outline ? "transparent" : t[0],
      color: solid ? "var(--text-on-accent)" : t[1],
      border: "1px solid " + (outline ? t[2] : "transparent")
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Badge.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
const SIZES = {
  sm: {
    padding: "7px 14px",
    font: "var(--text-xs)",
    gap: "6px"
  },
  md: {
    padding: "11px 22px",
    font: "var(--text-sm)",
    gap: "8px"
  },
  lg: {
    padding: "15px 30px",
    font: "var(--text-base)",
    gap: "10px"
  }
};
function palette(variant, hover, press) {
  if (variant === "primary") return {
    background: press ? "var(--bronze-700)" : hover ? "var(--bronze-600)" : "var(--bronze-500)",
    color: "var(--text-on-accent)",
    border: "1px solid transparent",
    boxShadow: hover ? "var(--shadow-md)" : "var(--shadow-xs)"
  };
  if (variant === "secondary") return {
    background: hover ? "var(--parchment-50)" : "var(--surface-card)",
    color: "var(--ink-800)",
    border: "1px solid " + (hover ? "var(--line-rule)" : "var(--line-hairline)"),
    boxShadow: hover ? "var(--shadow-sm)" : "none"
  };
  if (variant === "ghost") return {
    background: hover ? "var(--parchment-100)" : "transparent",
    color: press ? "var(--bronze-700)" : "var(--bronze-500)",
    border: "1px solid transparent",
    boxShadow: "none"
  };
  return {
    background: hover ? "var(--parchment-100)" : "var(--parchment-200)",
    color: "var(--ink-900)",
    border: "1px solid transparent",
    boxShadow: hover ? "var(--shadow-md)" : "none"
  };
}
function Button({
  children,
  variant = "primary",
  size = "md",
  icon,
  iconPosition = "right",
  fullWidth = false,
  disabled = false,
  href,
  onClick,
  type = "button",
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const [press, setPress] = React.useState(false);
  const s = SIZES[size] || SIZES.md;
  const p = palette(variant, hover && !disabled, press && !disabled);
  const style = {
    display: fullWidth ? "flex" : "inline-flex",
    width: fullWidth ? "100%" : "auto",
    alignItems: "center",
    justifyContent: "center",
    gap: s.gap,
    padding: s.padding,
    fontSize: s.font,
    fontFamily: "var(--font-body)",
    fontWeight: "var(--weight-medium)",
    letterSpacing: "var(--tracking-cjk-body)",
    lineHeight: 1.2,
    borderRadius: "var(--radius-md)",
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.4 : 1,
    textDecoration: "none",
    whiteSpace: "nowrap",
    transform: press && !disabled ? "translateY(1px)" : "none",
    transition: "background var(--dur-fast) var(--ease-standard), color var(--dur-fast) var(--ease-standard), box-shadow var(--dur-base) var(--ease-standard), border-color var(--dur-fast) var(--ease-standard)",
    ...p
  };
  const handlers = disabled ? {} : {
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setPress(false);
    },
    onMouseDown: () => setPress(true),
    onMouseUp: () => setPress(false),
    onClick
  };
  const body = [icon && iconPosition === "left" ? React.createElement("span", {
    key: "i",
    style: {
      display: "flex"
    }
  }, icon) : null, React.createElement("span", {
    key: "t"
  }, children), icon && iconPosition === "right" ? React.createElement("span", {
    key: "r",
    style: {
      display: "flex"
    }
  }, icon) : null];
  if (href && !disabled) return React.createElement("a", {
    href,
    style,
    ...handlers,
    ...rest
  }, body);
  return React.createElement("button", {
    type,
    disabled,
    style,
    ...handlers,
    ...rest
  }, body);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/Card.jsx
try { (() => {
const PAD = {
  sm: "var(--space-4)",
  md: "var(--space-6)",
  lg: "var(--space-7)"
};
function Card({
  children,
  padding = "md",
  tone = "paper",
  emphasized = false,
  interactive = false,
  onClick,
  style: extra,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const inverse = tone === "inverse";
  const lift = interactive && hover;
  const style = {
    background: inverse ? "var(--surface-inverse)" : tone === "muted" ? "var(--surface-card-muted)" : "var(--surface-card)",
    color: inverse ? "var(--text-on-inverse)" : "var(--text-body)",
    border: "1px solid " + (inverse ? "var(--line-inverse)" : lift ? "var(--line-rule)" : "var(--line-hairline)"),
    borderTop: emphasized ? "2px solid var(--bronze-500)" : undefined,
    borderRadius: "var(--radius-md)",
    padding: PAD[padding] || PAD.md,
    boxShadow: lift ? "var(--shadow-md)" : inverse ? "none" : "var(--shadow-sm)",
    transition: "box-shadow var(--dur-base) var(--ease-standard), border-color var(--dur-fast) var(--ease-standard), transform var(--dur-base) var(--ease-standard)",
    transform: lift ? "translateY(-2px)" : "none",
    cursor: interactive ? "pointer" : "default",
    ...extra
  };
  return React.createElement("div", {
    style,
    onClick,
    onMouseEnter: interactive ? () => setHover(true) : undefined,
    onMouseLeave: interactive ? () => setHover(false) : undefined,
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Card });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Card.jsx", error: String((e && e.message) || e) }); }

// components/core/Divider.jsx
try { (() => {
function Divider({
  variant = "hair",
  inverse = false,
  vertical = false,
  spacing = "var(--space-7)",
  ...rest
}) {
  const w = variant === "strong" ? 2 : variant === "rule" ? 1.5 : 1;
  const color = inverse ? "var(--line-inverse)" : variant === "hair" ? "var(--line-hairline)" : variant === "rule" ? "var(--line-rule)" : "var(--line-strong)";
  if (vertical) return React.createElement("div", {
    style: {
      width: w,
      alignSelf: "stretch",
      background: color,
      margin: "0 " + spacing
    },
    ...rest
  });
  return React.createElement("div", {
    style: {
      height: w,
      background: color,
      margin: spacing + " 0"
    },
    ...rest
  });
}
Object.assign(__ds_scope, { Divider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Divider.jsx", error: String((e && e.message) || e) }); }

// components/core/Eyebrow.jsx
try { (() => {
function Eyebrow({
  children,
  tone = "bronze",
  as = "div",
  ...rest
}) {
  const color = tone === "muted" ? "var(--text-muted)" : tone === "inverse" ? "rgba(239,230,210,.7)" : "var(--text-accent)";
  return React.createElement(as, {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      fontWeight: "var(--weight-medium)",
      textTransform: "uppercase",
      letterSpacing: "var(--tracking-caps)",
      color,
      lineHeight: 1.4
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/core/Icon.jsx
try { (() => {
/* Lucide (CDN) stands in for a brand icon set — see readme ICONOGRAPHY.
   Rendered as a CSS mask so the glyph always inherits currentColor. */
const BASE = "https://unpkg.com/lucide-static@0.441.0/icons/";
function Icon({
  name,
  size = 18,
  style: extra,
  ...rest
}) {
  const url = "url(" + BASE + name + ".svg)";
  return React.createElement("span", {
    "aria-hidden": "true",
    role: "presentation",
    style: {
      display: "inline-block",
      width: size,
      height: size,
      flexShrink: 0,
      background: "currentColor",
      WebkitMaskImage: url,
      maskImage: url,
      WebkitMaskRepeat: "no-repeat",
      maskRepeat: "no-repeat",
      WebkitMaskPosition: "center",
      maskPosition: "center",
      WebkitMaskSize: "contain",
      maskSize: "contain",
      ...extra
    },
    ...rest
  });
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Icon.jsx", error: String((e && e.message) || e) }); }

// components/core/IconButton.jsx
try { (() => {
const SIZES = {
  sm: 28,
  md: 34,
  lg: 42
};
function IconButton({
  icon,
  label,
  variant = "quiet",
  size = "md",
  disabled = false,
  onClick,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const d = SIZES[size] || SIZES.md;
  const quiet = variant === "quiet";
  return React.createElement("button", {
    type: "button",
    "aria-label": label,
    disabled,
    onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      width: d,
      height: d,
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      borderRadius: "var(--radius-md)",
      cursor: disabled ? "not-allowed" : "pointer",
      opacity: disabled ? 0.4 : 1,
      color: quiet ? hover ? "var(--bronze-600)" : "var(--ink-500)" : "var(--text-on-accent)",
      background: quiet ? hover ? "var(--parchment-100)" : "transparent" : hover ? "var(--bronze-600)" : "var(--bronze-500)",
      border: variant === "outline" ? "1px solid var(--line-hairline)" : "1px solid transparent",
      transition: "background var(--dur-fast) var(--ease-standard), color var(--dur-fast) var(--ease-standard)"
    },
    ...rest
  }, icon);
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/core/PlanCard.jsx
try { (() => {
function PlanCard({
  tier,
  name,
  tagline,
  deliverable,
  mode,
  action,
  emphasized = false
}) {
  return React.createElement(__ds_scope.Card, {
    padding: "lg",
    emphasized,
    interactive: true,
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-5)",
      height: "100%"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      justifyContent: "space-between",
      gap: "var(--space-4)"
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, tier), mode ? React.createElement(__ds_scope.Badge, {
    tone: emphasized ? "bronze" : "neutral",
    variant: emphasized ? "solid" : "soft"
  }, mode) : null), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)"
    }
  }, React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-2xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, name), tagline ? React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-md)",
      color: "var(--text-body)"
    }
  }, tagline) : null), React.createElement("div", {
    style: {
      marginTop: "auto",
      paddingTop: "var(--space-5)",
      borderTop: "1px solid var(--line-hairline)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)"
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, "\u8001\u677f\u5f97\u5230"), React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-sm)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-body)"
    }
  }, deliverable)), action ? React.createElement("div", null, action) : null);
}
Object.assign(__ds_scope, { PlanCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/PlanCard.jsx", error: String((e && e.message) || e) }); }

// components/core/Quote.jsx
try { (() => {
function Quote({
  children,
  author,
  role,
  tone = "default",
  size = "md"
}) {
  const inverse = tone === "inverse";
  return React.createElement("figure", {
    style: {
      margin: 0,
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-5)"
    }
  }, React.createElement("blockquote", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontWeight: "var(--weight-regular)",
      fontSize: size === "lg" ? "var(--text-2xl)" : size === "sm" ? "var(--text-md)" : "var(--text-lg)",
      lineHeight: "var(--leading-normal)",
      letterSpacing: "var(--tracking-cjk-body)",
      color: inverse ? "var(--text-on-inverse)" : "var(--text-display)"
    }
  }, "\u201c", children, "\u201d"), author ? React.createElement("figcaption", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)"
    }
  }, React.createElement("span", {
    style: {
      width: 18,
      height: 1,
      background: inverse ? "var(--line-inverse)" : "var(--line-strong)"
    }
  }), React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: inverse ? "rgba(239,230,210,.7)" : "var(--text-muted)"
    }
  }, role ? role + " " + author : author)) : null);
}
Object.assign(__ds_scope, { Quote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Quote.jsx", error: String((e && e.message) || e) }); }

// components/core/Seal.jsx
try { (() => {
/* Wordmark rule: TIANSIGHT reads as INSIGHT — the I N S I G H T letters are gold,
   the T and the A are ink. Never set the wordmark in a single flat color.
   The split has to survive at 14px, so it carries BOTH a value contrast (light gold
   against near-black ink — bronze-500 is too dark to separate on parchment) and a
   weight contrast (ink letters 700, gold letters 400). */
const WORDMARK = [["T", 0], ["I", 1], ["A", 0], ["N", 1], ["S", 1], ["I", 1], ["G", 1], ["H", 1], ["T", 1]];
function Wordmark({
  fontSize,
  inverse
}) {
  const ink = inverse ? "var(--parchment-100)" : "var(--ink-900)";
  const gold = inverse ? "var(--bronze-300)" : "var(--gold-400)";
  return React.createElement("span", {
    "aria-label": "TIANSIGHT",
    style: {
      fontFamily: "var(--font-quote)",
      fontSize,
      textTransform: "uppercase",
      letterSpacing: "var(--tracking-caps-wide)",
      lineHeight: 1.1,
      whiteSpace: "nowrap"
    }
  }, WORDMARK.map(function (l, i) {
    return React.createElement("span", {
      key: i,
      style: {
        color: l[1] ? gold : ink,
        fontWeight: l[1] ? "var(--weight-regular)" : "var(--weight-bold)"
      }
    }, l[0]);
  }));
}
function defaultSealSrc() {
  try {
    var s = document.querySelector('script[src*="_ds_bundle.js"]');
    if (s) return s.getAttribute("src").replace(/_ds_bundle\.js.*$/, "") + "assets/logo-seal.png";
  } catch (e) {}
  return "assets/logo-seal.png";
}
function Seal({
  size = 36,
  src = defaultSealSrc(),
  wordmark = false,
  subtitle,
  inverse = false,
  align = "row"
}) {
  const stack = align === "column";
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: stack ? "column" : "row",
      alignItems: "center",
      gap: stack ? "var(--space-4)" : "var(--space-3)",
      textAlign: stack ? "center" : "left"
    }
  }, React.createElement("img", {
    src,
    alt: "侍天 TIANSIGHT",
    width: size,
    height: size,
    style: {
      display: "block",
      width: size,
      height: size,
      opacity: inverse ? 0.92 : 1,
      filter: inverse ? "invert(1) sepia(.35) saturate(.6) brightness(1.15)" : "none"
    }
  }), wordmark ? React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "3px"
    }
  }, React.createElement(Wordmark, {
    fontSize: stack ? "var(--text-lg)" : "var(--text-sm)",
    inverse: inverse
  }), subtitle ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-3xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: inverse ? "rgba(239,230,210,.6)" : "var(--text-muted)"
    }
  }, subtitle) : null) : null);
}
Object.assign(__ds_scope, { Wordmark, Seal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Seal.jsx", error: String((e && e.message) || e) }); }

// components/core/SectionHeading.jsx
try { (() => {
function SectionHeading({
  eyebrow,
  title,
  subtitle,
  action,
  align = "left",
  tone = "default",
  rule = true,
  size = "lg"
}) {
  const inverse = tone === "inverse";
  const titleSize = size === "sm" ? "var(--text-xl)" : size === "md" ? "var(--text-2xl)" : "var(--text-3xl)";
  return React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "space-between",
      gap: "var(--space-7)",
      flexWrap: "wrap",
      textAlign: align,
      paddingBottom: rule ? "var(--space-4)" : 0,
      borderBottom: rule ? "1.5px solid " + (inverse ? "var(--line-inverse)" : "var(--line-rule)") : "none",
      justifyContent: align === "center" ? "center" : "space-between"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)",
      maxWidth: align === "center" ? "var(--measure)" : "none"
    }
  }, eyebrow ? React.createElement(__ds_scope.Eyebrow, {
    tone: inverse ? "inverse" : "bronze"
  }, eyebrow) : null, React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: titleSize,
      fontWeight: "var(--weight-medium)",
      lineHeight: "var(--leading-snug)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: inverse ? "var(--text-on-inverse)" : "var(--text-display)"
    }
  }, title), subtitle ? React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-md)",
      lineHeight: "var(--leading-normal)",
      color: inverse ? "rgba(239,230,210,.72)" : "var(--text-muted)",
      maxWidth: "var(--measure)"
    }
  }, subtitle) : null), action ? React.createElement("div", {
    style: {
      flexShrink: 0,
      paddingBottom: "4px"
    }
  }, action) : null);
}
Object.assign(__ds_scope, { SectionHeading });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/SectionHeading.jsx", error: String((e && e.message) || e) }); }

// components/core/Stat.jsx
try { (() => {
const TONES = {
  default: "var(--ink-900)",
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  inverse: "var(--parchment-100)"
};
const SIZES = {
  sm: "var(--text-2xl)",
  md: "var(--text-3xl)",
  lg: "var(--text-4xl)",
  xl: "var(--text-5xl)"
};
function Stat({
  value,
  unit,
  label,
  note,
  tone = "default",
  size = "md",
  align = "left"
}) {
  const inverse = tone === "inverse";
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)",
      textAlign: align,
      alignItems: align === "center" ? "center" : "flex-start"
    }
  }, label ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: inverse ? "rgba(239,230,210,.6)" : "var(--text-muted)"
    }
  }, label) : null, React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "4px"
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      fontVariantNumeric: "tabular-nums",
      fontSize: SIZES[size] || SIZES.md,
      fontWeight: "var(--weight-medium)",
      lineHeight: 1,
      color: TONES[tone] || TONES.default
    }
  }, value), unit ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-sm)",
      color: inverse ? "rgba(239,230,210,.7)" : "var(--text-muted)"
    }
  }, unit) : null), note ? React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: inverse ? "rgba(239,230,210,.6)" : "var(--text-muted)"
    }
  }, note) : null);
}
Object.assign(__ds_scope, { Stat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Stat.jsx", error: String((e && e.message) || e) }); }

// components/core/StepMarker.jsx
try { (() => {
const CN = ["\u4e00", "\u4e8c", "\u4e09", "\u56db", "\u4e94", "\u516d", "\u4e03", "\u516b", "\u4e5d"];
function StepMarker({
  index = 1,
  numeral,
  label,
  sublabel,
  active = false,
  size = 44,
  tone = "default"
}) {
  const inverse = tone === "inverse";
  const ch = numeral || CN[index - 1] || String(index);
  return React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-4)"
    }
  }, React.createElement("span", {
    style: {
      width: size,
      height: size,
      flexShrink: 0,
      borderRadius: "var(--radius-seal)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-display)",
      fontSize: Math.round(size * 0.42),
      fontWeight: "var(--weight-medium)",
      lineHeight: 1,
      border: "1px solid " + (active ? "var(--line-strong)" : inverse ? "var(--line-inverse)" : "var(--line-rule)"),
      background: active ? "var(--bronze-500)" : "transparent",
      color: active ? "var(--text-on-accent)" : inverse ? "var(--text-on-inverse)" : "var(--bronze-500)",
      transition: "all var(--dur-base) var(--ease-standard)"
    }
  }, ch), label ? React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: inverse ? "var(--text-on-inverse)" : "var(--text-display)"
    }
  }, label), sublabel ? React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: inverse ? "rgba(239,230,210,.65)" : "var(--text-muted)"
    }
  }, sublabel) : null) : null);
}
Object.assign(__ds_scope, { StepMarker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/StepMarker.jsx", error: String((e && e.message) || e) }); }

// components/core/Tag.jsx
try { (() => {
function Tag({
  children,
  active = false,
  onClick,
  onRemove,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const clickable = !!onClick;
  return React.createElement("span", {
    onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "8px",
      padding: "6px 14px",
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-xs)",
      fontWeight: active ? "var(--weight-medium)" : "var(--weight-regular)",
      color: active ? "var(--bronze-600)" : "var(--ink-600)",
      background: active ? "var(--parchment-100)" : hover && clickable ? "var(--parchment-100)" : "transparent",
      border: "1px solid " + (active ? "var(--line-strong)" : "var(--line-hairline)"),
      borderRadius: "var(--radius-pill)",
      cursor: clickable ? "pointer" : "default",
      transition: "all var(--dur-fast) var(--ease-standard)"
    },
    ...rest
  }, children, onRemove ? React.createElement("span", {
    onClick: onRemove,
    style: {
      cursor: "pointer",
      color: "var(--text-muted)",
      fontSize: "var(--text-2xs)"
    }
  }, "\u00d7") : null);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Tag.jsx", error: String((e && e.message) || e) }); }

// components/data/BarSeries.jsx
try { (() => {
const TONES = {
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  datum: "var(--datum-500)",
  muted: "var(--parchment-400)"
};
function BarSeries({
  data = [],
  max,
  unit,
  showValues = true,
  labelWidth = 96,
  height = 14
}) {
  const peak = max != null ? max : Math.max.apply(null, data.map(function (d) {
    return Math.abs(d.value);
  }).concat([1]));
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, data.map(function (d, i) {
    return React.createElement("div", {
      key: i,
      style: {
        display: "grid",
        gridTemplateColumns: labelWidth + "px 1fr " + (showValues ? "72px" : "0px"),
        alignItems: "center",
        gap: "var(--space-4)"
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-xs)",
        color: "var(--ink-700)",
        textAlign: "right",
        whiteSpace: "nowrap",
        overflow: "hidden",
        textOverflow: "ellipsis"
      }
    }, d.label), React.createElement("span", {
      style: {
        display: "block",
        height,
        background: "var(--parchment-300)",
        borderRadius: "var(--radius-xs)",
        overflow: "hidden"
      }
    }, React.createElement("span", {
      style: {
        display: "block",
        height: "100%",
        width: Math.max(1, Math.abs(d.value) / peak * 100) + "%",
        background: TONES[d.tone] || TONES.bronze,
        borderRadius: "var(--radius-xs)",
        transition: "width var(--dur-reveal) var(--ease-out)"
      }
    })), showValues ? React.createElement("span", {
      style: {
        fontFamily: "var(--font-numeral)",
        fontVariantNumeric: "tabular-nums",
        fontSize: "var(--text-2xs)",
        color: "var(--ink-600)",
        textAlign: "right"
      }
    }, d.value, unit ? React.createElement("span", {
      style: {
        color: "var(--text-muted)"
      }
    }, unit) : null) : null);
  }));
}
Object.assign(__ds_scope, { BarSeries });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/BarSeries.jsx", error: String((e && e.message) || e) }); }

// components/data/LedgerTable.jsx
try { (() => {
const TONES = {
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  datum: "var(--datum-500)",
  muted: "var(--parchment-400)"
};
function LedgerTable({
  columns = [],
  rows = [],
  caption,
  dense = false,
  footer
}) {
  const pad = dense ? "8px 12px" : "12px 14px";
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, caption ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, caption) : null, React.createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "collapse",
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderRadius: "var(--radius-sm)"
    }
  }, React.createElement("thead", null, React.createElement("tr", null, columns.map(function (c, i) {
    return React.createElement("th", {
      key: i,
      style: {
        padding: pad,
        textAlign: c.align || "left",
        fontFamily: "var(--font-body)",
        fontSize: "var(--text-2xs)",
        fontWeight: "var(--weight-medium)",
        letterSpacing: ".08em",
        textTransform: "uppercase",
        color: "var(--text-muted)",
        borderBottom: "1.5px solid var(--line-rule)",
        whiteSpace: "nowrap"
      }
    }, c.label);
  }))), React.createElement("tbody", null, rows.map(function (r, ri) {
    return React.createElement("tr", {
      key: ri
    }, columns.map(function (c, ci) {
      const cell = r[c.key];
      const tone = r[c.key + "Tone"];
      return React.createElement("td", {
        key: ci,
        style: {
          padding: pad,
          textAlign: c.align || "left",
          borderBottom: ri === rows.length - 1 ? "none" : "1px solid var(--line-hairline)",
          fontFamily: c.mono ? "var(--font-numeral)" : "var(--font-body)",
          fontVariantNumeric: c.mono ? "tabular-nums" : "normal",
          fontSize: c.mono ? "var(--text-xs)" : "var(--text-sm)",
          color: tone ? TONES[tone] : "var(--ink-800)",
          verticalAlign: "top",
          lineHeight: "var(--leading-snug)"
        }
      }, cell);
    }));
  }))), footer ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, footer) : null);
}
Object.assign(__ds_scope, { LedgerTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/LedgerTable.jsx", error: String((e && e.message) || e) }); }

// components/data/Matrix2x2.jsx
try { (() => {
const TONES = {
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  datum: "var(--datum-500)",
  muted: "var(--parchment-400)"
};
function Matrix2x2({
  xAxis,
  yAxis,
  quadrants = [],
  points = [],
  height = 300
}) {
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "22px 1fr",
      gap: "var(--space-3)"
    }
  }, React.createElement("span", {
    style: {
      writingMode: "vertical-rl",
      transform: "rotate(180deg)",
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)",
      justifySelf: "center",
      alignSelf: "center"
    }
  }, yAxis), React.createElement("div", {
    style: {
      position: "relative",
      height,
      border: "1px solid var(--line-rule)",
      background: "var(--surface-card)",
      borderRadius: "var(--radius-sm)",
      overflow: "hidden"
    }
  }, React.createElement("span", {
    style: {
      position: "absolute",
      left: 0,
      right: 0,
      top: "50%",
      height: 1,
      background: "var(--line-hairline)"
    }
  }), React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: "50%",
      width: 1,
      background: "var(--line-hairline)"
    }
  }), quadrants.map(function (q, i) {
    const pos = [{
      top: 10,
      left: 12
    }, {
      top: 10,
      right: 12
    }, {
      bottom: 10,
      left: 12
    }, {
      bottom: 10,
      right: 12
    }][i] || {};
    return React.createElement("span", {
      key: i,
      style: Object.assign({
        position: "absolute",
        fontSize: "var(--text-2xs)",
        lineHeight: 1.5,
        maxWidth: "42%",
        color: TONES[q.tone] || "var(--text-muted)",
        textAlign: pos.right != null ? "right" : "left"
      }, pos)
    }, q.label);
  }), points.map(function (p, i) {
    return React.createElement("span", {
      key: i,
      style: {
        position: "absolute",
        left: p.x * 100 + "%",
        bottom: p.y * 100 + "%",
        transform: "translate(-50%, 50%)",
        display: "flex",
        alignItems: "center",
        gap: "6px",
        whiteSpace: "nowrap"
      }
    }, React.createElement("span", {
      style: {
        width: p.size || 9,
        height: p.size || 9,
        borderRadius: "var(--radius-pill)",
        background: TONES[p.tone] || TONES.bronze,
        flexShrink: 0,
        boxShadow: "0 0 0 3px var(--surface-card)"
      }
    }), React.createElement("span", {
      style: {
        fontSize: "var(--text-3xs)",
        color: "var(--ink-600)"
      }
    }, p.label));
  }))), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "22px 1fr"
    }
  }, React.createElement("span", null), React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)",
      textAlign: "center"
    }
  }, xAxis)));
}
Object.assign(__ds_scope, { Matrix2x2 });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Matrix2x2.jsx", error: String((e && e.message) || e) }); }

// components/data/MetricRow.jsx
try { (() => {
const TONES = {
  bronze: "var(--bronze-500)",
  growth: "var(--growth-500)",
  loss: "var(--loss-500)",
  caution: "var(--caution-500)",
  datum: "var(--datum-500)",
  muted: "var(--parchment-400)"
};
function MetricRow({
  label,
  sublabel,
  value,
  unit,
  delta,
  deltaTone = "growth",
  note,
  dense = false
}) {
  return React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr auto",
      alignItems: "baseline",
      gap: "var(--space-4)",
      padding: (dense ? "10px" : "14px") + " 0",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      color: "var(--ink-800)"
    }
  }, label), sublabel ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, sublabel) : null, note ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, note) : null), React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-3)"
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      fontVariantNumeric: "tabular-nums",
      fontSize: "var(--text-lg)",
      fontWeight: "var(--weight-medium)",
      color: "var(--ink-900)"
    }
  }, value, unit ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)",
      marginLeft: 3
    }
  }, unit) : null), delta != null ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      fontSize: "var(--text-2xs)",
      color: TONES[deltaTone] || TONES.datum,
      minWidth: 52,
      textAlign: "right"
    }
  }, delta) : null));
}
Object.assign(__ds_scope, { MetricRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/MetricRow.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function Checkbox({
  label,
  description,
  checked,
  defaultChecked,
  onChange,
  disabled = false,
  id,
  ...rest
}) {
  const uid = id || React.useId();
  return React.createElement("label", {
    htmlFor: uid,
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "flex-start",
      cursor: disabled ? "not-allowed" : "pointer",
      opacity: disabled ? 0.45 : 1
    }
  }, React.createElement("input", {
    type: "checkbox",
    id: uid,
    checked,
    defaultChecked,
    onChange,
    disabled,
    style: {
      width: 15,
      height: 15,
      marginTop: 3,
      accentColor: "var(--bronze-500)",
      flexShrink: 0,
      borderRadius: "var(--radius-xs)"
    },
    ...rest
  }), React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      color: "var(--ink-800)"
    }
  }, label), description ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, description) : null));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/Input.jsx
try { (() => {
function Input({
  label,
  hint,
  error,
  prefix,
  suffix,
  size = "md",
  disabled = false,
  required = false,
  id,
  style: extra,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const uid = id || React.useId();
  const pad = size === "sm" ? "7px 10px" : "10px 12px";
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)",
      ...extra
    }
  }, label ? React.createElement("label", {
    htmlFor: uid,
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-xs)",
      fontWeight: "var(--weight-medium)",
      color: "var(--ink-700)"
    }
  }, label, required ? React.createElement("span", {
    style: {
      color: "var(--loss-500)",
      marginLeft: 4
    }
  }, "*") : null) : null, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "8px",
      padding: pad,
      background: disabled ? "var(--parchment-100)" : "var(--surface-card)",
      border: "1px solid " + (error ? "var(--loss-500)" : focus ? "var(--line-strong)" : "var(--line-hairline)"),
      borderRadius: "var(--radius-md)",
      boxShadow: focus ? "0 0 0 2px var(--focus-ring)" : "none",
      opacity: disabled ? 0.55 : 1,
      transition: "border-color var(--dur-fast) var(--ease-standard), box-shadow var(--dur-fast) var(--ease-standard)"
    }
  }, prefix ? React.createElement("span", {
    style: {
      color: "var(--text-muted)",
      display: "flex"
    }
  }, prefix) : null, React.createElement("input", {
    id: uid,
    disabled,
    required,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      border: "none",
      outline: "none",
      background: "transparent",
      fontFamily: "var(--font-body)",
      fontSize: size === "sm" ? "var(--text-xs)" : "var(--text-sm)",
      color: "var(--ink-800)",
      letterSpacing: "var(--tracking-cjk-body)"
    },
    ...rest
  }), suffix ? React.createElement("span", {
    style: {
      color: "var(--text-muted)",
      fontSize: "var(--text-xs)",
      display: "flex"
    }
  }, suffix) : null), error || hint ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: error ? "var(--loss-500)" : "var(--text-muted)"
    }
  }, error || hint) : null);
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Input.jsx", error: String((e && e.message) || e) }); }

// components/forms/Radio.jsx
try { (() => {
function Radio({
  label,
  description,
  name,
  value,
  checked,
  onChange,
  disabled = false,
  id,
  ...rest
}) {
  const uid = id || React.useId();
  return React.createElement("label", {
    htmlFor: uid,
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "flex-start",
      cursor: disabled ? "not-allowed" : "pointer",
      opacity: disabled ? 0.45 : 1
    }
  }, React.createElement("input", {
    type: "radio",
    id: uid,
    name,
    value,
    checked,
    onChange,
    disabled,
    style: {
      width: 15,
      height: 15,
      marginTop: 3,
      accentColor: "var(--bronze-500)",
      flexShrink: 0
    },
    ...rest
  }), React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      color: "var(--ink-800)"
    }
  }, label), description ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, description) : null));
}
Object.assign(__ds_scope, { Radio });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Radio.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function Select({
  label,
  hint,
  error,
  options = [],
  size = "md",
  disabled = false,
  id,
  style: extra,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const uid = id || React.useId();
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)",
      ...extra
    }
  }, label ? React.createElement("label", {
    htmlFor: uid,
    style: {
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-xs)",
      fontWeight: "var(--weight-medium)",
      color: "var(--ink-700)"
    }
  }, label) : null, React.createElement("div", {
    style: {
      position: "relative",
      display: "flex"
    }
  }, React.createElement("select", {
    id: uid,
    disabled,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      appearance: "none",
      WebkitAppearance: "none",
      width: "100%",
      padding: size === "sm" ? "7px 32px 7px 10px" : "10px 34px 10px 12px",
      fontFamily: "var(--font-body)",
      fontSize: size === "sm" ? "var(--text-xs)" : "var(--text-sm)",
      color: "var(--ink-800)",
      background: disabled ? "var(--parchment-100)" : "var(--surface-card)",
      border: "1px solid " + (error ? "var(--loss-500)" : focus ? "var(--line-strong)" : "var(--line-hairline)"),
      borderRadius: "var(--radius-md)",
      outline: "none",
      boxShadow: focus ? "0 0 0 2px var(--focus-ring)" : "none",
      opacity: disabled ? 0.55 : 1,
      cursor: disabled ? "not-allowed" : "pointer"
    },
    ...rest
  }, options.map(function (o) {
    const v = typeof o === "string" ? o : o.value;
    const l = typeof o === "string" ? o : o.label;
    return React.createElement("option", {
      key: v,
      value: v
    }, l);
  })), React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      right: 12,
      top: "50%",
      transform: "translateY(-50%)",
      pointerEvents: "none",
      color: "var(--bronze-500)",
      fontSize: "10px"
    }
  }, "\u25be")), error || hint ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: error ? "var(--loss-500)" : "var(--text-muted)"
    }
  }, error || hint) : null);
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function Switch({
  label,
  description,
  checked = false,
  onChange,
  disabled = false,
  id
}) {
  const uid = id || React.useId();
  return React.createElement("label", {
    htmlFor: uid,
    style: {
      display: "flex",
      gap: "var(--space-4)",
      alignItems: "center",
      justifyContent: "space-between",
      cursor: disabled ? "not-allowed" : "pointer",
      opacity: disabled ? 0.45 : 1
    }
  }, React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      color: "var(--ink-800)"
    }
  }, label), description ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, description) : null), React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-flex",
      flexShrink: 0
    }
  }, React.createElement("input", {
    type: "checkbox",
    role: "switch",
    id: uid,
    checked,
    onChange,
    disabled,
    style: {
      position: "absolute",
      opacity: 0,
      width: 40,
      height: 22,
      margin: 0,
      cursor: "inherit"
    }
  }), React.createElement("span", {
    style: {
      width: 40,
      height: 22,
      borderRadius: "var(--radius-pill)",
      background: checked ? "var(--bronze-500)" : "var(--parchment-300)",
      border: "1px solid " + (checked ? "var(--bronze-500)" : "var(--line-hairline)"),
      transition: "background var(--dur-base) var(--ease-standard)",
      display: "block"
    }
  }), React.createElement("span", {
    style: {
      position: "absolute",
      top: 3,
      left: checked ? 21 : 3,
      width: 16,
      height: 16,
      borderRadius: "var(--radius-pill)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-xs)",
      transition: "left var(--dur-base) var(--ease-standard)"
    }
  })));
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/people/ExpertCard.jsx
try { (() => {
/* 专家顾问团 seat (X · 增值专项): portrait + short 头衔. No photos were supplied, so
   an absent `photo` renders a labelled placeholder — never a drawn avatar or an emoji. */
function ExpertCard({
  name,
  title,
  field,
  photo,
  tags = [],
  layout = "stack",
  size = 132,
  interactive = false,
  onClick
}) {
  const [hover, setHover] = React.useState(false);
  const row = layout === "row";
  const portrait = photo ? React.createElement("img", {
    src: photo,
    alt: name,
    style: {
      width: size,
      height: size,
      objectFit: "cover",
      borderRadius: "50%",
      display: "block",
      flexShrink: 0,
      filter: "saturate(.85)"
    }
  }) : React.createElement("div", {
    "aria-label": name + " 头像待补",
    style: {
      width: size,
      height: size,
      flexShrink: 0,
      borderRadius: "50%",
      background: "var(--parchment-300)",
      border: "1px dashed var(--line-rule)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontSize: "var(--text-3xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--ink-500)",
      textAlign: "center"
    }
  }, "专家头像");
  return React.createElement("div", {
    onClick: onClick,
    onMouseEnter: interactive ? function () {
      setHover(true);
    } : undefined,
    onMouseLeave: interactive ? function () {
      setHover(false);
    } : undefined,
    style: {
      display: "flex",
      flexDirection: row ? "row" : "column",
      gap: row ? "var(--space-5)" : "var(--space-4)",
      alignItems: row ? "center" : "flex-start",
      minWidth: 0,
      cursor: interactive ? "pointer" : "default",
      opacity: interactive && hover ? .88 : 1,
      transition: "opacity var(--dur-fast) var(--ease-standard)"
    }
  }, portrait, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8,
      minWidth: 0
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, name), React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      color: "var(--text-accent)",
      lineHeight: "var(--leading-snug)"
    }
  }, title), field ? React.createElement("span", {
    style: {
      paddingTop: 8,
      borderTop: "1px solid var(--line-hairline)",
      fontSize: "var(--text-xs)",
      color: "var(--text-muted)"
    }
  }, field) : null, tags.length ? React.createElement("span", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 6,
      paddingTop: 4
    }
  }, tags.map(function (t) {
    return React.createElement("span", {
      key: t,
      style: {
        padding: "2px 8px",
        fontSize: "var(--text-3xs)",
        color: "var(--ink-600)",
        background: "var(--parchment-100)",
        border: "1px solid var(--line-hairline)",
        borderRadius: "var(--radius-xs)"
      }
    }, t);
  })) : null));
}
Object.assign(__ds_scope, { ExpertCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/people/ExpertCard.jsx", error: String((e && e.message) || e) }); }

// components/people/PartnerCase.jsx
try { (() => {
/* 伙伴案例: one partner's story. Numbers are before/after pairs so the claim is
   verifiable — matching the brand rule that every result carries its evidence. */
function PartnerCase({
  brand,
  kind,
  stores,
  since,
  summary,
  metrics = [],
  quote,
  actions = [],
  logo
}) {
  return React.createElement("article", {
    style: {
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderTop: "2px solid var(--bronze-500)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-sm)",
      padding: "var(--space-7)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-6)"
    }
  }, React.createElement("header", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      justifyContent: "space-between",
      gap: "var(--space-6)",
      flexWrap: "wrap"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-5)"
    }
  }, logo ? React.createElement("img", {
    src: logo,
    alt: brand,
    style: {
      height: 44,
      maxWidth: 140,
      objectFit: "contain"
    }
  }) : null, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-2xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, brand), React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: "var(--text-muted)"
    }
  }, [kind, stores ? stores + " 家门店" : null, since ? "自 " + since : null].filter(Boolean).join(" · "))))), summary ? React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-md)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-body)",
      maxWidth: "40em"
    }
  }, summary) : null, metrics.length ? React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.min(metrics.length, 4) + ", minmax(0,1fr))",
      gap: "var(--space-6)",
      paddingTop: "var(--space-5)",
      borderTop: "1px solid var(--line-hairline)"
    }
  }, metrics.map(function (m) {
    return React.createElement("div", {
      key: m.label,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 8
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-2xs)",
        letterSpacing: "var(--tracking-caps)",
        textTransform: "uppercase",
        color: "var(--text-muted)"
      }
    }, m.label), React.createElement("span", {
      style: {
        display: "flex",
        alignItems: "baseline",
        gap: 8,
        fontFamily: "var(--font-numeral)"
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-sm)",
        color: "var(--text-muted)",
        textDecoration: "line-through"
      }
    }, m.before), React.createElement("span", {
      style: {
        fontSize: "var(--text-2xs)",
        color: "var(--text-muted)"
      }
    }, "\u2192"), React.createElement("span", {
      style: {
        fontSize: "var(--text-2xl)",
        fontWeight: 500,
        color: m.tone === "loss" ? "var(--loss-500)" : "var(--growth-500)"
      }
    }, m.after)), m.delta ? React.createElement("span", {
      style: {
        fontFamily: "var(--font-numeral)",
        fontSize: "var(--text-2xs)",
        color: m.tone === "loss" ? "var(--loss-500)" : "var(--growth-500)"
      }
    }, m.delta) : null);
  })) : null, actions.length ? React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)",
      paddingTop: "var(--space-5)",
      borderTop: "1px solid var(--line-hairline)"
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, "\u6267\u884c\u4e86\u4ec0\u4e48"), actions.map(function (a, i) {
    return React.createElement("div", {
      key: i,
      style: {
        display: "flex",
        gap: 12,
        fontSize: "var(--text-sm)",
        color: "var(--ink-800)"
      }
    }, React.createElement("span", {
      style: {
        fontFamily: "var(--font-numeral)",
        color: "var(--bronze-500)",
        flexShrink: 0
      }
    }, String(i + 1).padStart(2, "0")), React.createElement("span", null, a));
  })) : null, quote ? React.createElement("figure", {
    style: {
      margin: 0,
      paddingTop: "var(--space-5)",
      borderTop: "1px solid var(--line-hairline)",
      display: "flex",
      flexDirection: "column",
      gap: 14
    }
  }, React.createElement("blockquote", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-display)"
    }
  }, "\u201c", quote.text, "\u201d"), React.createElement("figcaption", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 12,
      fontSize: "var(--text-xs)",
      color: "var(--text-muted)"
    }
  }, React.createElement("span", {
    style: {
      width: 18,
      height: 1,
      background: "var(--line-strong)"
    }
  }), (quote.role ? quote.role + " " : "") + quote.by)) : null);
}
Object.assign(__ds_scope, { PartnerCase });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/people/PartnerCase.jsx", error: String((e && e.message) || e) }); }

// slides/SlideFrame.jsx
try { (() => {
/* Resolve brand assets from wherever _ds_bundle.js was loaded, so slides work in the
   card grid, in templates/, and in a consuming project's _ds/ tree alike. */
function dsAsset(file) {
  try {
    var s = document.querySelector('script[src*="_ds_bundle.js"]');
    if (s) return s.getAttribute("src").replace(/_ds_bundle\.js.*$/, "") + "assets/" + file;
  } catch (e) {}
  return "../assets/" + file;
}
const GROUNDS = {
  parchment: {
    bg: "var(--surface-page)",
    fg: "var(--text-body)",
    head: "var(--text-display)"
  },
  paper: {
    bg: "var(--surface-card)",
    fg: "var(--text-body)",
    head: "var(--text-display)"
  },
  muted: {
    bg: "var(--parchment-100)",
    fg: "var(--text-body)",
    head: "var(--text-display)"
  },
  ink: {
    bg: "var(--surface-inverse)",
    fg: "rgba(239,230,210,.72)",
    head: "var(--text-on-inverse)"
  }
};

/* 1280×720 slide shell. Owns the ground, the 72px margin, the hairline footer,
   the seal and the page number — so no slide re-invents deck furniture. */
function SlideFrame({
  children,
  ground = "parchment",
  eyebrow,
  page,
  footer,
  grain = false,
  seal = true,
  pad = 72
}) {
  const g = GROUNDS[ground] || GROUNDS.parchment;
  const inverse = ground === "ink";
  return React.createElement("div", {
    className: grain ? "ts-paper" : undefined,
    style: {
      position: "relative",
      width: 1280,
      height: 720,
      boxSizing: "border-box",
      overflow: "hidden",
      background: grain ? undefined : g.bg,
      color: g.fg,
      fontFamily: "var(--font-body)",
      display: "flex",
      flexDirection: "column",
      padding: pad + "px " + pad + "px " + (pad - 20) + "px"
    }
  }, eyebrow ? React.createElement("div", {
    style: {
      fontSize: "var(--text-sm)",
      fontWeight: "var(--weight-medium)",
      textTransform: "uppercase",
      letterSpacing: "var(--tracking-caps)",
      color: inverse ? "rgba(239,230,210,.65)" : "var(--text-accent)",
      marginBottom: 28,
      flexShrink: 0
    }
  }, eyebrow) : null, React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: "flex",
      flexDirection: "column"
    }
  }, children), React.createElement("div", {
    style: {
      flexShrink: 0,
      marginTop: 26,
      paddingTop: 16,
      borderTop: "1px solid " + (inverse ? "var(--line-inverse)" : "var(--line-hairline)"),
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: 24,
      fontSize: "var(--text-2xs)",
      color: inverse ? "rgba(239,230,210,.5)" : "var(--text-muted)"
    }
  }, React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10
    }
  }, seal ? React.createElement("img", {
    src: dsAsset("logo-seal.png"),
    alt: "",
    width: 20,
    height: 20,
    style: {
      display: "block",
      filter: inverse ? "invert(1) sepia(.35) saturate(.6) brightness(1.15)" : "none",
      opacity: inverse ? .9 : .8
    }
  }) : null, React.createElement("span", null, footer || "侍天 TIANSIGHT · 餐饮第二大脑")), page != null ? React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      letterSpacing: 0
    }
  }, String(page).padStart(2, "0")) : null));
}

/* Slide-scale heading — display serif, tuned so 1280×720 text never drops below 24px. */
function SlideTitle({
  children,
  size = "lg",
  ground = "parchment",
  style: extra
}) {
  const px = size === "xl" ? 88 : size === "lg" ? 62 : size === "md" ? 46 : 36;
  return React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: px,
      fontWeight: "var(--weight-medium)",
      lineHeight: 1.16,
      letterSpacing: "var(--tracking-cjk-display)",
      color: ground === "ink" ? "var(--text-on-inverse)" : "var(--text-display)",
      ...extra
    }
  }, children);
}

/* Labelled placeholder for imagery the brand has not supplied (portraits, QR, photos). */
function SlidePlaceholder({
  label,
  aspect = "1 / 1",
  radius = "var(--radius-md)",
  round = false,
  style: extra
}) {
  return React.createElement("div", {
    style: {
      aspectRatio: aspect,
      width: "100%",
      background: "var(--parchment-300)",
      border: "1px dashed var(--line-rule)",
      borderRadius: round ? "50%" : radius,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      textAlign: "center",
      fontFamily: "var(--font-body)",
      fontSize: "var(--text-2xs)",
      color: "var(--ink-500)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      padding: 12,
      ...extra
    }
  }, label);
}
Object.assign(__ds_scope, { dsAsset, SlideFrame, SlideTitle, SlidePlaceholder });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/SlideFrame.jsx", error: String((e && e.message) || e) }); }

// slides/ChartSlide.jsx
try { (() => {
/* Chart slide: one graphic, one 结论, one 动作. Pass the chart as `chart` — reuse the
   data components (BarSeries / Matrix2x2) rather than drawing a slide-only chart. */
function ChartSlide({
  eyebrow = "分析维度 · 经营洞察图谱",
  title = "增长在何处，利润失于何处",
  chartTitle,
  chart,
  takeaway,
  action,
  source,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "paper",
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "grid",
      gridTemplateColumns: "minmax(0,1.35fr) minmax(0,.65fr)",
      gap: 60,
      alignItems: "stretch"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 24,
      minWidth: 0
    }
  }, chartTitle ? React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, chartTitle) : null, React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center"
    }
  }, chart), source ? React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, source) : null), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 30,
      paddingLeft: 44,
      borderLeft: "1px solid var(--line-hairline)"
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "sm"
  }, title), takeaway ? React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 12
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, "结论"), React.createElement("span", {
    style: {
      fontSize: 24,
      lineHeight: "var(--leading-normal)",
      color: "var(--ink-800)"
    }
  }, takeaway)) : null, action ? React.createElement("div", {
    style: {
      marginTop: "auto",
      paddingTop: 26,
      borderTop: "1.5px solid var(--line-rule)",
      display: "flex",
      flexDirection: "column",
      gap: 12
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-sm)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, "先改这件"), React.createElement("span", {
    style: {
      fontSize: 24,
      lineHeight: "var(--leading-normal)",
      color: "var(--ink-800)"
    }
  }, action)) : null)));
}
Object.assign(__ds_scope, { ChartSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/ChartSlide.jsx", error: String((e && e.message) || e) }); }

// slides/ContactSlide.jsx
try { (() => {
function ContactSlide({
  title = "携手侍天，持续领先。",
  body = "以最近 6 个月经营数据为始，7 日内交付经营诊断：增长机会、利润流失、行动次序、验收标准。",
  cta = "微信扫码联系侍天",
  qr,
  tagline = "Time is money. With TIANSIGHT, lose neither.",
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "ink",
    page: page,
    seal: false,
    footer: "侍天 TIANSIGHT / Table AI Alliance"
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) auto",
      gap: 80,
      alignItems: "center"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 34
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "lg",
    ground: "ink"
  }, title), React.createElement("p", {
    style: {
      margin: 0,
      maxWidth: "24em",
      fontSize: 25,
      lineHeight: "var(--leading-normal)",
      color: "rgba(239,230,210,.72)"
    }
  }, body), React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-quote)",
      fontStyle: "italic",
      fontSize: 24,
      color: "var(--bronze-300)"
    }
  }, tagline)), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 22,
      width: 260
    }
  }, React.createElement("div", {
    style: {
      width: 220,
      background: "var(--paper)",
      padding: 14,
      borderRadius: "var(--radius-md)"
    }
  }, qr ? React.createElement("img", {
    src: qr,
    alt: cta,
    style: {
      width: "100%",
      display: "block"
    }
  }) : React.createElement(__ds_scope.SlidePlaceholder, {
    label: "微信二维码",
    style: {
      background: "var(--parchment-200)",
      border: "1px dashed var(--line-rule)"
    }
  })), React.createElement("span", {
    style: {
      fontSize: 21,
      color: "rgba(239,230,210,.72)",
      textAlign: "center"
    }
  }, cta), React.createElement("img", {
    src: __ds_scope.dsAsset("logo-seal.png"),
    alt: "侍天",
    width: 56,
    height: 56,
    style: {
      display: "block",
      filter: "invert(1) sepia(.35) saturate(.6) brightness(1.15)",
      opacity: .9
    }
  }))));
}
Object.assign(__ds_scope, { ContactSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/ContactSlide.jsx", error: String((e && e.message) || e) }); }

// slides/ExpertTeamSlide.jsx
try { (() => {
/* 专家顾问团 (X · 增值专项). Portrait + short title, per brand: no photos have been
   supplied, so each seat renders a labelled placeholder until `photo` is passed. */
const DEFAULT_EXPERTS = [{
  name: "专家姓名",
  title: "淮扬菜出品顾问",
  field: "菜品结构 · 出餐标准"
}, {
  name: "专家姓名",
  title: "连锁运营顾问",
  field: "门店标准 · 人效"
}, {
  name: "专家姓名",
  title: "供应链顾问",
  field: "成本优化 · 损耗"
}, {
  name: "专家姓名",
  title: "会员增长顾问",
  field: "复购分层 · 渠道"
}];
function ExpertTeamSlide({
  eyebrow = "X · 增值专项",
  title = "专家顾问团，随层挂接",
  subtitle = "行业专家与侍天系统并行：判断有人背书，动作有人带教。",
  experts = DEFAULT_EXPERTS,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "paper",
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      gap: 40
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 24,
      color: "var(--text-muted)",
      maxWidth: "36em"
    }
  }, subtitle)), React.createElement("div", {
    style: {
      flex: 1,
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: 36
    }
  }, experts.map(function (e, i) {
    return React.createElement("div", {
      key: i,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 20
      }
    }, React.createElement("div", {
      style: {
        width: "100%",
        maxWidth: 168
      }
    }, e.photo ? React.createElement("img", {
      src: e.photo,
      alt: e.name,
      style: {
        width: "100%",
        aspectRatio: "1 / 1",
        objectFit: "cover",
        borderRadius: "50%",
        display: "block",
        filter: "saturate(.85)"
      }
    }) : React.createElement(__ds_scope.SlidePlaceholder, {
      label: "专家头像",
      round: true
    })), React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 10
      }
    }, React.createElement("span", {
      style: {
        fontFamily: "var(--font-display)",
        fontSize: 30,
        fontWeight: 500,
        letterSpacing: "var(--tracking-cjk-display)",
        color: "var(--text-display)"
      }
    }, e.name), React.createElement("span", {
      style: {
        fontSize: 21,
        color: "var(--text-accent)",
        lineHeight: "var(--leading-snug)"
      }
    }, e.title), e.field ? React.createElement("span", {
      style: {
        paddingTop: 10,
        borderTop: "1px solid var(--line-hairline)",
        fontSize: 19,
        color: "var(--text-muted)"
      }
    }, e.field) : null));
  }))));
}
Object.assign(__ds_scope, { ExpertTeamSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/ExpertTeamSlide.jsx", error: String((e && e.message) || e) }); }

// slides/FiveQuestionsSlide.jsx
try { (() => {
const CN = ["一", "二", "三", "四", "五"];
const DEFAULT_STEPS = [{
  label: "现状",
  q: "现在怎样",
  aim: "看清经营"
}, {
  label: "机会",
  q: "机会在哪",
  aim: "发现增长点"
}, {
  label: "优先",
  q: "先做什么",
  aim: "判断顺序"
}, {
  label: "行动",
  q: "谁来负责",
  aim: "形成清单"
}, {
  label: "结果",
  q: "是否有效",
  aim: "验证结果"
}];
function FiveQuestionsSlide({
  eyebrow = "经营五问",
  title = "五问既明，百事可决",
  note = "逐店逐月，循环推演。经验证有效的做法沉淀为门店标准，不随人员流动而流失。",
  steps = DEFAULT_STEPS,
  active,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "parchment",
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 52
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("div", {
    style: {
      position: "relative",
      display: "grid",
      gridTemplateColumns: "repeat(5, minmax(0,1fr))",
      gap: 24
    }
  }, React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      left: 28,
      right: 28,
      top: 27,
      height: 1,
      background: "var(--line-rule)"
    }
  }), steps.map(function (s, i) {
    const on = active === i;
    return React.createElement("div", {
      key: s.label,
      style: {
        position: "relative",
        display: "flex",
        flexDirection: "column",
        gap: 18
      }
    }, React.createElement("span", {
      style: {
        width: 56,
        height: 56,
        borderRadius: "50%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "var(--font-display)",
        fontSize: 25,
        fontWeight: 500,
        border: "1px solid " + (on ? "var(--bronze-500)" : "var(--line-rule)"),
        background: on ? "var(--bronze-500)" : "var(--surface-page)",
        color: on ? "var(--text-on-accent)" : "var(--bronze-500)"
      }
    }, CN[i]), React.createElement("span", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 8
      }
    }, React.createElement("span", {
      style: {
        fontFamily: "var(--font-display)",
        fontSize: 34,
        fontWeight: 500,
        letterSpacing: "var(--tracking-cjk-display)",
        color: "var(--text-display)"
      }
    }, s.label), React.createElement("span", {
      style: {
        fontSize: 20,
        color: "var(--text-accent)"
      }
    }, s.q), React.createElement("span", {
      style: {
        fontSize: 20,
        color: "var(--text-muted)"
      }
    }, s.aim)));
  })), React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: 24,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--ink-700)"
    }
  }, note)));
}
Object.assign(__ds_scope, { FiveQuestionsSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/FiveQuestionsSlide.jsx", error: String((e && e.message) || e) }); }

// slides/LadderSlide.jsx
try { (() => {
const CN = ["一", "二", "三", "X"];
const DEFAULT_TIERS = [{
  tier: "第一层 · 经营洞察",
  name: "先把问题看清",
  got: "增长机会、利润流失点与 90 天行动清单"
}, {
  tier: "第二层 · 第二大脑共建",
  name: "让系统昼夜守望经营",
  mode: "深度共建",
  got: "打通收银、平台与会员数据：自动对账、异常预警、决策看板"
}, {
  tier: "第三层 · AI 原生品牌",
  name: "从零构建一个新品牌",
  mode: "从 0 共创",
  got: "品牌、组织、数字底座一体化设计，数据资产归品牌自己"
}, {
  tier: "X · 增值专项",
  name: "随层挂接，按需启用",
  mode: "按需挂接",
  got: "企业大学、菜单结构、运营托管、成本优化、专家顾问团支持"
}];
function LadderSlide({
  eyebrow = "服务与价格",
  title = "层层递进，天天有数",
  tiers = DEFAULT_TIERS,
  highlight = 1,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "parchment",
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      gap: 34
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("div", {
    style: {
      flex: 1,
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: 22
    }
  }, tiers.map(function (t, i) {
    const on = i === highlight;
    return React.createElement("div", {
      key: t.tier,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 18,
        padding: 28,
        background: on ? "var(--surface-card)" : "var(--parchment-100)",
        border: "1px solid var(--line-hairline)",
        borderTop: "2px solid " + (on ? "var(--bronze-500)" : "var(--line-hairline)"),
        borderRadius: "var(--radius-md)",
        boxShadow: on ? "var(--shadow-md)" : "none"
      }
    }, React.createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        gap: 12
      }
    }, React.createElement("span", {
      style: {
        width: 40,
        height: 40,
        borderRadius: "50%",
        border: "1px solid var(--line-rule)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "var(--font-display)",
        fontSize: 19,
        color: "var(--bronze-500)"
      }
    }, CN[i]), t.mode ? React.createElement("span", {
      style: {
        padding: "4px 10px",
        fontSize: "var(--text-2xs)",
        borderRadius: "var(--radius-xs)",
        background: on ? "var(--bronze-500)" : "var(--parchment-200)",
        color: on ? "var(--text-on-accent)" : "var(--ink-600)"
      }
    }, t.mode) : null), React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 10
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-xs)",
        letterSpacing: "var(--tracking-caps)",
        textTransform: "uppercase",
        color: "var(--text-accent)"
      }
    }, t.tier), React.createElement("span", {
      style: {
        fontFamily: "var(--font-display)",
        fontSize: 28,
        fontWeight: 500,
        lineHeight: 1.25,
        letterSpacing: "var(--tracking-cjk-display)",
        color: "var(--text-display)"
      }
    }, t.name)), React.createElement("div", {
      style: {
        marginTop: "auto",
        paddingTop: 18,
        borderTop: "1px solid var(--line-hairline)",
        display: "flex",
        flexDirection: "column",
        gap: 8
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-2xs)",
        letterSpacing: "var(--tracking-caps)",
        textTransform: "uppercase",
        color: "var(--text-muted)"
      }
    }, "老板得到"), React.createElement("span", {
      style: {
        fontSize: 19,
        lineHeight: "var(--leading-snug)",
        color: "var(--text-body)"
      }
    }, t.got)));
  }))));
}
Object.assign(__ds_scope, { LadderSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/LadderSlide.jsx", error: String((e && e.message) || e) }); }

// slides/PartnerWallSlide.jsx
try { (() => {
const DEFAULT_PARTNERS = ["苏帮袁", "清水亭", "3699 河鲜小馆", "游园京梦", "吴裕泰", "韵 1980 新派淮扬菜", "潮发潮汕牛肉", "更多伙伴"];
const DEFAULT_QUOTE = {
  text: "报告分析得非常细，最关键的是结论有证据，不靠猜，也没有 AI 幻觉。大力推荐。",
  by: "张总",
  role: "餐饮老板"
};

/* Partner names are set in TYPE — no logo artwork has been supplied. Pass `logos`
   as [{name, src}] once real marks exist and they render in place of the type. */
function PartnerWallSlide({
  eyebrow = "伙伴背书",
  title = "这些品牌，已与侍天同行",
  subtitle = "从单店到连锁，从新派淮扬菜到老字号。",
  partners = DEFAULT_PARTNERS,
  logos,
  quote = DEFAULT_QUOTE,
  page
}) {
  const items = logos && logos.length ? logos : partners.map(function (p) {
    return {
      name: p
    };
  });
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "parchment",
    grain: true,
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      gap: 34
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "space-between",
      gap: 40,
      paddingBottom: 20,
      borderBottom: "1.5px solid var(--line-rule)"
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("span", {
    style: {
      fontSize: 22,
      color: "var(--text-muted)",
      flexShrink: 0
    }
  }, subtitle)), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: 1,
      background: "var(--line-hairline)",
      border: "1px solid var(--line-hairline)",
      borderRadius: "var(--radius-md)",
      overflow: "hidden"
    }
  }, items.map(function (it, i) {
    return React.createElement("div", {
      key: it.name,
      style: {
        background: "var(--surface-card)",
        height: 108,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: 16,
        textAlign: "center"
      }
    }, it.src ? React.createElement("img", {
      src: it.src,
      alt: it.name,
      style: {
        maxWidth: "80%",
        maxHeight: 56,
        objectFit: "contain"
      }
    }) : React.createElement("span", {
      style: {
        fontFamily: "var(--font-display)",
        fontSize: it.name.length > 6 ? 21 : 26,
        fontWeight: 500,
        letterSpacing: "var(--tracking-cjk-display)",
        lineHeight: 1.25,
        color: i === items.length - 1 ? "var(--text-muted)" : "var(--ink-800)"
      }
    }, it.name));
  })), quote ? React.createElement("figure", {
    style: {
      margin: 0,
      marginTop: "auto",
      display: "flex",
      flexDirection: "column",
      gap: 18
    }
  }, React.createElement("blockquote", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: 30,
      lineHeight: "var(--leading-snug)",
      letterSpacing: "var(--tracking-cjk-body)",
      color: "var(--text-display)",
      maxWidth: "30em"
    }
  }, "\u201c", quote.text, "\u201d"), React.createElement("figcaption", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 14,
      fontSize: 20,
      color: "var(--text-muted)"
    }
  }, React.createElement("span", {
    style: {
      width: 24,
      height: 1,
      background: "var(--line-strong)"
    }
  }), (quote.role ? quote.role + " " : "") + quote.by)) : null));
}
Object.assign(__ds_scope, { PartnerWallSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/PartnerWallSlide.jsx", error: String((e && e.message) || e) }); }

// slides/PromiseSlide.jsx
try { (() => {
const DEFAULT_STATS = [{
  label: "数据窗口",
  value: "6",
  unit: "个月"
}, {
  label: "交付周期",
  value: "7",
  unit: "日内",
  accent: true
}, {
  label: "行动清单",
  value: "90",
  unit: "天"
}];
function PromiseSlide({
  eyebrow = "侍天的承诺",
  title = "交 6 个月经营数据，7 日内取得经营诊断",
  body = "增长在何处、利润失于何处、先改哪三件、由谁执行、以何验收。",
  stats = DEFAULT_STATS,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "parchment",
    grain: true,
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "grid",
      gridTemplateColumns: "minmax(0,1.1fr) minmax(0,.9fr)",
      gap: 72,
      alignItems: "center"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 32
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 26,
      lineHeight: "var(--leading-normal)",
      color: "var(--text-body)",
      maxWidth: "24em"
    }
  }, body)), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 0,
      borderTop: "1.5px solid var(--line-rule)"
    }
  }, stats.map(function (s, i) {
    return React.createElement("div", {
      key: i,
      style: {
        display: "flex",
        alignItems: "baseline",
        justifyContent: "space-between",
        gap: 24,
        padding: "26px 0",
        borderBottom: "1px solid var(--line-hairline)"
      }
    }, React.createElement("span", {
      style: {
        fontSize: 22,
        color: "var(--text-muted)",
        letterSpacing: "var(--tracking-cjk-body)"
      }
    }, s.label), React.createElement("span", {
      style: {
        display: "flex",
        alignItems: "baseline",
        gap: 8
      }
    }, React.createElement("span", {
      style: {
        fontFamily: "var(--font-numeral)",
        fontVariantNumeric: "tabular-nums",
        fontSize: 62,
        fontWeight: 500,
        lineHeight: 1,
        color: s.accent ? "var(--bronze-500)" : "var(--ink-900)"
      }
    }, s.value), React.createElement("span", {
      style: {
        fontSize: 22,
        color: "var(--text-muted)"
      }
    }, s.unit)));
  }))));
}
Object.assign(__ds_scope, { PromiseSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/PromiseSlide.jsx", error: String((e && e.message) || e) }); }

// slides/ProofSlide.jsx
try { (() => {
const DEFAULT_ELEMENTS = [{
  k: "证据",
  v: "工作日午市点单集中在 38–58 元价格带，套餐渗透仅 34%。"
}, {
  k: "利润影响",
  v: "+18.2 万 / 季",
  tone: "growth"
}, {
  k: "执行动作",
  v: "重排午市套餐结构，主推高毛利招牌组合"
}, {
  k: "验收指标",
  v: "午市客单 +8%"
}];
function ProofSlide({
  eyebrow = "以样张为证",
  title = "一份报告，就是一份决策文件",
  conclusion = "午市套餐结构存在可加码空间",
  elements = DEFAULT_ELEMENTS,
  note = "可计算的，量化到位；不可计算的，如实说明。",
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "muted",
    eyebrow: eyebrow,
    page: page
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 40
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 18
    }
  }, React.createElement(__ds_scope.SlideTitle, {
    size: "md"
  }, title), React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 24,
      color: "var(--text-muted)"
    }
  }, "每条结论具备四要素：证据、利润影响、执行动作、验收指标。")), React.createElement("div", {
    style: {
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderTop: "2px solid var(--bronze-500)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-sm)",
      padding: 40,
      display: "flex",
      flexDirection: "column",
      gap: 30
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 20
    }
  }, React.createElement("span", {
    style: {
      width: 44,
      height: 44,
      flexShrink: 0,
      borderRadius: "50%",
      border: "1px solid var(--line-rule)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-display)",
      fontSize: 20,
      color: "var(--bronze-500)"
    }
  }, "一"), React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: 38,
      fontWeight: 500,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, conclusion)), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: 32,
      paddingTop: 26,
      borderTop: "1px solid var(--line-hairline)"
    }
  }, elements.map(function (e) {
    return React.createElement("div", {
      key: e.k,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 12
      }
    }, React.createElement("span", {
      style: {
        fontSize: "var(--text-sm)",
        letterSpacing: "var(--tracking-caps)",
        textTransform: "uppercase",
        color: "var(--text-accent)"
      }
    }, e.k), React.createElement("span", {
      style: {
        fontSize: e.tone ? 30 : 21,
        lineHeight: "var(--leading-snug)",
        fontFamily: e.tone ? "var(--font-numeral)" : "var(--font-body)",
        fontWeight: e.tone ? 500 : 400,
        color: e.tone === "growth" ? "var(--growth-500)" : e.tone === "loss" ? "var(--loss-500)" : "var(--ink-800)"
      }
    }, e.v));
  }))), React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: 24,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--ink-700)"
    }
  }, note)));
}
Object.assign(__ds_scope, { ProofSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/ProofSlide.jsx", error: String((e && e.message) || e) }); }

// slides/SectionSlide.jsx
try { (() => {
const CN = ["一", "二", "三", "四", "五", "六"];

/* Section divider. The oversized outline seal circle is the brand's one permitted
   large ornament — bleeding off the right edge at 4–8% bronze. */
function SectionSlide({
  index = 1,
  numeral,
  title,
  subtitle,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "muted",
    page: page
  }, React.createElement("div", {
    style: {
      position: "absolute",
      right: -170,
      top: -110,
      width: 620,
      height: 620,
      borderRadius: "50%",
      border: "1.5px solid rgba(118,85,31,.10)",
      pointerEvents: "none"
    }
  }), React.createElement("div", {
    style: {
      position: "absolute",
      right: -50,
      top: 60,
      width: 400,
      height: 400,
      borderRadius: "50%",
      border: "1px solid rgba(118,85,31,.07)",
      pointerEvents: "none"
    }
  }), React.createElement("div", {
    style: {
      position: "relative",
      flex: 1,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 34
    }
  }, React.createElement("span", {
    style: {
      width: 78,
      height: 78,
      borderRadius: "50%",
      border: "1.5px solid var(--bronze-500)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-display)",
      fontSize: 36,
      fontWeight: 500,
      color: "var(--bronze-500)"
    }
  }, numeral || CN[index - 1] || String(index)), React.createElement(__ds_scope.SlideTitle, {
    size: "lg"
  }, title), subtitle ? React.createElement("p", {
    style: {
      margin: 0,
      maxWidth: "26em",
      fontSize: 25,
      lineHeight: "var(--leading-normal)",
      color: "var(--text-body)"
    }
  }, subtitle) : null));
}
Object.assign(__ds_scope, { SectionSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/SectionSlide.jsx", error: String((e && e.message) || e) }); }

// slides/TitleSlide.jsx
try { (() => {
const GOLD = [["T", 0], ["I", 1], ["A", 0], ["N", 1], ["S", 1], ["I", 1], ["G", 1], ["H", 1], ["T", 1]];
function TitleSlide({
  title = "拍板之前，问侍天",
  subtitle = "交 6 个月经营数据，7 日内取得经营诊断",
  client,
  date,
  page
}) {
  return React.createElement(__ds_scope.SlideFrame, {
    ground: "ink",
    page: page,
    seal: false,
    footer: "Second Brain / Decision System / Restaurant Growth"
  }, React.createElement("div", {
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 40
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 22
    }
  }, React.createElement("img", {
    src: __ds_scope.dsAsset("logo-seal.png"),
    alt: "侍天",
    width: 76,
    height: 76,
    style: {
      display: "block",
      filter: "invert(1) sepia(.35) saturate(.6) brightness(1.15)",
      opacity: .94
    }
  }), React.createElement("span", {
    style: {
      fontFamily: "var(--font-quote)",
      fontSize: 26,
      textTransform: "uppercase",
      letterSpacing: "var(--tracking-caps-wide)",
      lineHeight: 1.1
    }
  }, GOLD.map(function (l, i) {
    return React.createElement("span", {
      key: i,
      style: {
        color: l[1] ? "var(--bronze-300)" : "var(--parchment-100)",
        fontWeight: l[1] ? 400 : 700
      }
    }, l[0]);
  }))), React.createElement(__ds_scope.SlideTitle, {
    size: "xl",
    ground: "ink"
  }, title), React.createElement("p", {
    style: {
      margin: 0,
      maxWidth: "26em",
      fontSize: 26,
      lineHeight: "var(--leading-normal)",
      color: "rgba(239,230,210,.72)"
    }
  }, subtitle), client || date ? React.createElement("div", {
    style: {
      display: "flex",
      gap: 44,
      marginTop: 12
    }
  }, client ? React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "rgba(239,230,210,.5)"
    }
  }, "提案对象"), React.createElement("span", {
    style: {
      fontSize: 24,
      fontFamily: "var(--font-display)",
      color: "var(--text-on-inverse)",
      letterSpacing: "var(--tracking-cjk-display)"
    }
  }, client)) : null, date ? React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "rgba(239,230,210,.5)"
    }
  }, "日期"), React.createElement("span", {
    style: {
      fontSize: 24,
      fontFamily: "var(--font-numeral)",
      color: "var(--text-on-inverse)"
    }
  }, date)) : null) : null));
}
Object.assign(__ds_scope, { TitleSlide });
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/TitleSlide.jsx", error: String((e && e.message) || e) }); }

// slides/deck-stage.js
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).
/* ═══ THIS PROJECT USES DESIGN COMPONENTS (.dc.html) ═══
 * Reference this stage from your <x-dc> template as an import — NEVER as a
 * raw <deck-stage> tag plus a <script src> (that hides the whole deck until
 * the stream finishes):
 *
 *   <x-import component-from-global-scope="deck-stage" from="./deck-stage.js"
 *             width="1920" height="1080" hint-size="100%,100%">
 *     <section data-label="Title" style="...">…</section>
 *     <section data-label="Agenda" style="...">…</section>
 *   </x-import>
 *
 * Slides are inline-styled <section> siblings; do not add a stylesheet or a
 * deck-stage:not(:defined) rule. The plain-HTML "Usage" block in the comment
 * below does NOT apply to .dc.html templates.
 */
/* BEGIN USAGE */
/**
 * <deck-stage> — reusable web component for HTML decks.
 *
 * Handles:
 *  (a) speaker notes — reads <script type="application/json" id="speaker-notes">
 *      and posts {slideIndexChanged: N} to the parent window on nav.
 *  (b) keyboard navigation — ←/→ and ↑/↓, PgUp/PgDn, Space, Home/End,
 *      number keys.
 *      On touch devices, tapping the left/right half of the stage goes
 *      prev/next — taps on links, buttons and other interactive slide
 *      content are left alone.
 *  (c) press R to reset to slide 0 (with a tasteful keyboard hint).
 *  (d) bottom-center overlay showing slide count + hints, fades out on
 *      idle; hovering or focusing its controls pins it visible until the
 *      pointer/focus leaves. While presenting it is pointer-summoned only:
 *      mouse movement (or hover/focus) shows it, slide changes never do.
 *  (e) auto-scaling — inner canvas is a fixed design size (default 1920×1080)
 *      scaled with `transform: scale()` to fit the viewport, letterboxed.
 *      Set the `noscale` attribute to render at authored size (1:1) — the
 *      PPTX exporter sets this so its DOM capture sees unscaled geometry.
 *  (f) print — `@media print` lays every slide out as its own page at the
 *      design size, so the browser's Print → Save as PDF produces a clean
 *      one-page-per-slide PDF with no extra setup.
 *  (g) thumbnail rail — resizable left-hand column of per-slide thumbnails
 *      (static clones). Click to navigate — the clicked slide becomes the
 *      selected (highlighted) slide; shift-click selects a range and
 *      cmd/ctrl-click toggles slides in and out of the selection
 *      (Escape collapses it back to the current slide); ↑/↓ with a
 *      thumbnail focused to step between slides; Delete/Backspace with a
 *      thumbnail focused to delete the selection (one confirm dialog,
 *      one undoable operation); drag to reorder (dragging collapses a
 *      multi-selection); right-click for
 *      Skip / Move up / Move down / Duplicate / Delete — over a
 *      multi-selection the menu offers "Delete N slides". Drag the rail's right edge to resize;
 *      width persists to
 *      localStorage. Skipped slides carry `data-deck-skip`, are dimmed in
 *      the rail, omitted from prev/next navigation, and hidden at print.
 *      They also carry no rail number and are excluded from the overlay's
 *      slide count: the remaining slides are numbered contiguously
 *      (Keynote-style), and a skipped CURRENT slide (reachable by rail
 *      click or deep link, never by prev/next) shows '–' as its position.
 *      The rail is suppressed in presenting mode, in the host's Preview
 *      mode (ViewerMode='none'), on `noscale`, on narrow viewports
 *      (≤640px), and via the `no-rail` attribute. Rail mutations dispatch
 *      a `dc-op` CustomEvent on the element (see docs/dc-ops.md) and do
 *      NOT touch the DOM: the host applies the op and re-renders;
 *      structural rail input is locked until the host posts
 *      {__dc_op_ack: true, applied}.
 *  (h) typographic defaults — a zero-specificity stylesheet injected into
 *      the document gives headings `text-wrap: balance` and body text
 *      (p, li, blockquote, figcaption) `text-wrap: pretty`, so slides
 *      avoid widowed/orphaned words by default. Any text-wrap declaration
 *      you author on those elements wins over these defaults.
 *
 * Slides are HIDDEN, not unmounted. Non-active slides stay in the DOM with
 * `visibility: hidden` + `opacity: 0`, so their state (videos, iframes,
 * form inputs, React trees) is preserved across navigation.
 *
 * Lifecycle event — the component dispatches a `slidechange` CustomEvent on
 * itself whenever the active slide changes (including the initial mount).
 * The event bubbles and composes out of shadow DOM, so you can listen on
 * the <deck-stage> element or on document:
 *
 *   document.querySelector('deck-stage').addEventListener('slidechange', (e) => {
 *     e.detail.index         // new 0-based index
 *     e.detail.previousIndex // previous index, or -1 on init
 *     e.detail.total         // total slide count
 *     e.detail.slide         // the new active slide element
 *     e.detail.previousSlide // the prior slide element, or null on init
 *     e.detail.reason        // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
 *   });
 *
 * Persistence: none at the deck level. The host app keeps the current slide
 * in its own URL (?slide=) and re-delivers it via location.hash on load, so a
 * bare load with no hash always starts at slide 1.
 *
 * Usage:
 *   <style>deck-stage:not(:defined){visibility:hidden}</style>
 *   <deck-stage width="1920" height="1080">
 *     <section data-label="Title">...</section>
 *     <section data-label="Agenda">...</section>
 *   </deck-stage>
 *   <script src="deck-stage.js"></script>
 *
 * The :not(:defined) rule prevents a flash of the first slide at its
 * authored styles before this script runs and attaches the shadow root.
 *
 * Slides are the direct element children of <deck-stage>. Each slide is
 * automatically tagged with:
 *   - data-screen-label="NN Label"   (1-indexed, for comment flow)
 *   - data-om-validate="no_overflowing_text,no_overlapping_text,slide_sized_text"
 *
 * Speaker notes stay in sync because the component posts {slideIndexChanged: N}
 * to the parent — just include the #speaker-notes script tag if asked for notes.
 *
 * Authoring guidance:
 *   - Write slide bodies as static HTML inside <deck-stage>, with sizing via
 *     CSS custom properties in a <style> block rather than JS constants.
 *     Static slide markup is what lets the user click a heading in edit mode
 *     and retype it directly; a slide rendered through <script type="text/babel">,
 *     React, or a loop over a JS array has to round-trip every tweak through a
 *     chat message instead. Reach for script-generated slides only when the
 *     content genuinely needs interactive behaviour static HTML can't express.
 *   - Do NOT set position/inset/width/height on the slide <section> elements —
 *     the component absolutely positions every slotted child for you.
 *   - Entrance animations: make the visible end-state the base style and
 *     animate *from* hidden, so print and reduced-motion show content.
 *     Gate the animation on [data-deck-active] and the motion query, e.g.
 *     `@media (prefers-reduced-motion:no-preference){ [data-deck-active] .x{animation:fade-in .5s both} }`.
 *     Avoid infinite decorative loops on slide content.
 */
/* END USAGE */

(() => {
  const DESIGN_W_DEFAULT = 1920;
  const DESIGN_H_DEFAULT = 1080;
  const OVERLAY_HIDE_MS = 1800;
  const VALIDATE_ATTR = 'no_overflowing_text,no_overlapping_text,slide_sized_text';
  const FINE_POINTER_MQ = matchMedia('(hover: hover) and (pointer: fine)');
  const NARROW_MQ = matchMedia('(max-width: 640px)');
  // Slide-authored controls that should keep a tap instead of it navigating.
  const INTERACTIVE_SEL = 'a[href], button, input, select, textarea, summary, label, video[controls], audio[controls], [role="button"], [onclick], [tabindex]:not([tabindex^="-"]), [contenteditable]:not([contenteditable="false" i])';
  const pad2 = n => String(n).padStart(2, '0');

  // Label precedence: data-label → data-screen-label (number stripped) → first heading → "Slide".
  const getSlideLabel = el => {
    const explicit = el.getAttribute('data-label');
    if (explicit) return explicit;
    const existing = el.getAttribute('data-screen-label');
    if (existing) return existing.replace(/^\s*\d+\s*/, '').trim() || existing;
    const h = el.querySelector('h1, h2, h3, [data-title]');
    const t = h && (h.textContent || '').trim().slice(0, 40);
    if (t) return t;
    return 'Slide';
  };
  const stylesheet = `
    :host {
      position: fixed;
      inset: 0;
      display: block;
      background: #000;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
      overflow: hidden;
      -webkit-tap-highlight-color: transparent;
    }
    /* connectedCallback holds this until document.fonts.ready (capped 2s) so
     * the first visible paint has the deck's real typography + final rail
     * layout. opacity (not visibility) so the active slide can't un-hide
     * itself via the ::slotted([data-deck-active]) visibility:visible rule.
     * Only the stage/rail hide — the black :host background stays, so the
     * iframe doesn't flash the page's default white. */
    :host([data-fonts-pending]) .stage,
    :host([data-fonts-pending]) .rail { opacity: 0; pointer-events: none; }

    .stage {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .canvas {
      position: relative;
      transform-origin: center center;
      flex-shrink: 0;
      background: #fff;
      will-change: transform;
      /* Slide edge on the black stage. Dark decks override the canvas
       * fill toward the stage's own black, leaving nothing to mark where
       * the slide ends — the faint white ring keeps the boundary legible
       * there while disappearing into the white of light decks. A
       * box-shadow, not outline/border: it follows any canvas rounding
       * and adds no layout size. */
      box-shadow: 0 0 0 1.5px rgba(255, 255, 255, 0.12);
    }

    /* Slides live in light DOM (via <slot>) so authored CSS still applies.
       We absolutely position each slotted child to stack them. */
    ::slotted(*) {
      position: absolute !important;
      inset: 0 !important;
      width: 100% !important;
      height: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
    }
    ::slotted([data-deck-active]) {
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .overlay {
      position: fixed;
      left: 50%;
      bottom: 22px;
      transform: translate(-50%, 6px) scale(0.92);
      filter: blur(6px);
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px;
      background: #000;
      color: #fff;
      border-radius: 999px;
      font-size: 12px;
      font-feature-settings: "tnum" 1;
      letter-spacing: 0.01em;
      opacity: 0;
      pointer-events: none;
      transition: opacity 260ms ease, transform 260ms cubic-bezier(.2,.8,.2,1), filter 260ms ease;
      transform-origin: center bottom;
      z-index: 2147483000;
      user-select: none;
    }
    .overlay[data-visible] {
      opacity: 1;
      pointer-events: auto;
      transform: translate(-50%, 0) scale(1);
      filter: blur(0);
    }

    .btn {
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      border: 0;
      margin: 0;
      padding: 0;
      color: inherit;
      font: inherit;
      cursor: default;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      min-width: 28px;
      border-radius: 999px;
      color: rgba(255,255,255,0.72);
      transition: background 140ms ease, color 140ms ease;
      -webkit-tap-highlight-color: transparent;
    }
    .btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
    .btn:active { background: rgba(255,255,255,0.18); }
    .btn:focus { outline: none; }
    .btn:focus-visible { outline: none; }
    .btn::-moz-focus-inner { border: 0; }
    .btn svg { width: 14px; height: 14px; display: block; }
    .btn.reset {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0 10px 0 12px;
      gap: 6px;
      color: rgba(255,255,255,0.72);
    }
    .btn.reset .kbd {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      font-size: 10px;
      line-height: 1;
      color: rgba(255,255,255,0.88);
      background: rgba(255,255,255,0.12);
      border-radius: 4px;
    }

    .count {
      font-variant-numeric: tabular-nums;
      color: #fff;
      font-weight: 500;
      padding: 0 8px;
      min-width: 42px;
      text-align: center;
      font-size: 12px;
    }
    .count .sep { color: rgba(255,255,255,0.45); margin: 0 3px; font-weight: 400; }
    .count .total { color: rgba(255,255,255,0.55); }

    .divider {
      width: 1px;
      height: 14px;
      background: rgba(255,255,255,0.18);
      margin: 0 2px;
    }

    /* ── Thumbnail rail ──────────────────────────────────────────────────
       Fixed column on the left; each thumbnail is a static deep-clone of
       the light-DOM slide scaled into a 16:9 (or design-aspect) frame. The
       stage re-fits around it (see _fit); hidden during present / noscale
       / print so capture geometry and fullscreen output are unchanged. */
    .rail {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--deck-rail-w, 188px);
      background: #141414;
      border-right: 1px solid rgba(255,255,255,0.08);
      overflow-y: auto;
      overflow-x: hidden;
      padding: 12px 10px;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 2147482500;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.18) transparent;
    }
    .rail::-webkit-scrollbar { width: 8px; }
    .rail::-webkit-scrollbar-track { background: transparent; margin: 2px; }
    .rail::-webkit-scrollbar-thumb {
      background: rgba(255,255,255,0.18);
      border-radius: 4px;
      border: 2px solid transparent;
      background-clip: content-box;
    }
    .rail::-webkit-scrollbar-thumb:hover {
      background: rgba(255,255,255,0.28);
      border: 2px solid transparent;
      background-clip: content-box;
    }
    :host([no-rail]) .rail,
    :host([noscale]) .rail { display: none; }
    .rail[data-presenting] { display: none; }
    @media (max-width: 640px) {
      .rail, .rail-resize { display: none; }
    }
    /* User-driven show/hide (the TweaksPanel toggle) slides instead of
       popping. Transitions are gated on :host([data-rail-anim]) — set only
       for the 200ms around the toggle — so window-resize and rail-width
       drag (which also call _fit) don't lag behind the cursor. */
    .rail[data-user-hidden] { transform: translateX(-100%); }
    :host([data-rail-anim]) .rail { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .stage { transition: left 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .canvas { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    /* transition shorthand replaces rather than merges — repeat the base
       .overlay opacity/transform/filter transitions so visibility changes
       during the 200ms toggle window still fade instead of popping. */
    :host([data-rail-anim]) .overlay {
      transition: margin-left 200ms cubic-bezier(.3,.7,.4,1),
                  opacity 260ms ease,
                  transform 260ms cubic-bezier(.2,.8,.2,1),
                  filter 260ms ease;
    }

    .thumb {
      position: relative;
      display: flex;
      align-items: flex-start;
      gap: 8px;
      cursor: pointer;
      user-select: none;
    }
    .thumb .num {
      width: 16px;
      flex-shrink: 0;
      font-size: 11px;
      font-weight: 500;
      text-align: right;
      color: rgba(255,255,255,0.55);
      padding-top: 2px;
      font-variant-numeric: tabular-nums;
    }
    .thumb .frame {
      position: relative;
      flex: 1;
      min-width: 0;
      aspect-ratio: var(--deck-aspect);
      background: #fff;
      border-radius: 4px;
      outline: 2px solid transparent;
      outline-offset: 0;
      overflow: hidden;
      transition: outline-color 120ms ease;
    }
    .thumb:hover .frame { outline-color: rgba(255,255,255,0.25); }
    .thumb { outline: none; }
    .thumb:focus-visible .frame { outline-color: rgba(255,255,255,0.5); }
    .thumb[data-selected] .num { color: #fff; }
    .thumb[data-selected] .frame {
      outline-color: rgba(217,119,87,0.65);
      box-shadow: 0 0 0 4px rgba(217,119,87,0.18);
    }
    .thumb[data-current] .num { color: #fff; }
    .thumb[data-current] .frame {
      outline-color: #D97757;
      box-shadow: 0 0 0 4px rgba(217,119,87,0.25);
    }
    /* While dragging, the thumb itself is the drag visual (the native drag
       image is suppressed in dragstart so the snapshot can't wander off the
       rail horizontally): elevate it rather than dim it, and let hit-testing
       ignore it so dragover reaches the sibling thumb under the pointer
       instead of the moving element itself. */
    .thumb[data-dragging] { opacity: 0.9; z-index: 30; pointer-events: none; }
    .thumb[data-dragging] .frame {
      outline-color: rgba(255,255,255,0.5);
      box-shadow: 0 6px 24px rgba(0,0,0,0.5);
    }
    .thumb::before {
      content: '';
      position: absolute;
      left: 24px;
      right: 0;
      height: 3px;
      border-radius: 2px;
      background: #D97757;
      opacity: 0;
      pointer-events: none;
    }
    .thumb[data-drop="before"]::before { top: -8px; opacity: 1; }
    .thumb[data-drop="after"]::before { bottom: -8px; opacity: 1; }
    .thumb[data-skip] .frame { opacity: 0.35; }
    .thumb[data-skip] .frame::after {
      content: 'Skipped';
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.45);
      color: #fff;
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.04em;
    }

    .ctxmenu {
      position: fixed;
      min-width: 150px;
      padding: 4px;
      background: #242424;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 7px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.45);
      z-index: 2147483100;
      display: none;
      font-size: 12px;
    }
    .ctxmenu[data-open] { display: block; }
    .ctxmenu button {
      display: block;
      width: 100%;
      appearance: none;
      border: 0;
      background: transparent;
      color: #e8e8e8;
      font: inherit;
      text-align: left;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
    .ctxmenu button:hover:not(:disabled) { background: rgba(255,255,255,0.08); }
    .ctxmenu button:disabled { opacity: 0.35; cursor: default; }
    .ctxmenu hr {
      border: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin: 4px 2px;
    }

    .rail-resize {
      position: fixed;
      left: calc(var(--deck-rail-w, 188px) - 3px);
      top: 0;
      bottom: 0;
      width: 6px;
      cursor: col-resize;
      z-index: 2147482600;
      touch-action: none;
    }
    .rail-resize:hover,
    .rail-resize[data-dragging] { background: rgba(255,255,255,0.12); }
    :host([no-rail]) .rail-resize,
    :host([noscale]) .rail-resize,
    .rail[data-presenting] + .rail-resize,
    .rail[data-user-hidden] + .rail-resize { display: none; }

    /* Delete-confirm popup — matches the SPA's ConfirmDialog layout
       (title + message body, depressed footer with Cancel / Delete). */
    .confirm-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 2147483200;
      display: none;
      align-items: center;
      justify-content: center;
    }
    .confirm-backdrop[data-open] { display: flex; }
    .confirm {
      width: 320px;
      max-width: calc(100vw - 32px);
      background: #2a2a2a;
      color: #e8e8e8;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 12px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: inherit;
      animation: deck-confirm-in 0.18s ease;
    }
    @keyframes deck-confirm-in {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    .confirm .body { padding: 20px 20px 16px; }
    .confirm .title { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .confirm .msg { font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.65); }
    .confirm .footer {
      padding: 14px 20px;
      background: #1f1f1f;
      border-top: 1px solid rgba(255,255,255,0.08);
      display: flex;
      justify-content: flex-end;
      gap: 8px;
    }
    .confirm button {
      appearance: none;
      font: inherit;
      font-size: 13px;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
    }
    .confirm .cancel {
      background: transparent;
      border: 0;
      color: rgba(255,255,255,0.8);
    }
    .confirm .cancel:hover { background: rgba(255,255,255,0.08); }
    .confirm .danger {
      background: #c96442;
      border: 1px solid rgba(0,0,0,0.15);
      color: #fff;
      box-shadow: 0 1px 3px rgba(166,50,68,0.3), 0 2px 6px rgba(166,50,68,0.18);
    }
    .confirm .danger:hover { background: #b5563a; }

    /* ── Print: one page per slide, no chrome ────────────────────────────
       The screen layout stacks every slide at inset:0 inside a scaled
       canvas; for print we want them in document flow at the authored
       design size so the browser paginates one slide per sheet. The
       @page size is set from the width/height attributes via the inline
       <style id="deck-stage-print-page"> that _syncPrintPageRule appends
       to the document (the @page at-rule has no effect inside shadow DOM). */
    @media print {
      :host {
        position: static;
        inset: auto;
        background: none;
        overflow: visible;
        color: inherit;
      }
      .stage { position: static; display: block; }
      .canvas {
        transform: none !important;
        width: auto !important;
        height: auto !important;
        background: none;
        will-change: auto;
      }
      ::slotted(*) {
        position: relative !important;
        inset: auto !important;
        width: var(--deck-design-w) !important;
        height: var(--deck-design-h) !important;
        box-sizing: border-box !important;
        /* Size containment: slotted content that overflows the design box
         * (an image-slot's aspect-ratio-derived width, say) must not count
         * toward Chromium's print document width — without this, an
         * abs-positioned child past the page edge shrinks the whole PDF
         * to fit (~75%). Containment is safe here because the definite
         * width/height above size the slide regardless of content.
         * (Absorbed from PR #2619 with its owner's agreement.) */
        contain: size !important;
        opacity: 1 !important;
        visibility: visible !important;
        pointer-events: auto;
        break-after: page;
        page-break-after: always;
        break-inside: avoid;
        overflow: hidden;
      }
      /* :last-child alone isn't enough once data-deck-skip hides the
         trailing slide(s) — the last *visible* slide still carries
         break-after:page and prints a blank sheet. _markLastVisible()
         maintains data-deck-last-visible on the last non-skipped slide. */
      ::slotted(*:last-child),
      ::slotted([data-deck-last-visible]) {
        break-after: auto;
        page-break-after: auto;
      }
      ::slotted([data-deck-skip]) { display: none !important; }
      .overlay, .rail, .rail-resize, .ctxmenu, .confirm-backdrop { display: none !important; }
    }
  `;
  class DeckStage extends HTMLElement {
    static get observedAttributes() {
      return ['width', 'height', 'noscale', 'no-rail'];
    }
    constructor() {
      super();
      this._root = this.attachShadow({
        mode: 'open'
      });
      this._index = 0;
      this._slides = [];
      // Explicit multi-selection (slide elements). Empty means the
      // selection is implicitly the current slide, so Delete always has
      // a well-defined target while the rail has focus.
      this._selected = new Set();
      this._selAnchor = null;
      this._notes = [];
      this._hideTimer = null;
      this._mouseIdleTimer = null;
      this._menuIndex = -1;
      // Overlay pinning: while the pointer is over the controls toolbar or
      // a control has keyboard focus, the idle-hide timeout must not
      // dismiss it (a pointer parked ON the controls doesn't generate
      // mousemove, so without the pin the toolbar vanishes under the
      // user's cursor after OVERLAY_HIDE_MS). Read by _flashOverlay's
      // hide timeout; cleared by mouseleave/focusout, which resume the
      // normal idle fade.
      this._overlayHover = false;
      this._overlayFocus = false;
      // Capability marker for the host's injected guest bundle. Copies
      // WITHOUT _navArrowsUpDown are frozen per-project builds that
      // predate native ArrowUp/ArrowDown slide nav — the bundle translates
      // Up/Down to Right/Left for those (installDeckArrowKeyTranslator in
      // apps/web/src/guest/edit-mode.ts) and must stand down here or every
      // press would advance twice. A marker, not a version number, so a
      // future capability can add its own independent probe.
      this._navArrowsUpDown = true;
      // Same contract for rail Delete/Backspace: copies WITHOUT
      // _railDeleteKey predate the thumbs' own Delete/Backspace binding,
      // and the bundle opens the delete confirm for them
      // (installDeckRailDeleteFallback in apps/web/src/guest/edit-mode.ts).
      // Current builds consume the key at the thumb (stopPropagation), so
      // the marker is belt-and-braces — it keeps the fallback standing
      // down even if a future build lets the key bubble past the thumb.
      this._railDeleteKey = true;
      // Same contract for skip-aware numbering: copies WITHOUT
      // _railSkipNumbers number every thumb 1..N and count skipped slides
      // in the overlay total — the bundle rewrites both for those
      // (installDeckSkipNumberingFallback in apps/web/src/guest/edit-mode.ts).
      // Here the component renumbers natively, so the fallback stands down.
      this._railSkipNumbers = true;
      this._onKey = this._onKey.bind(this);
      this._onResize = this._onResize.bind(this);
      this._onSlotChange = this._onSlotChange.bind(this);
      this._onMouseMove = this._onMouseMove.bind(this);
      this._onTap = this._onTap.bind(this);
      this._onMessage = this._onMessage.bind(this);
      // Capture-phase close so a click anywhere dismisses the menu, but
      // ignore clicks that land inside the menu itself — otherwise the
      // capture handler runs before the menu's own (bubble) handler and
      // clears _menuIndex out from under it.
      this._onDocClick = e => {
        if (this._menu && e.composedPath && e.composedPath().includes(this._menu)) return;
        this._closeMenu();
      };
    }
    get designWidth() {
      return parseInt(this.getAttribute('width'), 10) || DESIGN_W_DEFAULT;
    }
    get designHeight() {
      return parseInt(this.getAttribute('height'), 10) || DESIGN_H_DEFAULT;
    }
    connectedCallback() {
      // Presenter-view popup loads deckUrl?_snthumb=...#N for its prev/cur/
      // next thumbnails — the rail has no business rendering inside those
      // (wrong scale, and it offsets the stage so the thumb shows a gutter).
      if (/[?&]_snthumb=/.test(location.search)) this.setAttribute('no-rail', '');
      this._render();
      this._loadNotes();
      this._syncPrintPageRule();
      this._ensurePrintSizingMeta();
      this._ensureTextWrapDefaults();
      window.addEventListener('keydown', this._onKey);
      window.addEventListener('resize', this._onResize);
      window.addEventListener('mousemove', this._onMouseMove, {
        passive: true
      });
      window.addEventListener('message', this._onMessage);
      window.addEventListener('click', this._onDocClick, true);
      this.addEventListener('click', this._onTap);
      // Print lays every slide out as its own page, so [data-deck-active]-
      // gated entrance styles need the attribute on every slide (not just
      // the current one) or their content prints at the hidden base style.
      // The transient freeze style lands BEFORE the attributes so any
      // attribute-keyed transition fires at 0s (changing transition-
      // duration after a transition has started doesn't affect it).
      this._onBeforePrint = () => {
        this._syncPrintPageRule();
        // Self-heal: a departed doc-page may have removed the page-global
        // print-sizing meta this deck deferred to at connect time.
        this._ensurePrintSizingMeta();
        if (this._freezeStyle) this._freezeStyle.remove();
        this._freezeStyle = document.createElement('style');
        this._freezeStyle.textContent = '*,*::before,*::after{transition-duration:0s !important}';
        document.head.appendChild(this._freezeStyle);
        this._slides.forEach(s => s.setAttribute('data-deck-active', ''));
      };
      this._onAfterPrint = () => {
        this._applyIndex({
          showOverlay: false,
          broadcast: false
        });
        if (this._freezeStyle) {
          this._freezeStyle.remove();
          this._freezeStyle = null;
        }
      };
      window.addEventListener('beforeprint', this._onBeforePrint);
      window.addEventListener('afterprint', this._onAfterPrint);
      // Initial collection + layout happens via slotchange, which fires on mount.
      this._enableRail();
      // Hold the stage hidden until webfonts are ready so the first visible
      // paint has the deck's real typography — the :not(:defined) guard in
      // the page HTML only covers custom-element upgrade, not font load.
      // Capped so a 404'd font URL can't blank the deck indefinitely.
      this.setAttribute('data-fonts-pending', '');
      const reveal = () => this.removeAttribute('data-fonts-pending');
      // Unconditional cap — rAF can be suspended in a hidden iframe, which
      // would strand the one inside the rAF callback.
      setTimeout(reveal, 2000);
      // rAF first: fonts.ready is a pre-resolved promise until layout has
      // resolved the slotted text's font-family and pushed a FontFace into
      // 'loading'. Reading it here in connectedCallback (parse-time) would
      // settle the race in a microtask before any font fetch starts.
      requestAnimationFrame(() => {
        Promise.race([document.fonts ? document.fonts.ready : Promise.resolve(), new Promise(r => setTimeout(r, 2000))]).then(reveal, reveal);
      });
    }
    _enableRail() {
      // Idempotent — older host builds still post __omelette_rail_enabled.
      // no-rail guard keeps the observers/stylesheet walk off the cheap path
      // for presenter-popup thumbnail iframes (three per view — cur/prev/next).
      if (this._railEnabled || this.hasAttribute('no-rail')) return;
      this._railEnabled = true;
      // Per-viewer preference — restored alongside rail width. Default on;
      // only a stored '0' (from the TweaksPanel toggle) hides it.
      this._railVisible = true;
      try {
        if (localStorage.getItem('deck-stage.railVisible') === '0') this._railVisible = false;
      } catch (e) {}
      // Live thumbnail updates: watch the light-DOM slides for content
      // edits and re-clone just the affected thumb(s), debounced. Ignore
      // the data-deck-* / data-screen-label / data-om-validate attributes
      // this component itself writes so nav doesn't trigger spurious
      // refreshes — except data-deck-skip, which now arrives from the host
      // re-render and is what updates the rail badge, print bookkeeping,
      // and deckSkipped re-broadcast. Also ignore data-dc-tpl /
      // data-om-slide-id — host-reserved bookkeeping stamps (the host's
      // ATTR_RESERVED guard bounds them the same way) that structural
      // edits renumber/re-mint on slides whose content didn't change;
      // re-cloning on that churn is what made a slide move flash its
      // thumbnails.
      const OWN_ATTRS = /^data-(deck-(?!skip$)|screen-label$|om-(validate|slide-id)$|dc-tpl$)/;
      this._liveDirty = new Set();
      this._liveObserver = new MutationObserver(records => {
        for (const r of records) {
          if (r.type === 'attributes' && OWN_ATTRS.test(r.attributeName || '')) continue;
          let n = r.target;
          while (n && n.parentElement !== this) n = n.parentElement;
          // Skip/unskip is handled below without re-cloning (the badge sits
          // on the thumb wrapper, not the clone) — don't mark the slide
          // dirty for an attr change whose only visible effect is the badge.
          if (n && this._slideSet && this._slideSet.has(n) && !(r.type === 'attributes' && r.attributeName === 'data-deck-skip')) {
            this._liveDirty.add(n);
          }
          // Host-driven skip toggle: sync the rail badge + print + presenter
          // skipped-list the way _toggleSkip used to do locally.
          if (r.type === 'attributes' && r.attributeName === 'data-deck-skip' && n && this._slideSet && this._slideSet.has(n)) {
            const i = this._slides.indexOf(n);
            if (this._thumbs && this._thumbs[i]) {
              if (n.hasAttribute('data-deck-skip')) this._thumbs[i].thumb.setAttribute('data-skip', '');else this._thumbs[i].thumb.removeAttribute('data-skip');
            }
            this._markLastVisible();
            this._renumberRail();
            this._syncCount();
            try {
              window.postMessage({
                slideIndexChanged: this._index,
                deckTotal: this._slides.length,
                deckSkipped: this._skippedIndices()
              }, '*');
            } catch (e) {}
          }
        }
        if (this._liveDirty.size && !this._liveTimer) {
          this._liveTimer = setTimeout(() => {
            this._liveTimer = null;
            this._liveDirty.forEach(s => this._refreshThumb(s));
            this._liveDirty.clear();
          }, 200);
        }
      });
      this._liveObserver.observe(this, {
        subtree: true,
        childList: true,
        characterData: true,
        attributes: true
      });
      // Lazy thumbnail materialization — clone the slide only when its
      // frame scrolls into (or near) the rail viewport. rootMargin gives
      // ~4 thumbs of pre-load so fast scrolling doesn't flash blanks.
      this._railObserver = new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting && e.target.__deckThumb) {
            this._materialize(e.target.__deckThumb);
          }
        });
      }, {
        root: this._rail,
        rootMargin: '400px 0px'
      });
      // Tweaks typically change CSS vars / attrs OUTSIDE <deck-stage>
      // (on <html>, <body>, a wrapper div, or a <style> tag), which
      // _liveObserver can't see. Re-snapshot author CSS (constructable
      // sheet is shared by reference, so one replaceSync updates every
      // thumb shadow root) and re-sync each thumb host's attrs + custom
      // properties. In-slide DOM mutations are _liveObserver's job.
      // Debounced so slider drags don't thrash.
      this._onTweakChange = () => {
        clearTimeout(this._tweakTimer);
        this._tweakTimer = setTimeout(() => {
          this._snapshotAuthorCss();
          // One getComputedStyle for the whole batch — each
          // getPropertyValue read below reuses the same computed style
          // as long as nothing invalidates layout between thumbs.
          const cs = getComputedStyle(this);
          (this._thumbs || []).forEach(t => {
            if (t.host) this._syncThumbHostAttrs(t.host, cs);
          });
        }, 120);
      };
      window.addEventListener('tweakchange', this._onTweakChange);
      // Stylesheets that finish loading AFTER the snapshot below never
      // reach the thumbs on their own: a still-pending <link> contributes
      // nothing to document.styleSheets, and nothing re-reads it on load,
      // so the live slides restyle while every clone keeps the stale
      // sheet. dc-runtime's helmet mounts design-system <link>s at render
      // time, so a deck-stage that connects first snapshots before that
      // CSS exists. Funnel late arrivals into the same debounced resync:
      // hook load/error on every current <link>, and watch <head> for
      // links and styles mounted or rewritten later. Deliberately not
      // rAF- or fonts.ready-driven — rAF is throttled/suspended in hidden
      // iframes (thumbnail/presenter contexts), and a font-file load
      // doesn't change cssRules, so it needs no resync.
      this._hookedLinks = [];
      this._hookSheetLoad = el => {
        if (!el.matches || !el.matches('link[rel~="stylesheet" i]')) return;
        if (this._hookedLinks.indexOf(el) !== -1) return;
        this._hookedLinks.push(el);
        el.addEventListener('load', this._onTweakChange);
        el.addEventListener('error', this._onTweakChange);
      };
      document.querySelectorAll('link[rel~="stylesheet" i]').forEach(this._hookSheetLoad);
      this._headObserver = new MutationObserver(records => {
        let resync = false;
        for (const r of records) {
          if (r.type === 'characterData') {
            // Only <style> text is CSS — a ticking <title> shouldn't
            // wake the resync forever.
            const p = r.target.parentNode;
            if (p && p.nodeName === 'STYLE') resync = true;
            continue;
          }
          if (r.type === 'attributes') {
            // A late rel/href rewrite turns an inert <link> into a
            // stylesheet (hook it; its load fires even on cache hits);
            // a media/disabled flip changes effective rules with no
            // event. Resync only if this link is or ever was a
            // stylesheet — favicon/preload/canonical href churn isn't
            // a resync.
            if (r.target.nodeName === 'LINK') {
              this._hookSheetLoad(r.target);
              if (this._hookedLinks.indexOf(r.target) !== -1) resync = true;
            } else if (r.target.nodeName === 'STYLE') resync = true;
            continue;
          }
          // childList: only links and styles carry CSS. A new <link> has
          // no rules until it loads — hook it rather than resync now; a
          // <style> mount/unmount or text-node swap takes effect
          // immediately. _freezeStyle (our beforeprint helper) is skipped
          // on add only — no removal-side guard: _onAfterPrint nulls the
          // ref before the observer fires, so that check would be dead;
          // the one debounced no-op resync per print is harmless.
          if (r.target.nodeName === 'STYLE') resync = true;
          for (const n of r.addedNodes) {
            if (n.nodeName === 'LINK') this._hookSheetLoad(n);else if (n.nodeName === 'STYLE' && n !== this._freezeStyle) resync = true;
          }
          for (const n of r.removedNodes) {
            if (n.nodeName === 'LINK') {
              const hi = this._hookedLinks.indexOf(n);
              if (hi !== -1) {
                this._hookedLinks.splice(hi, 1);
                n.removeEventListener('load', this._onTweakChange);
                n.removeEventListener('error', this._onTweakChange);
                resync = true;
              }
            } else if (n.nodeName === 'STYLE') resync = true;
          }
        }
        if (resync) this._onTweakChange();
      });
      this._headObserver.observe(document.head, {
        childList: true,
        subtree: true,
        characterData: true,
        attributes: true,
        attributeFilter: ['rel', 'href', 'media', 'disabled']
      });
      this._snapshotAuthorCss();
      // Re-snapshot once any still-loading stylesheet settles — it throws on
      // .cssRules above and silently contributes '' → unstyled thumbs on a
      // cold mount. {once:true}; routed through the debounced handler.
      document.querySelectorAll('link[rel~="stylesheet"]').forEach(l => {
        try {
          if (l.sheet && l.sheet.cssRules) return;
        } catch (e) {}
        l.addEventListener('load', this._onTweakChange, {
          once: true
        });
        l.addEventListener('error', this._onTweakChange, {
          once: true
        });
      });
      if (document.fonts) document.fonts.ready.then(this._onTweakChange, this._onTweakChange);
      // Build the rail now that it's enabled — slotchange already fired,
      // so _renderRail's early-return skipped the initial build.
      this._syncRailHidden();
      this._renderRail();
      this._fit();
    }

    /** Snapshot document stylesheets into a constructable sheet that each
     *  thumbnail's nested shadow root adopts — so author CSS styles the
     *  cloned slide content without touching this component's chrome.
     *  Cross-origin sheets throw on .cssRules — skip them. Re-callable:
     *  the existing constructable sheet is reused via replaceSync so every
     *  already-adopted shadow root picks up the fresh CSS without re-adopt. */
    _snapshotAuthorCss() {
      // :root in an adopted sheet inside a shadow root matches nothing
      // (only the document root qualifies), so author rules like
      // `:root[data-voice="modern"] .serif` never reach the clones.
      // Rewrite :root → :host and mirror <html>'s data-*/class/lang onto
      // each thumb host (see _syncThumbHostAttrs) so the same selectors
      // match inside the thumbnail's shadow tree.
      const authorCss = Array.from(document.styleSheets).map(sh => {
        try {
          return Array.from(sh.cssRules).map(r => r.cssText).join('\n');
        } catch (e) {
          return '';
        }
      }).join('\n')
      // The shadow host is featureless outside the functional :host(...)
      // form, so any compound on :root — [attr], .class, #id, :pseudo —
      // must become :host(<compound>) not :host<compound>. Same for the
      // html type selector (Tailwind class-strategy dark mode emits
      // html.dark; Pico uses html[data-theme]), which has nothing to
      // match inside the thumb's shadow tree.
      .replace(/:root((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)/g, ':host($1)').replace(/:root\b/g, ':host').replace(/(^|[\s,>~+(}])html((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)(?![-\w])/g, '$1:host($2)').replace(/(^|[\s,>~+(}])html(?![-\w])/g, '$1:host');
      // Every custom property the author references. _syncThumbHostAttrs
      // mirrors each one's *computed* value at <deck-stage> onto the
      // thumb host so the live value wins over the :host default above
      // regardless of which ancestor the tweak wrote to (<html>, <body>,
      // a wrapper div, or the deck-stage element itself all inherit
      // down to getComputedStyle(this)).
      this._authorVars = new Set(authorCss.match(/--[\w-]+/g) || []);
      try {
        if (!this._adoptedSheet) this._adoptedSheet = new CSSStyleSheet();
        this._adoptedSheet.replaceSync(authorCss);
      } catch (e) {
        this._adoptedSheet = null;
        this._authorCss = authorCss;
      }
    }
    _syncThumbHostAttrs(host, cs) {
      const de = document.documentElement;
      // setAttribute overwrites but can't delete — an attr removed from
      // <html> (toggleAttribute off, classList emptied) would linger on
      // the host and :host([data-*]) / :host(.foo) rules would keep
      // matching. Remove stale mirrored attrs first; iterate backward
      // because removeAttribute mutates the live NamedNodeMap.
      for (let i = host.attributes.length - 1; i >= 0; i--) {
        const n = host.attributes[i].name;
        if ((n.startsWith('data-') || n === 'class' || n === 'lang') && !de.hasAttribute(n)) {
          host.removeAttribute(n);
        }
      }
      for (const a of de.attributes) {
        if (a.name.startsWith('data-') || a.name === 'class' || a.name === 'lang') {
          host.setAttribute(a.name, a.value);
        }
      }
      // The :root→:host rewrite in _snapshotAuthorCss pins each custom
      // property to its stylesheet default on the thumb host, shadowing
      // the live value that would otherwise inherit. Tweaks can write the
      // live value on any ancestor — <html>, <body>, a wrapper div, the
      // deck-stage element — so read it as the *computed* value at
      // <deck-stage> (which sees the whole inheritance chain) rather than
      // trying to guess which element the author wrote to. Inline on the
      // host beats the :host{} rule. remove-stale covers vars dropped
      // from the stylesheet between snapshots.
      const vars = this._authorVars || new Set();
      for (let i = host.style.length - 1; i >= 0; i--) {
        const p = host.style[i];
        if (p.startsWith('--') && !vars.has(p)) host.style.removeProperty(p);
      }
      const live = cs || getComputedStyle(this);
      vars.forEach(p => {
        const v = live.getPropertyValue(p);
        if (v) host.style.setProperty(p, v.trim());else host.style.removeProperty(p);
      });
    }
    disconnectedCallback() {
      // A disconnect mid-drag never gets a dragend, so the document-level
      // drag tracker must be torn down here like every other global hook.
      this._stopDragTrack();
      window.removeEventListener('keydown', this._onKey);
      window.removeEventListener('resize', this._onResize);
      window.removeEventListener('mousemove', this._onMouseMove);
      window.removeEventListener('message', this._onMessage);
      window.removeEventListener('click', this._onDocClick, true);
      window.removeEventListener('beforeprint', this._onBeforePrint);
      window.removeEventListener('afterprint', this._onAfterPrint);
      if (this._freezeStyle) {
        this._freezeStyle.remove();
        this._freezeStyle = null;
      }
      this.removeEventListener('click', this._onTap);
      if (this._hideTimer) clearTimeout(this._hideTimer);
      if (this._mouseIdleTimer) clearTimeout(this._mouseIdleTimer);
      if (this._liveTimer) clearTimeout(this._liveTimer);
      if (this._tweakTimer) clearTimeout(this._tweakTimer);
      if (this._railAnimTimer) clearTimeout(this._railAnimTimer);
      if (this._scaleRaf) cancelAnimationFrame(this._scaleRaf);
      if (this._liveObserver) this._liveObserver.disconnect();
      if (this._railObserver) this._railObserver.disconnect();
      if (this._headObserver) this._headObserver.disconnect();
      (this._hookedLinks || []).forEach(l => {
        l.removeEventListener('load', this._onTweakChange);
        l.removeEventListener('error', this._onTweakChange);
      });
      this._hookedLinks = [];
      if (this._onTweakChange) window.removeEventListener('tweakchange', this._onTweakChange);
      // Drop the text-wrap defaults when the last deck-stage leaves, so a
      // deleted deck's typography can't restyle whatever replaces it.
      // (#deck-stage-print-page keeps its existing keep-forever lifecycle.)
      if (!document.querySelector('deck-stage')) {
        const tw = document.getElementById('deck-stage-text-wrap');
        if (tw) tw.remove();
        const ps = document.getElementById('deck-stage-print-sizing');
        if (ps) ps.remove();
      }
    }
    attributeChangedCallback() {
      if (this._canvas) {
        this._canvas.style.width = this.designWidth + 'px';
        this._canvas.style.height = this.designHeight + 'px';
        this._canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
        this._canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
        if (this._rail) {
          this._rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
        }
        this._fit();
        this._scaleThumbs();
        this._syncPrintPageRule();
      }
    }
    _render() {
      const style = document.createElement('style');
      style.textContent = stylesheet;
      const stage = document.createElement('div');
      stage.className = 'stage';
      const canvas = document.createElement('div');
      canvas.className = 'canvas';
      canvas.style.width = this.designWidth + 'px';
      canvas.style.height = this.designHeight + 'px';
      canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
      canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
      const slot = document.createElement('slot');
      slot.addEventListener('slotchange', this._onSlotChange);
      canvas.appendChild(slot);
      stage.appendChild(canvas);

      // Overlay: compact, solid black, with clickable controls.
      const overlay = document.createElement('div');
      overlay.className = 'overlay export-hidden';
      overlay.setAttribute('role', 'toolbar');
      overlay.setAttribute('aria-label', 'Deck controls');
      overlay.setAttribute('data-omelette-chrome', '');
      overlay.innerHTML = `
        <button class="btn prev" type="button" aria-label="Previous slide" title="Previous (←)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10 3L5 8l5 5"/></svg>
        </button>
        <span class="count" aria-live="polite"><span class="current">1</span><span class="sep">/</span><span class="total">1</span></span>
        <button class="btn next" type="button" aria-label="Next slide" title="Next (→)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3l5 5-5 5"/></svg>
        </button>
        <span class="divider"></span>
        <button class="btn reset" type="button" aria-label="Reset to first slide" title="Reset (R)">Reset<span class="kbd">R</span></button>
      `;
      overlay.querySelector('.prev').addEventListener('click', () => this._advance(-1, 'click'));
      overlay.querySelector('.next').addEventListener('click', () => this._advance(1, 'click'));
      overlay.querySelector('.reset').addEventListener('click', () => this._go(0, 'click'));

      // Pin the controls while the user is interacting with them —
      // hovering, or keyboard focus on a control. The hidden overlay is
      // pointer-events:none, so these only ever engage while it's already
      // visible. 'pointer' source: these are user-interaction paths, so
      // they may show/refresh the overlay even while presenting (see
      // _flashOverlay).
      overlay.addEventListener('mouseenter', () => {
        this._overlayHover = true;
        this._flashOverlay('pointer');
      });
      overlay.addEventListener('mouseleave', () => {
        const hadPin = this._overlayHover;
        this._overlayHover = false;
        // Resume the idle fade — never summon. Without the guard, a
        // mouseleave that fires because the overlay was force-hidden
        // (presenting entry flips it to pointer-events:none under the
        // cursor) would pop the controls right back up.
        if (hadPin || overlay.hasAttribute('data-visible')) this._flashOverlay('pointer');
      });
      overlay.addEventListener('focusin', e => {
        // Keyboard-origin focus only (:focus-visible): a mouse click also
        // focuses the clicked button, and pinning on that would hold the
        // controls open indefinitely after a single click — the hover pin
        // already covers the mouse case. Engines without :focus-visible
        // fall back to pinning on any focus (the safe direction).
        var kb = true;
        try {
          var t = e.target;
          kb = !(t && t.matches && !t.matches(':focus-visible'));
        } catch (err) {
          kb = true;
        }
        if (!kb) return;
        this._overlayFocus = true;
        this._flashOverlay('pointer');
      });
      overlay.addEventListener('focusout', e => {
        // Only unpin when focus truly left the toolbar — tabbing between
        // its buttons stays pinned. relatedTarget is null when focus
        // leaves the document entirely; treat that as leaving.
        if (e.relatedTarget && overlay.contains(e.relatedTarget)) return;
        const hadPin = this._overlayFocus;
        this._overlayFocus = false;
        // Resume-the-fade only (see mouseleave): a click-focused button
        // losing focus to a later stage click must not summon the
        // controls mid-presentation.
        if (hadPin || overlay.hasAttribute('data-visible')) this._flashOverlay('pointer');
      });

      // Thumbnail rail + context menu. Thumbnails are populated in
      // _renderRail() after _collectSlides().
      const rail = document.createElement('div');
      rail.className = 'rail export-hidden';
      rail.setAttribute('data-omelette-chrome', '');
      // Edit mode hooks wheel to pan the canvas; this opts the rail's own
      // scrollview out so thumbnails stay scrollable while editing.
      rail.setAttribute('data-dc-wheel-passthru', '');
      rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
      // Edge auto-scroll while dragging a thumb near the rail's top/bottom
      // so off-screen drop targets are reachable. Native dragover fires
      // continuously while the pointer is stationary, so a per-event nudge
      // (ramped by edge proximity) is enough — no rAF loop needed.
      rail.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        const r = rail.getBoundingClientRect();
        const EDGE = 40;
        const dt = e.clientY - r.top;
        const db = r.bottom - e.clientY;
        if (dt < EDGE) rail.scrollTop -= Math.ceil((EDGE - dt) / 3);else if (db < EDGE) rail.scrollTop += Math.ceil((EDGE - db) / 3);
      });
      const menu = document.createElement('div');
      menu.className = 'ctxmenu export-hidden';
      menu.setAttribute('data-omelette-chrome', '');
      menu.innerHTML = `
        <button type="button" data-act="skip">Skip slide</button>
        <button type="button" data-act="up">Move up</button>
        <button type="button" data-act="down">Move down</button>
        <button type="button" data-act="duplicate">Duplicate slide</button>
        <hr>
        <button type="button" data-act="delete">Delete slide</button>
      `;
      menu.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        const i = this._menuIndex;
        const list = this._menuIndices;
        this._closeMenu();
        if (act === 'skip') this._toggleSkip(i);else if (act === 'up') this._moveSlide(i, i - 1);else if (act === 'down') this._moveSlide(i, i + 1);else if (act === 'duplicate') this._duplicateSlide(i);else if (act === 'delete') this._openConfirm(list && list.length ? list : [i]);
      });
      menu.addEventListener('contextmenu', e => e.preventDefault());

      // Rail resize handle — drag to set --deck-rail-w, persisted to
      // localStorage so the width survives reloads.
      const resize = document.createElement('div');
      resize.className = 'rail-resize export-hidden';
      resize.setAttribute('data-omelette-chrome', '');
      resize.addEventListener('pointerdown', e => {
        e.preventDefault();
        resize.setPointerCapture(e.pointerId);
        resize.setAttribute('data-dragging', '');
        const move = ev => this._setRailWidth(ev.clientX);
        const up = () => {
          resize.removeEventListener('pointermove', move);
          resize.removeEventListener('pointerup', up);
          resize.removeEventListener('pointercancel', up);
          resize.removeAttribute('data-dragging');
          try {
            localStorage.setItem('deck-stage.railWidth', String(this._railPx));
          } catch (err) {}
        };
        resize.addEventListener('pointermove', move);
        resize.addEventListener('pointerup', up);
        resize.addEventListener('pointercancel', up);
      });

      // Delete-confirm dialog — mirrors the SPA's ConfirmDialog layout.
      const confirm = document.createElement('div');
      confirm.className = 'confirm-backdrop export-hidden';
      confirm.setAttribute('data-omelette-chrome', '');
      confirm.innerHTML = `
        <div class="confirm" role="dialog" aria-modal="true">
          <div class="body">
            <div class="title">Delete slide?</div>
            <div class="msg">This slide will be removed from the deck.</div>
          </div>
          <div class="footer">
            <button type="button" class="cancel">Cancel</button>
            <button type="button" class="danger">Delete</button>
          </div>
        </div>
      `;
      confirm.addEventListener('click', e => {
        if (e.target === confirm) {
          this._closeConfirm();
          this._focusCurrentThumb();
        }
      });
      confirm.querySelector('.cancel').addEventListener('click', () => {
        this._closeConfirm();
        this._focusCurrentThumb();
      });
      confirm.querySelector('.danger').addEventListener('click', () => {
        // Re-resolve at click time — the elements are the user's actual
        // selection; their indices may have shifted since confirm-open.
        const list = (this._confirmEls || []).map(el => this._slides.indexOf(el)).filter(i => i >= 0);
        this._closeConfirm();
        this._deleteSlides(list);
        this._focusCurrentThumb();
      });
      this._root.append(style, rail, resize, stage, overlay, menu, confirm);
      this._canvas = canvas;
      this._stage = stage;
      this._slot = slot;
      this._overlay = overlay;
      this._rail = rail;
      this._resize = resize;
      this._menu = menu;
      this._confirm = confirm;
      this._countEl = overlay.querySelector('.current');
      this._totalEl = overlay.querySelector('.total');

      // Restore persisted rail width.
      let rw = 188;
      try {
        const s = localStorage.getItem('deck-stage.railWidth');
        if (s) rw = parseInt(s, 10) || rw;
      } catch (err) {}
      this._setRailWidth(rw);
      this._syncRailHidden();
    }
    _setRailWidth(px) {
      const w = Math.max(120, Math.min(360, Math.round(px)));
      this._railPx = w;
      this.style.setProperty('--deck-rail-w', w + 'px');
      this._fit();
      // _scaleThumbs forces a sync layout (frame.offsetWidth) then writes
      // N transforms. During a resize drag this runs per-pointermove;
      // coalesce to one per frame.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }

    /** @page must live in the document stylesheet — it's a no-op inside
     *  shadow DOM. (Re-)append so any author @page landing later in
     *  source order can't reintroduce a margin and push each slide onto
     *  two sheets; called again from beforeprint. */
    _syncPrintPageRule() {
      const id = 'deck-stage-print-page';
      let tag = document.getElementById(id);
      if (!tag) {
        tag = document.createElement('style');
        tag.id = id;
      }
      (document.body || document.head).appendChild(tag);
      tag.textContent = '@page { size: ' + this.designWidth + 'px ' + this.designHeight + 'px; margin: 0; } ' + '@media print { html, body { margin: 0 !important; padding: 0 !important; background: none !important; overflow: visible !important; height: auto !important; } ' + '* { -webkit-print-color-adjust: exact; print-color-adjust: exact; ' + 'backdrop-filter: none !important; -webkit-backdrop-filter: none !important; } ' +
      // Jump authored animations/transitions to their end state so print
      // never captures mid-entrance — pairs with the beforeprint handler
      // in connectedCallback that sets data-deck-active on every slide.
      '*, *::before, *::after { animation-delay: -99s !important; animation-duration: .001s !important; ' + 'animation-iteration-count: 1 !important; animation-fill-mode: both !important; ' + 'animation-play-state: running !important; transition-duration: 0s !important; } }';
    }

    /** Announces the deck's print-sizing mode to the host app:
     *  meta[name="omelette-print-sizing"] content "default-landscape" — a
     *  deck prints one slide per page on the user's paper size, landscape.
     *  The export path probes the meta to decide what true paper size to
     *  inject at print time (the @page px rule above stays as the
     *  standalone-print fallback; an injected later rule overrides it).
     *  Never overrides an authored meta or another component's; removed
     *  when the last deck-stage leaves. data-omelette-injected keeps it
     *  out of serialized source. */
    _ensurePrintSizingMeta() {
      if (document.querySelector('meta[name="omelette-print-sizing"]')) return;
      const tag = document.createElement('meta');
      tag.id = 'deck-stage-print-sizing';
      tag.name = 'omelette-print-sizing';
      tag.content = 'default-landscape';
      tag.setAttribute('data-omelette-injected', '');
      document.head.appendChild(tag);
    }

    /** Typographic defaults for slide text: balance headings, avoid
     *  widowed/orphaned words in body copy (browsers without text-wrap
     *  support drop the declarations). Zero-specificity via :where() so
     *  any text-wrap authored on those elements wins. Lives in the document,
     *  not the shadow root, for two reasons: document rules reach the
     *  slotted (light DOM) slides, and _snapshotAuthorCss copies document
     *  stylesheets into each thumbnail's shadow root, so the thumbs wrap
     *  the same way — a deck-stage-scoped selector would match nothing
     *  there. data-omelette-injected marks the tag for the host editor
     *  to strip at serialize, so it is never written back as authored
     *  source. */
    _ensureTextWrapDefaults() {
      if (document.getElementById('deck-stage-text-wrap')) return;
      const tag = document.createElement('style');
      tag.id = 'deck-stage-text-wrap';
      tag.setAttribute('data-omelette-injected', '');
      tag.textContent = ':where(h1,h2,h3,h4,h5,h6){text-wrap:balance}' + ':where(p,li,blockquote,figcaption){text-wrap:pretty}';
      document.head.appendChild(tag);
    }
    _onSlotChange() {
      // Self-mutate path already reconciled synchronously and emitted
      // slidechange; skip the async slotchange it caused.
      if (this._squelchSlotChange) {
        this._squelchSlotChange = false;
        return;
      }
      // Primary lock-clear is the host's __deck_rail_ack; this clears on a
      // dropped ack so the rail can't stay dead.
      this._railLock = false;
      this._collectSlides();
      this._restoreIndex();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'init'
      });
      this._fit();
      // The deck just changed under any open rail surface — an open
      // confirm or menu is a question about the OLD deck (its labels and
      // counts may now lie), so close them rather than let a stale
      // answer fire. The element-held selection re-resolves, but the
      // user should re-read what they're deleting.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        this._closeConfirm();
        // The dialog held focus (danger button); hand it back to the rail.
        this._focusCurrentThumb(true);
      }
      if (this._menu && this._menu.hasAttribute('data-open')) this._closeMenu();
      // Editor-mode deletes rebuild the rail through here; a confirmed
      // delete that started from the keyboard still owes focus to the
      // (new) current thumb.
      if (this._pendingRailRefocus) this._focusCurrentThumb(true);
    }
    _collectSlides() {
      const assigned = this._slot.assignedElements({
        flatten: true
      });
      this._slides = assigned.filter(el => {
        // Skip template/style/script nodes even if someone slots them.
        const tag = el.tagName;
        return tag !== 'TEMPLATE' && tag !== 'SCRIPT' && tag !== 'STYLE';
      });
      this._slideSet = new Set(this._slides);
      // Selection is element-keyed: drop entries whose slide is gone
      // (deleted, or replaced wholesale by a host re-render).
      if (this._selected && this._selected.size) {
        this._selected.forEach(s => {
          if (!this._slideSet.has(s)) this._selected.delete(s);
        });
      }
      if (this._selAnchor && !this._slideSet.has(this._selAnchor)) this._selAnchor = null;
      this._slides.forEach((slide, i) => {
        const n = i + 1;
        slide.setAttribute('data-screen-label', `${pad2(n)} ${getSlideLabel(slide)}`);

        // Validation attribute for comment flow / auto-checks.
        if (!slide.hasAttribute('data-om-validate')) {
          slide.setAttribute('data-om-validate', VALIDATE_ATTR);
        }
        slide.setAttribute('data-deck-slide', String(i));
      });
      if (this._index >= this._slides.length) this._index = Math.max(0, this._slides.length - 1);
      this._markLastVisible();
      this._syncCount();
      this._renderRail();
    }

    /** Tag the last non-skipped slide so print CSS can drop its
     *  break-after (see the @media print comment above — :last-child
     *  alone matches a hidden skipped slide). */
    _markLastVisible() {
      let last = null;
      this._slides.forEach(s => {
        s.removeAttribute('data-deck-last-visible');
        if (!s.hasAttribute('data-deck-skip')) last = s;
      });
      if (last) last.setAttribute('data-deck-last-visible', '');
    }
    _loadNotes() {
      // Per-slide data-speaker-notes is authoritative when present (attrs
      // travel with the element on reorder/dup/delete); a slide without
      // the attr falls through to the legacy #speaker-notes JSON array
      // PER SLIDE so a single attr on a JSON-authored deck doesn't blank
      // the rest.
      const tag = document.getElementById('speaker-notes');
      let json = null;
      if (tag) try {
        const p = JSON.parse(tag.textContent || '[]');
        if (Array.isArray(p)) json = p;
      } catch (e) {
        console.warn('[deck-stage] Failed to parse #speaker-notes JSON:', e);
      }
      this._notes = this._slides.map((s, i) => {
        const a = s.getAttribute('data-speaker-notes');
        return a !== null ? a : json && typeof json[i] === 'string' ? json[i] : '';
      });
    }
    _restoreIndex() {
      // The host's ?slide= param is delivered as a #<int> hash (1-indexed) on
      // the iframe src. No hash → slide 1; the deck itself keeps no position
      // state across loads.
      const h = (location.hash || '').match(/^#(\d+)$/);
      if (h) {
        const n = parseInt(h[1], 10) - 1;
        if (n >= 0 && n < this._slides.length) this._index = n;
      }
    }
    _applyIndex({
      showOverlay = true,
      broadcast = true,
      reason = 'init'
    } = {}) {
      if (!this._slides.length) return;
      const prev = this._prevIndex == null ? -1 : this._prevIndex;
      const curr = this._index;
      // Keep the iframe's own hash in sync so an in-iframe location.reload()
      // (reload banner path in viewer-handle.ts) lands on the current slide,
      // not the stale deep-link hash from initial load.
      try {
        history.replaceState(null, '', '#' + (curr + 1));
      } catch (e) {}
      this._slides.forEach((s, i) => {
        if (i === curr) s.setAttribute('data-deck-active', '');else s.removeAttribute('data-deck-active');
      });
      this._syncCount();
      // Follow-scroll on every navigation (init deep-link, keyboard, click,
      // tap, external goTo) — the only time we *don't* want the rail to
      // track current is after a rail-internal mutation, where _renderRail
      // has already restored the user's scroll position and yanking back to
      // current would undo it.
      this._syncRail(reason !== 'mutation');
      if (broadcast) {
        // (1) Legacy: host-window postMessage for speaker-notes renderers.
        try {
          window.postMessage({
            slideIndexChanged: curr,
            deckTotal: this._slides.length,
            deckSkipped: this._skippedIndices()
          }, '*');
        } catch (e) {}

        // (2) In-page CustomEvent on the <deck-stage> element itself.
        //     Bubbles and composes out of shadow DOM so slide code can listen:
        //       document.querySelector('deck-stage').addEventListener('slidechange', e => {
        //         e.detail.index, e.detail.previousIndex, e.detail.total, e.detail.slide, e.detail.reason
        //       });
        const detail = {
          index: curr,
          previousIndex: prev,
          total: this._slides.length,
          slide: this._slides[curr] || null,
          previousSlide: prev >= 0 ? this._slides[prev] || null : null,
          reason: reason // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
        };
        this.dispatchEvent(new CustomEvent('slidechange', {
          detail,
          bubbles: true,
          composed: true
        }));
      }
      this._prevIndex = curr;
      if (showOverlay) this._flashOverlay();
    }
    _flashOverlay(source) {
      // Host posts __omelette_presenting while in fullscreen/tab
      // presentation mode. While presenting, the overlay is
      // pointer-summoned only: it appears on mouse movement and while the
      // user hovers/focuses the controls (source 'pointer'), but never
      // flashes on slide changes or nav-key presses (the default 'auto'
      // source) — a keyboard-driven advance must not blink chrome at the
      // audience. Outside presenting, both sources flash as before.
      if (!this._overlay) return;
      if (this._presenting && source !== 'pointer') return;
      this._overlay.setAttribute('data-visible', '');
      if (this._hideTimer) clearTimeout(this._hideTimer);
      this._hideTimer = setTimeout(() => {
        // Pinned by hover or focus on the controls — keep them up. The
        // matching mouseleave/focusout re-flashes, so the idle fade
        // resumes from that moment.
        if (this._overlayHover || this._overlayFocus) return;
        this._overlay.removeAttribute('data-visible');
      }, OVERLAY_HIDE_MS);
    }
    _railWidth() {
      // State-based, no offsetWidth: the first _fit() can run before the
      // rail has had layout on some load paths, and a 0 there paints the
      // slide full-width for one frame before the post-slotchange _fit()
      // corrects it.
      if (!this._railEnabled || !this._railVisible || this.hasAttribute('no-rail') || this.hasAttribute('noscale') || this._presenting || this._previewMode || NARROW_MQ.matches) return 0;
      return this._railPx || 0;
    }
    _fit() {
      if (!this._canvas) return;
      const stage = this._canvas.parentElement;
      // PPTX export sets noscale so the DOM capture sees authored-size
      // geometry — the scaled canvas is in shadow DOM, so the exporter's
      // resetTransformSelector can't reach .canvas.style.transform directly.
      if (this.hasAttribute('noscale')) {
        this._canvas.style.transform = 'none';
        if (stage) stage.style.left = '0';
        if (this._overlay) this._overlay.style.marginLeft = '0';
        return;
      }
      const rw = this._railWidth();
      if (stage) stage.style.left = rw + 'px';
      // Overlay is centred on the viewport via left:50% + translate(-50%);
      // marginLeft shifts the centre by rw/2 so it lands in the middle of
      // the [rw, innerWidth] stage region.
      if (this._overlay) this._overlay.style.marginLeft = rw / 2 + 'px';
      const vw = window.innerWidth - rw;
      const vh = window.innerHeight;
      const s = Math.min(vw / this.designWidth, vh / this.designHeight);
      this._canvas.style.transform = `scale(${s})`;
    }
    _onResize() {
      this._fit();
      // Crossing the narrow-viewport breakpoint reveals the rail — rerun the
      // thumbnail scale the same way _setRailWidth does.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }
    _onMouseMove() {
      // Keep overlay visible while mouse moves; hide after idle. 'pointer'
      // source: mouse movement summons the controls even while presenting.
      this._flashOverlay('pointer');
    }
    _onMessage(e) {
      const d = e.data;
      if (d && typeof d.__omelette_presenting === 'boolean') {
        // Unchanged value → idempotent re-delivery (the guest bundle
        // re-posts when a deck mounts mid-presentation, and host + bundle
        // can both deliver at entry). Skip the resets: re-running the
        // entry work on every delivery would dismiss the pointer-summoned
        // overlay under a hovering cursor and close menus on every slide
        // change. Mirrors the preview_mode branch's unchanged-value guard
        // below.
        if (d.__omelette_presenting !== !!this._presenting) {
          this._presenting = d.__omelette_presenting;
          // A presenting transition invalidates interaction pins: carried
          // across the flip, a stale pin would hold the first summoned
          // overlay open with no pointer anywhere near it. Hide on BOTH
          // transitions: entry cleans the audience's screen, and on exit a
          // pin-skipped hide timeout may have left data-visible set with
          // no timer armed — without this, the footer would linger in the
          // editor until the next mousemove. The next interaction
          // re-summons it either way.
          this._overlayHover = false;
          this._overlayFocus = false;
          if (this._overlay) {
            this._overlay.removeAttribute('data-visible');
            if (this._hideTimer) clearTimeout(this._hideTimer);
          }
          this._syncRailHidden();
          this._closeMenu();
          this._closeConfirm();
          this._fit();
          this._scaleThumbs();
        }
      }
      // Host's Preview segment (ViewerMode='none'): the rail's drag-reorder /
      // right-click skip-delete affordances are editing chrome, so hide it
      // while the user is just looking at the deck. Same hard-hide path as
      // presenting; independent of the user's _railVisible preference so
      // returning to Edit restores whatever they had.
      if (d && typeof d.__omelette_preview_mode === 'boolean') {
        if (d.__omelette_preview_mode === this._previewMode) return;
        this._previewMode = d.__omelette_preview_mode;
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Host has processed a dc-op; rail input is safe again. Not tied to
      // slotchange — setAttr and refusal don't fire one. On refusal,
      // revert the optimistic _index/hash adjustment so the next nav
      // starts from what's actually on screen.
      if (d && d.__dc_op_ack) {
        this._railLock = false;
        if (d.applied === false && this._indexBeforeEmit != null) {
          this._index = this._indexBeforeEmit;
          try {
            history.replaceState(null, '', '#' + (this._index + 1));
          } catch (e) {}
        }
        this._indexBeforeEmit = null;
        // A refused op never re-renders, so slotchange won't restore the
        // keyboard flow's focus — do it here. (Applied ops refocus in
        // _onSlotChange, after the rail has been rebuilt.)
        if (d.applied === false && this._pendingRailRefocus) {
          this._focusCurrentThumb(true);
        }
      }
      // Per-viewer show/hide, driven by the TweaksPanel's auto-injected
      // "Thumbnail rail" toggle (or any author script). Independent of
      // whether the Tweaks panel itself is open — closing the panel
      // doesn't change rail visibility. Persists alongside rail width.
      if (d && d.type === '__deck_rail_visible' && typeof d.on === 'boolean') {
        if (d.on === this._railVisible) return;
        this._railVisible = d.on;
        try {
          localStorage.setItem('deck-stage.railVisible', d.on ? '1' : '0');
        } catch (e) {}
        // Arm the transition, commit it, then flip state — otherwise the
        // browser coalesces both writes and nothing animates on show.
        this.setAttribute('data-rail-anim', '');
        void (this._rail && this._rail.offsetHeight);
        this._syncRailHidden();
        this._fit();
        this._scaleThumbs();
        clearTimeout(this._railAnimTimer);
        this._railAnimTimer = setTimeout(() => this.removeAttribute('data-rail-anim'), 220);
      }
      if (d && d.type === '__omelette_rail_enabled') this._enableRail();
    }
    _syncRailHidden() {
      if (!this._rail) return;
      // data-presenting is the hard hide (display:none) for flag-off,
      // presentation mode, and the host's Preview segment — instant, no
      // transition. data-user-hidden is the soft hide (translateX(-100%))
      // for the viewer's rail toggle, so show/hide slides under
      // :host([data-rail-anim]).
      const hard = !this._railEnabled || this._presenting || this._previewMode;
      if (hard) this._rail.setAttribute('data-presenting', '');else this._rail.removeAttribute('data-presenting');
      if (!this._railVisible) this._rail.setAttribute('data-user-hidden', '');else this._rail.removeAttribute('data-user-hidden');
      // translateX hide leaves thumbs (tabIndex=0) in the tab order —
      // inert keeps them unfocusable while the rail is off-screen.
      this._rail.inert = hard || !this._railVisible;
    }
    _onTap(e) {
      // Touch-only — keyboard + the overlay toolbar cover nav on desktop.
      if (FINE_POINTER_MQ.matches) return;
      // Only taps that land on the stage (slide content or letterbox); the
      // overlay / rail / menus are siblings with their own click handlers.
      const path = e.composedPath();
      if (!this._stage || !path.includes(this._stage)) return;
      // Let interactive slide content keep the tap. composedPath (not
      // e.target.closest) so we see through open shadow roots — a <button>
      // inside a slide-authored custom element retargets e.target to the
      // host but still appears in the composed path.
      if (e.defaultPrevented) return;
      for (const n of path) {
        if (n === this._stage) break;
        if (n.matches && n.matches(INTERACTIVE_SEL)) return;
      }
      e.preventDefault();
      const rw = this._railWidth();
      const mid = rw + (window.innerWidth - rw) / 2;
      this._advance(e.clientX < mid ? -1 : 1, 'tap');
    }
    _onKey(e) {
      // Ignore when the user is typing. composedPath()[0], not e.target: a
      // window-level keydown retargets e.target to the shadow host, which
      // would miss an <input> or contenteditable inside a web component on
      // a slide (same reason _onTap uses composedPath).
      const t = e.composedPath ? e.composedPath()[0] : e.target;
      if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
      // Confirm dialog swallows nav keys while open; Escape cancels. Enter
      // is left to the focused button's native activation so Tab→Cancel
      // →Enter activates Cancel, not the window-level confirm path.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        if (e.key === 'Escape') {
          this._closeConfirm();
          this._focusCurrentThumb();
          e.preventDefault();
        }
        return;
      }
      if (e.key === 'Escape' && this._menu && this._menu.hasAttribute('data-open')) {
        this._closeMenu();
        e.preventDefault();
        return;
      }
      if (e.key === 'Escape' && this._selected.size) {
        // Collapse the multi-selection back to the current slide (the
        // implicit selection), not to nothing.
        this._clearSelection();
        e.preventDefault();
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      const key = e.key;
      let handled = true;
      if (key === 'ArrowRight' || key === 'PageDown' || key === ' ' || key === 'Spacebar') {
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowLeft' || key === 'PageUp') {
        this._advance(-1, 'keyboard');
      } else if (key === 'ArrowDown' && !e.defaultPrevented) {
        // ↓/↑ page slides like →/← (Keynote/PowerPoint parity). Window
        // level only: rail thumbs keep their own ↑/↓ walk (their handler
        // stops propagation before this one), and the typing guard above
        // already covers inputs and contenteditable slide content.
        // Deliberate tradeoff: like Space/PageDown before them, these are
        // scroll keys — slide content that wants keyboard scrolling claims
        // them with preventDefault, which this branch honors (checked here
        // and not for the long-standing keys above, so ←/→/Space behavior
        // is unchanged and ↑/↓ behave identically on frozen copies, whose
        // translator in the guest bundle applies the same guard).
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowUp' && !e.defaultPrevented) {
        this._advance(-1, 'keyboard');
      } else if (key === 'Home') {
        this._go(0, 'keyboard');
      } else if (key === 'End') {
        this._go(this._slides.length - 1, 'keyboard');
      } else if (key === 'r' || key === 'R') {
        this._go(0, 'keyboard');
      } else if (/^[0-9]$/.test(key)) {
        // 1..9 jump to that slide; 0 jumps to 10.
        const n = key === '0' ? 9 : parseInt(key, 10) - 1;
        if (n < this._slides.length) this._go(n, 'keyboard');
      } else {
        handled = false;
      }
      if (handled) {
        e.preventDefault();
        this._flashOverlay();
      }
    }
    _go(i, reason = 'api') {
      // User-initiated navigation collapses a multi-selection down to
      // the (implicit) current slide, like Keynote's arrow keys. 'click'
      // handles its own selection; programmatic reasons leave it alone.
      if (reason === 'keyboard' || reason === 'tap') this._clearSelection();
      if (!this._slides.length) return;
      const clamped = Math.max(0, Math.min(this._slides.length - 1, i));
      if (clamped === this._index) {
        this._flashOverlay();
        return;
      }
      this._index = clamped;
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason
      });
    }

    /** Step forward/back skipping any slide marked data-deck-skip. Falls
     *  back to _go's clamp-at-ends behaviour (flash overlay) when there's
     *  nothing further in that direction. */
    _advance(dir, reason) {
      if (!this._slides.length) return;
      let i = this._index + dir;
      while (i >= 0 && i < this._slides.length && this._slides[i].hasAttribute('data-deck-skip')) {
        i += dir;
      }
      if (i < 0 || i >= this._slides.length) {
        this._flashOverlay();
        return;
      }
      this._go(i, reason);
    }

    // ── Thumbnail rail ────────────────────────────────────────────────────
    //
    // Thumbs are keyed by slide element and reused across _renderRail()
    // calls, so a reorder/delete is an O(changed) DOM shuffle instead of an
    // O(N) teardown-and-re-clone. Each thumb starts as a lightweight shell
    // (num + empty frame); the clone is materialized lazily by an
    // IntersectionObserver when the frame scrolls into (or near) view, so
    // only visible-ish slides pay the clone + image-decode cost.

    _renderRail() {
      if (!this._rail || !this._railEnabled) {
        this._thumbs = [];
        return;
      }
      // FLIP: record each *materialized* thumb's top before the reconcile.
      // Off-screen (non-materialized) thumbs don't need the animation and
      // skipping their getBoundingClientRect saves a forced layout per
      // off-screen thumb on large decks.
      const prevTops = new Map();
      (this._thumbs || []).forEach(({
        thumb,
        slide,
        host
      }) => {
        if (host) prevTops.set(slide, thumb.getBoundingClientRect().top);
      });
      const st = this._rail.scrollTop;

      // Reconcile: reuse thumbs that already exist for a slide, create
      // shells for new slides, drop thumbs for removed slides.
      const bySlide = new Map();
      (this._thumbs || []).forEach(t => bySlide.set(t.slide, t));
      const next = [];
      this._slides.forEach(slide => {
        let t = bySlide.get(slide);
        if (t) bySlide.delete(slide);else t = this._makeThumb(slide);
        next.push(t);
      });
      // Orphans — slides removed since last render.
      bySlide.forEach(t => {
        if (this._railObserver) this._railObserver.unobserve(t.frame);
        t.thumb.remove();
      });
      // Put thumbs into document order to match _slides. insertBefore on
      // an already-correctly-placed node is a no-op, so this is cheap
      // when nothing moved.
      next.forEach((t, i) => {
        const want = t.thumb;
        const at = this._rail.children[i];
        if (at !== want) this._rail.insertBefore(want, at || null);
        t.i = i;
        if (t.slide.hasAttribute('data-deck-skip')) t.thumb.setAttribute('data-skip', '');else t.thumb.removeAttribute('data-skip');
        if (this._selected.has(t.slide)) t.thumb.setAttribute('data-selected', '');else t.thumb.removeAttribute('data-selected');
      });
      this._thumbs = next;
      this._renumberRail();
      this._rail.scrollTop = st;
      if (prevTops.size) {
        const moved = [];
        this._thumbs.forEach(({
          thumb,
          slide
        }) => {
          // The live-dragged thumb is positioned by the drag tracker; a
          // FLIP transform+transition here would clobber it mid-drag.
          if (thumb === this._dragThumb) return;
          const old = prevTops.get(slide);
          if (old == null) return;
          const dy = old - thumb.getBoundingClientRect().top;
          if (Math.abs(dy) < 1) return;
          thumb.style.transition = 'none';
          thumb.style.transform = `translateY(${dy}px)`;
          moved.push(thumb);
        });
        if (moved.length) {
          // Commit the inverted positions before flipping the transition
          // on — otherwise the browser coalesces both style writes and
          // nothing animates.
          void this._rail.offsetHeight;
          moved.forEach(t => {
            t.style.transition = 'transform 180ms cubic-bezier(.2,.7,.3,1)';
            t.style.transform = '';
          });
          setTimeout(() => moved.forEach(t => {
            t.style.transition = '';
          }), 220);
        }
      }
      requestAnimationFrame(() => this._scaleThumbs());
      this._syncRail(false);
    }

    /** Create a lightweight thumb shell for one slide. The clone is
     *  materialized later by the IntersectionObserver. Event handlers
     *  look up the thumb's *current* index (via _thumbs.indexOf) so the
     *  same element can be reused across reorders. */
    _makeThumb(slide) {
      const thumb = document.createElement('div');
      thumb.className = 'thumb';
      thumb.tabIndex = 0;
      const num = document.createElement('div');
      num.className = 'num';
      const frame = document.createElement('div');
      frame.className = 'frame';
      thumb.append(num, frame);
      const entry = {
        thumb,
        num,
        frame,
        slide,
        clone: null,
        host: null,
        i: -1
      };
      // entry.i is refreshed on every _renderRail reconcile pass, so
      // handlers read the thumb's current position without an O(N) scan.
      const idx = () => entry.i;
      thumb.addEventListener('click', e => {
        const i = idx();
        const slide = this._slides[i];
        // WebKit doesn't focus a plain element on click — focus
        // explicitly so Delete/Backspace works right after selecting a
        // slide by mouse. preventScroll: _syncRail owns the rail's
        // scroll position.
        thumb.focus({
          preventScroll: true
        });
        if (e.shiftKey || e.metaKey || e.ctrlKey) {
          // Multi-select gestures adjust the selection without
          // navigating (Keynote/Figma convention).
          e.preventDefault();
          if (e.shiftKey) {
            // Range from the anchor (last plain/cmd-clicked slide;
            // falls back to the current slide) to here, replacing any
            // previous range.
            let a = this._selAnchor ? this._slides.indexOf(this._selAnchor) : -1;
            if (a < 0) {
              a = this._index;
              this._selAnchor = this._slides[a] || null;
            }
            this._selected.clear();
            for (let j = Math.min(a, i); j <= Math.max(a, i); j++) {
              this._selected.add(this._slides[j]);
            }
          } else if (slide) {
            // Toggle. An empty explicit selection implicitly holds the
            // current slide — materialize it first so cmd-clicking a
            // second slide selects both.
            if (!this._selected.size && i !== this._index && this._slides[this._index]) {
              this._selected.add(this._slides[this._index]);
            }
            if (this._selected.has(slide)) this._selected.delete(slide);else {
              this._selected.add(slide);
              this._selAnchor = slide;
            }
          }
          this._syncSelection();
          return;
        }
        this._clearSelection();
        this._selAnchor = slide || null;
        this._go(i, 'click');
      });
      // ↑/↓ step through the rail when a thumb has focus. _go clamps at the
      // ends and _applyIndex→_syncRail scrolls the new current thumb into
      // view; we move focus to it (preventScroll — _syncRail already
      // scrolled) so a held key walks the whole list. stopPropagation keeps
      // this out of the window-level _onKey nav handler.
      thumb.addEventListener('keydown', e => {
        // Delete/Backspace with the rail focused deletes this thumb's
        // slide through the same confirm dialog as the menu item.
        // Listening on the thumb (never window-level) is what keeps
        // typing in the notes panel / slide inputs from ever landing
        // here; the target check is belt-and-braces for anything
        // focusable that ends up inside a thumb.
        if ((e.key === 'Delete' || e.key === 'Backspace') && !e.metaKey && !e.ctrlKey && !e.altKey) {
          const t = e.target;
          if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
          e.preventDefault();
          e.stopPropagation();
          // Same refusals as the menu item: never every slide, never
          // while a prior structural op is waiting on its ack. The
          // whole-deck refusal is announced (the menu greys its item
          // out; a silently dead key reads as breakage). The rail-lock
          // refusal stays silent: it lasts one ack round-trip and
          // matches the existing single-delete behavior.
          if (this._railLock) return;
          // Explicit selection wins; otherwise the focused thumb (which
          // plain click and ↑/↓ keep equal to the current slide).
          const sel = this._selected.size ? this._selectionIndices() : [idx()];
          if (sel.length >= this._slides.length) {
            this._showNotice(sel.length === 1 ? 'The last slide can’t be deleted.' : 'At least one slide has to stay — the whole deck can’t be deleted.');
            return;
          }
          this._openConfirm(sel);
          return;
        }
        if (e.key !== 'ArrowUp' && e.key !== 'ArrowDown') return;
        if (e.metaKey || e.ctrlKey || e.altKey) return;
        e.preventDefault();
        e.stopPropagation();
        this._go(idx() + (e.key === 'ArrowDown' ? 1 : -1), 'keyboard');
        const cur = this._thumbs && this._thumbs[this._index];
        if (cur) cur.thumb.focus({
          preventScroll: true
        });
      });
      thumb.addEventListener('contextmenu', e => {
        e.preventDefault();
        this._openMenu(idx(), e.clientX, e.clientY);
      });
      thumb.draggable = true;
      thumb.addEventListener('dragstart', e => {
        // v1: dragging moves ONE slide, so a multi-selection would lie
        // about what's about to move — collapse it. (Group drag would
        // instead keep it and emit a batched move.)
        this._clearSelection();
        this._dragFrom = idx();
        // Deferred to the next frame: the [data-dragging] rule sets
        // pointer-events:none on the drag SOURCE, and applying that
        // synchronously inside dragstart makes Chromium (and WebKit) cancel
        // the drag — dragstart then an immediate dragend, no dragover or
        // drop, so thumbnails could not be reordered by dragging at all.
        // One frame is invisible and lands before the first dragover needs
        // the source to be hit-test-transparent. Guarded twice so the
        // attribute can never strand on a thumb that is no longer being
        // dragged (pointer-events:none would leave it unclickable for the
        // session): the pending frame is cancelled in dragend
        // (_cancelDragAttr), and the callback itself re-checks that THIS
        // thumb is still the live drag source (a new drag on another thumb
        // re-points the drag state). Deliberately NOT cancelled in
        // _stopDragTrack — _startDragTrack calls it at the start of every
        // drag, which would kill the mark this dragstart just scheduled
        // (see _cancelDragAttr).
        this._dragAttrRaf = requestAnimationFrame(() => {
          this._dragAttrRaf = null;
          if (this._dragFrom != null && this._dragThumb === thumb) {
            thumb.setAttribute('data-dragging', '');
          }
        });
        e.dataTransfer.effectAllowed = 'move';
        try {
          e.dataTransfer.setData('text/plain', String(this._dragFrom));
        } catch (err) {}
        // Constrain the drag visual to the rail's vertical axis. The
        // browser's default drag image is a free-floating snapshot that
        // follows the OS cursor in BOTH axes and the DnD API offers no way
        // to constrain it — so swap it for a transparent stand-in and move
        // the thumb itself along Y instead (_startDragTrack). The drop
        // logic below always read only clientY; this makes the visual
        // match it.
        try {
          e.dataTransfer.setDragImage(this._dragBlank(), 0, 0);
        } catch (err) {}
        this._startDragTrack(thumb, e.clientY);
      });
      thumb.addEventListener('dragend', () => {
        this._cancelDragAttr();
        thumb.removeAttribute('data-dragging');
        this._stopDragTrack();
        this._clearDrop();
        this._dragFrom = null;
      });
      thumb.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        e.dataTransfer.dropEffect = 'move';
        const r = thumb.getBoundingClientRect();
        this._setDrop(idx(), e.clientY < r.top + r.height / 2 ? 'before' : 'after');
      });
      thumb.addEventListener('drop', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        const i = idx();
        const r = thumb.getBoundingClientRect();
        let to = e.clientY >= r.top + r.height / 2 ? i + 1 : i;
        if (this._dragFrom < to) to--;
        const from = this._dragFrom;
        this._clearDrop();
        this._dragFrom = null;
        if (to !== from) this._moveSlide(from, to);
      });
      if (this._railObserver) this._railObserver.observe(frame);
      frame.__deckThumb = entry;
      return entry;
    }

    /** Lazily build the clone for a thumb that has scrolled into view. */
    _materialize(entry) {
      if (entry.host) return;
      const dw = this.designWidth,
        dh = this.designHeight;
      let clone = entry.slide.cloneNode(true);
      // The clone participates in the document's flat tree, so the
      // templates' position-based CSS page counters (.slide
      // { counter-increment: page }) would count every materialized
      // thumb before the real slides — folios print offset by the
      // thumb count (slide 2 reading "7" on a five-slide deck).
      // Neutralize the counter on the clone and drop its folio pill:
      // a thumbnail's own page number is unreadable at thumb scale
      // anyway, and the real slides' numbers stay truthful.
      clone.style.counterIncrement = 'none';
      clone.querySelectorAll('.page-foot').forEach(pf => pf.remove());
      // Canvas bitmaps don't clone — swap each cloned canvas for an <img>
      // of the live pixels. Best-effort: tainted canvases throw (left
      // as-is); zero-size are skipped; WebGL without preserveDrawingBuffer
      // reads back blank and the thumb gets a blank img (same as before).
      const liveCanvases = entry.slide.querySelectorAll('canvas');
      const cloneCanvases = clone.querySelectorAll('canvas');
      cloneCanvases.forEach((cv, i) => {
        const live = liveCanvases[i];
        if (!live || !live.width || !live.height) return;
        try {
          const img = document.createElement('img');
          img.src = live.toDataURL();
          img.alt = '';
          img.style.cssText = cv.style.cssText;
          img.className = cv.className;
          img.width = live.width;
          img.height = live.height;
          // Author CSS that sized the <canvas> via tag selector won't match
          // the <img> — pin the live canvas's laid-out box on the snapshot.
          if (live.clientWidth) {
            img.style.width = live.clientWidth + 'px';
            img.style.height = live.clientHeight + 'px';
          }
          cv.replaceWith(img);
        } catch (e) {}
      });
      // Neuter heavy media; replace <video> with its poster so the box
      // keeps a visual. <iframe>/<audio> become empty placeholders.
      // Parity with _inertify: transient top-layer UI never belongs in a
      // static thumb.
      clone.querySelectorAll('[popover], dialog').forEach(el => el.remove());
      clone.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      clone.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      // Images: defer decode and let the browser pick the smallest
      // srcset candidate for the ~140px thumb. Same-URL clones reuse the
      // slide's decoded bitmap (URL-keyed cache), so the remaining cost
      // is paint/composite — lazy+async keeps that off the main thread.
      clone.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Custom elements inside the slide would have their
      // connectedCallback fire when the clone is appended. Replace them
      // with inert boxes (_neuter) so a component-heavy deck doesn't run
      // N copies of each component's mount logic in the rail. Children
      // are preserved so layout-wrapper elements (<my-column><h2>…</h2>)
      // still show their authored content, and a shadow tree cloned along
      // via attachShadow({clonable:true}) (e.g. <image-slot>) moves onto
      // the box so the thumb shows the component's rendered content. The
      // querySelectorAll NodeList is static, so nested custom elements in
      // the moved subtree are still visited on later iterations.
      // querySelectorAll('*') returns descendants only — a custom-element
      // slide root (<my-slide>…</my-slide>) would slip through and upgrade
      // on append. Swap the root first.
      if (clone.tagName.includes('-')) clone = this._neuter(clone);
      clone.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(this._neuter(el));
      });
      // Strip ids only now: a defined custom element upgrades synchronously
      // during cloneNode and re-renders on attribute callbacks, so removing
      // 'id' any earlier resets components (e.g. <image-slot> falls back to
      // its author src). Post-neuter, only inert boxes and plain elements
      // remain, where the strip is just the usual duplicate-id hygiene.
      clone.removeAttribute('id');
      clone.removeAttribute('data-deck-active');
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      clone.style.cssText += ';position:absolute;top:0;left:0;transform-origin:0 0;' + 'pointer-events:none;width:' + dw + 'px;height:' + dh + 'px;' + 'box-sizing:border-box;overflow:hidden;visibility:visible;opacity:1;';
      const host = document.createElement('div');
      host.style.cssText = 'position:absolute;inset:0;';
      // Clones are display-only: inert removes anything focusable inside
      // them from the tab order, so the rail's Delete/Backspace handler
      // can never see a (retargeted) key press from cloned content.
      host.inert = true;
      this._syncThumbHostAttrs(host);
      const sr = host.attachShadow({
        mode: 'open'
      });
      if (this._adoptedSheet) sr.adoptedStyleSheets = [this._adoptedSheet];else {
        const st = document.createElement('style');
        st.textContent = this._authorCss || '';
        sr.appendChild(st);
      }
      sr.appendChild(clone);
      entry.frame.appendChild(host);
      entry.host = host;
      entry.clone = clone;
      if (this._thumbScale) clone.style.transform = 'scale(' + this._thumbScale + ')';
      // Once materialized the IO callback is a no-op early-return —
      // unobserve so scroll doesn't keep firing it.
      if (this._railObserver) this._railObserver.unobserve(entry.frame);
    }

    /** Replace a cloned custom element with an inert box (see the comment
     *  in _materialize). A shadow tree cloned along via {clonable:true}
     *  moves onto the box, so the thumb shows the component's real content
     *  with zero component logic; :host rules in the moved <style> match
     *  the box, and the preserved data-* attrs keep :host([data-…])
     *  selectors working. */
    _neuter(el) {
      // Adopt the shadow only when the cloned root carries renderable
      // content. A constructor-attach / connectedCallback-render component
      // clones into an empty (or style-only) slotless root — adopting that
      // would hide the light children the box is about to receive and drop
      // the placeholder chrome. Such components fall back to the plain box.
      let sr = el.shadowRoot;
      if (sr) {
        let renderable = false;
        for (let n = sr.firstElementChild; n; n = n.nextElementSibling) {
          const t = n.tagName;
          if (t !== 'STYLE' && t !== 'LINK') {
            renderable = true;
            break;
          }
        }
        if (!renderable) sr = null;
      }
      const box = document.createElement('div');
      box.style.cssText = (el.getAttribute('style') || '') + (sr ? '' : ';background:rgba(0,0,0,0.06);border:1px dashed rgba(0,0,0,0.15);');
      box.className = el.className;
      // Preserve theming/i18n hooks so [data-*] / :lang() / [dir]
      // descendant selectors still match the neutered root — but not
      // pointer-interaction transients (a mid-reframe/mid-drag re-clone
      // would render the interaction chrome statically in the thumb).
      for (const a of el.attributes) {
        const n = a.name;
        if (n === 'data-reframe' || n === 'data-panning' || n === 'data-over') continue;
        if (n.startsWith('data-') || n.startsWith('aria-') || n === 'lang' || n === 'dir' || n === 'role' || n === 'title') {
          box.setAttribute(n, a.value);
        }
      }
      while (el.firstChild) box.appendChild(el.firstChild);
      if (sr) this._adoptShadow(box, sr);
      return box;
    }

    /** Move a cloned shadow tree onto a neutered thumbnail box: attach an
     *  open root on the box, carry adoptedStyleSheets, move the children,
     *  then make the content inert. */
    _adoptShadow(box, sr) {
      let root;
      try {
        root = box.attachShadow({
          mode: 'open'
        });
      } catch (e) {
        return;
      }
      // Engine-cloned shadow roots never carry adoptedStyleSheets, but a
      // defined component's clone is upgrade-rebuilt (constructor runs
      // during cloneNode), so sheets it adopts there are present and
      // shared by reference — carry them.
      if (sr.adoptedStyleSheets && sr.adoptedStyleSheets.length) {
        try {
          root.adoptedStyleSheets = Array.prototype.slice.call(sr.adoptedStyleSheets);
        } catch (e) {}
      }
      // Clone rather than move: moving preserves listeners an upgraded
      // clone's constructor attached inside its shadow; cloning sheds
      // them, keeping thumbs free of component logic categorically.
      for (let n = sr.firstChild; n; n = n.nextSibling) {
        root.appendChild(n.cloneNode(true));
      }
      this._inertify(root);
    }

    /** Strip anything executable from copied shadow content and apply the
     *  same custom-element/media/img policy as the light-DOM clone.
     *  (Canvases inside copied shadow content stay blank — there is no
     *  live↔clone pairing across shadow boundaries to snapshot from.) */
    _inertify(root) {
      root.querySelectorAll('script').forEach(s => s.remove());
      // Transient top-layer UI can never belong in a static thumb. (A
      // cloned [popover] is display:none anyway — open state doesn't
      // clone — this just makes it categorical.)
      root.querySelectorAll('[popover], dialog').forEach(el => el.remove());
      // Same heavy-media policy as the light-DOM clone above.
      root.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      root.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      root.querySelectorAll('*').forEach(el => {
        for (let i = el.attributes.length - 1; i >= 0; i--) {
          if (/^on/i.test(el.attributes[i].name)) {
            el.removeAttribute(el.attributes[i].name);
          }
        }
      });
      root.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Nested custom elements inside copied shadow content would upgrade
      // on append — same treatment as the light DOM. querySelectorAll is
      // static, so boxes created mid-walk don't re-enter this loop.
      root.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(this._neuter(el));
      });
    }

    /** Re-clone a single thumb (live-update path). No-op if the thumb
     *  hasn't been materialized yet — it'll pick up current content when
     *  it scrolls into view. */
    _refreshThumb(slide) {
      const entry = (this._thumbs || []).find(t => t.slide === slide);
      if (!entry || !entry.host) return;
      entry.host.remove();
      entry.host = entry.clone = null;
      this._materialize(entry);
    }
    _scaleThumbs() {
      if (!this._thumbs || !this._thumbs.length) return;
      // Every frame is the same width; if it reads 0 the rail is
      // display:none (noscale / no-rail / presenting / print) — leave the
      // clones as-is and re-run when the rail is revealed.
      const fw = this._thumbs[0].frame.offsetWidth;
      if (!fw) return;
      this._thumbScale = fw / this.designWidth;
      this._thumbs.forEach(({
        clone
      }) => {
        if (clone) clone.style.transform = 'scale(' + this._thumbScale + ')';
      });
    }
    _setDrop(i, where) {
      // dragover fires at pointer-event rate; touch only the previous
      // and new target rather than sweeping all N thumbs.
      const t = this._thumbs && this._thumbs[i];
      if (this._dropOn && this._dropOn !== t) {
        this._dropOn.thumb.removeAttribute('data-drop');
      }
      if (t) t.thumb.setAttribute('data-drop', where);
      this._dropOn = t || null;
    }
    _clearDrop() {
      if (this._dropOn) this._dropOn.thumb.removeAttribute('data-drop');
      this._dropOn = null;
    }

    /** 1×1 transparent stand-in for setDragImage. Kept attached (offscreen
     *  in the shadow root) because some engines ignore a drag image that
     *  isn't in a rendered tree. Created lazily, reused for every drag. */
    _dragBlank() {
      if (!this._dragBlankEl) {
        const c = document.createElement('canvas');
        c.width = 1;
        c.height = 1;
        c.style.cssText = 'position:fixed;left:-9999px;top:0;width:1px;height:1px;';
        this._root.appendChild(c);
        this._dragBlankEl = c;
      }
      return this._dragBlankEl;
    }

    /** Vertical-only drag tracking: translate the dragged thumb along Y to
     *  follow the pointer, clamped to the rail, ignoring X entirely. A
     *  document-level capture listener is used because native dragover
     *  fires wherever the pointer is — so the thumb keeps tracking even
     *  while the pointer wanders over the stage — and it is removed the
     *  moment the drag ends. getBoundingClientRect already reflects the
     *  current transform, so the layout position is recovered by
     *  subtracting the translation applied so far (rail auto-scroll moves
     *  the layout position mid-drag; see the rail dragover handler). */
    _startDragTrack(thumb, startY) {
      // A lost dragend (the dragged thumb removed mid-drag by a remote
      // edit's re-render — browsers fire no dragend on a disconnected
      // source) would otherwise leave the previous listener installed
      // forever once this overwrite lands.
      this._stopDragTrack();
      this._dragThumb = thumb;
      // The FLIP reorder animation drives transform through a transition;
      // the live drag must not inherit one, or the thumb rubber-bands.
      // Killed BEFORE the grab-offset read: mid-FLIP the rect includes the
      // interpolated transform, which would bake a constant offset into
      // the whole drag.
      thumb.style.transition = 'none';
      this._dragGrab = startY - thumb.getBoundingClientRect().top;
      this._dragTy = 0;
      this._onDragTrack = e => {
        const t = this._dragThumb;
        if (!t) return;
        const rail = this._rail.getBoundingClientRect();
        const r = t.getBoundingClientRect();
        // A transformed ancestor (author wraps the deck in a CSS scale;
        // canvas-mode pan/zoom) scales viewport deltas: translateY(N)
        // moves the rect by s·N. Measure s from the thumb itself (rect is
        // scaled, offsetHeight is layout px) so the feedback loop stays
        // exact instead of oscillating at s ≥ 2. offsetHeight is 0 only
        // when unrendered — nothing to track then, treat as unscaled.
        const s = t.offsetHeight ? r.height / t.offsetHeight : 1;
        const layoutTop = r.top - s * this._dragTy;
        let want = e.clientY - this._dragGrab;
        want = Math.max(rail.top, Math.min(want, rail.bottom - r.height));
        this._dragTy = (want - layoutTop) / s;
        t.style.transform = 'translateY(' + this._dragTy + 'px)';
      };
      document.addEventListener('dragover', this._onDragTrack, true);
    }

    /** Cancel the thumb's deferred data-dragging mark if its frame has not
     *  fired yet — see the dragstart deferral. Called from dragend only:
     *  _stopDragTrack is the wrong home for it, because _startDragTrack
     *  defensively calls _stopDragTrack at the START of every drag (its
     *  lost-dragend reset), so a cancel there kills the mark the same
     *  dragstart just scheduled. The strand that matters — pointer-
     *  events:none left on a CONNECTED thumb that is no longer being
     *  dragged — is closed two ways: dragend cancels the pending frame
     *  here, and the frame callback re-checks that THIS thumb is still the
     *  live drag source (_dragFrom and _dragThumb, both cleared/re-pointed
     *  by dragend or by a new drag). The remaining lost-dragend case — the
     *  source slide removed mid-drag, so no dragend fires — ends with that
     *  thumb discarded by the rail reconcile (thumbs are keyed by slide
     *  element and a removed slide's thumb is not reused), so a mark landing
     *  on it is on a discarded node. The risk this defer adds over the old
     *  synchronous set is therefore the narrow rAF-after-dragend window,
     *  which the dragend cancel covers. */
    _cancelDragAttr() {
      if (this._dragAttrRaf != null) {
        cancelAnimationFrame(this._dragAttrRaf);
        this._dragAttrRaf = null;
      }
    }
    _stopDragTrack() {
      if (this._onDragTrack) {
        document.removeEventListener('dragover', this._onDragTrack, true);
        this._onDragTrack = null;
      }
      const t = this._dragThumb;
      if (t) {
        t.style.transform = '';
        t.style.transition = '';
      }
      this._dragThumb = null;
      this._dragTy = 0;
    }
    _syncRail(follow) {
      if (!this._thumbs) return;
      this._thumbs.forEach(({
        thumb
      }, i) => {
        if (i === this._index) {
          thumb.setAttribute('data-current', '');
          if (follow && typeof thumb.scrollIntoView === 'function') {
            thumb.scrollIntoView({
              block: 'nearest'
            });
          }
        } else {
          thumb.removeAttribute('data-current');
        }
      });
    }
    _openMenu(i, x, y) {
      if (!this._menu) return;
      this._menuIndex = i;
      const slide = this._slides[i];
      // Right-clicking a thumb OUTSIDE the selection collapses the
      // selection to that thumb (platform convention) — the menu then
      // always targets exactly what's highlighted.
      if (this._selected.size && slide && !this._selected.has(slide)) {
        this._selected.clear();
        this._selected.add(slide);
        this._selAnchor = slide;
        this._syncSelection();
      }
      const sel = this._selectionIndices();
      const bulk = sel.length > 1;
      this._menuIndices = bulk ? sel : [i];
      // Bulk mode offers only the one batched op that exists (delete);
      // the single-slide items address one index and stay hidden.
      this._menu.querySelectorAll('[data-act="skip"], [data-act="up"], [data-act="down"], [data-act="duplicate"], hr').forEach(el => {
        el.style.display = bulk ? 'none' : '';
      });
      const skip = slide && slide.hasAttribute('data-deck-skip');
      this._menu.querySelector('[data-act="skip"]').textContent = skip ? 'Unskip slide' : 'Skip slide';
      this._menu.querySelector('[data-act="up"]').disabled = i <= 0;
      this._menu.querySelector('[data-act="down"]').disabled = i >= this._slides.length - 1;
      const del = this._menu.querySelector('[data-act="delete"]');
      del.textContent = bulk ? 'Delete ' + sel.length + ' slides' : 'Delete slide';
      del.disabled = bulk ? sel.length >= this._slides.length : this._slides.length <= 1;
      // Place, then clamp to viewport after it's measurable.
      this._menu.style.left = x + 'px';
      this._menu.style.top = y + 'px';
      this._menu.setAttribute('data-open', '');
      const r = this._menu.getBoundingClientRect();
      const nx = Math.min(x, window.innerWidth - r.width - 4);
      const ny = Math.min(y, window.innerHeight - r.height - 4);
      this._menu.style.left = Math.max(4, nx) + 'px';
      this._menu.style.top = Math.max(4, ny) + 'px';
    }
    _closeMenu() {
      if (this._menu) this._menu.removeAttribute('data-open');
      this._menuIndex = -1;
      this._menuIndices = null;
    }
    _openConfirm(sel) {
      if (!this._confirm) return;
      const list = Array.isArray(sel) ? sel : [sel];
      // Hold the slide ELEMENTS: the deck can re-render while the dialog
      // is open (collaborator/agent edit), and a frozen index list would
      // then address the wrong slides — a same-count reorder even passes
      // the host's witness guard. Elements re-resolve at danger-click.
      this._confirmEls = list.map(i => this._slides[i]).filter(Boolean);
      // Title uses the rail's skip-aware label, so the confirm names the
      // number the user right-clicked (a raw index would disagree with the
      // rail whenever a skipped slide precedes the target).
      const lbl = list.length === 1 ? this._slideLabel(list[0]) : '';
      this._confirm.querySelector('.title').textContent = list.length === 1 ? lbl ? 'Delete slide ' + lbl + '?' : 'Delete skipped slide?' : 'Delete ' + list.length + ' slides?';
      this._confirm.querySelector('.msg').textContent = list.length === 1 ? 'This slide will be removed from the deck.' : 'These slides will be removed from the deck.';
      this._confirm.setAttribute('data-open', '');
      const btn = this._confirm.querySelector('.danger');
      if (btn && btn.focus) btn.focus();
    }
    _closeConfirm() {
      if (this._confirm) this._confirm.removeAttribute('data-open');
      this._confirmEls = null;
    }

    /** Return focus to the current slide's thumb so the keyboard flow
     *  (Delete → Enter → Delete …) survives the confirm dialog closing.
     *  Without 'force', skipped while a structural op is in flight
     *  (_railLock): _index is then an optimistic post-op value that
     *  doesn't address the pre-op thumb list — _pendingRailRefocus stays
     *  armed and the ack/slotchange paths call back with force once the
     *  rail reflects the op. Skipped (and disarmed) while the rail is
     *  inert (hidden / presenting). */
    _focusCurrentThumb(force) {
      if (!force && this._railLock) return;
      this._pendingRailRefocus = false;
      // Never yank focus from content the user reached meanwhile (e.g.
      // an input inside a slide during the ack round-trip) — only
      // reclaim it from the rail's own surfaces, or from nowhere.
      const ae = this._root && this._root.activeElement;
      const ours = !ae || this._rail && this._rail.contains(ae) || this._confirm && this._confirm.contains(ae) || this._menu && this._menu.contains(ae);
      const lightAe = document.activeElement;
      const lightOk = !lightAe || lightAe === document.body || lightAe === this;
      if (!ours || !lightOk) return;
      const cur = this._thumbs && this._thumbs[this._index];
      if (cur && this._rail && !this._rail.inert) cur.thumb.focus({
        preventScroll: true
      });
    }

    /** Selection as sorted slide indices. An empty explicit selection
     *  means the current slide (the rail's implicit selection). */
    _selectionIndices() {
      const out = [];
      this._slides.forEach((s, i) => {
        if (this._selected.has(s)) out.push(i);
      });
      if (!out.length && this._slides[this._index]) out.push(this._index);
      return out;
    }
    _clearSelection() {
      // Re-anchor before the early return: a plain click followed by
      // arrow/tap navigation leaves _selected empty but the anchor
      // pointing at the old slide, and a later shift-click would range
      // from there instead of the current slide.
      this._selAnchor = null;
      if (!this._selected.size) return;
      this._selected.clear();
      this._syncSelection();
    }
    _syncSelection() {
      (this._thumbs || []).forEach(t => {
        if (this._selected.has(t.slide)) t.thumb.setAttribute('data-selected', '');else t.thumb.removeAttribute('data-selected');
      });
    }

    /** Rail mutations. When a dc-runtime is present (`window.__dcUpdate`)
     *  the host owns the light DOM — handlers emit a dc-op only and the
     *  host applies it (to the editor's model or to the source file) and
     *  re-renders via dc-runtime; slotchange catches the rail up.
     *  Structural ops lock rail input until the host acks so a rapid second
     *  click can't address a stale index; setAttr/removeAttr respect the
     *  lock but don't set it (indices unchanged; the host serializes).
     *  `newIndex` is written to location.hash so slotchange's
     *  _restoreIndex lands on the right slide.
     *
     *  With NO dc-runtime (a raw .html deck), there's no re-render path,
     *  so handlers self-mutate locally for an instant update and emit
     *  `emitOnly: false`; the host persists to disk without
     *  re-rendering over the already-mutated DOM.
     *
     *  See docs/dc-ops.md for the contract. */
    /** True when the page's DC runtime reports a live template stream for
     *  any component here (newer support.js bundles only — older bundles
     *  lack the signal and the HOST-side gate covers those decks). Rail
     *  mutations are refused for the duration: a mid-stream op addresses
     *  slide indices the stream is rewriting underneath the click. */
    _streamActive() {
      try {
        return !!window.__dcUpdate && typeof window.__dcStreaming === 'function' && window.__dcStreaming();
      } catch (e) {
        return false;
      }
    }

    /** Transient in-stage notice for a refused mid-stream rail op. */
    _showStreamNotice() {
      this._showNotice('Claude is still updating this deck — try again when it finishes.');
    }

    /** Transient bottom-center toast for a refused rail gesture. */
    _showNotice(text) {
      if (!this._root) return;
      let n = this._streamNotice;
      if (!n) {
        n = document.createElement('div');
        n.className = 'export-hidden';
        n.setAttribute('data-omelette-chrome', '');
        n.setAttribute('role', 'status');
        n.style.cssText = 'position:fixed;left:50%;bottom:24px;transform:translateX(-50%);' + 'background:rgba(22,22,22,.94);color:#fff;' + 'font:500 13px/1.4 system-ui,sans-serif;padding:8px 14px;' + 'border-radius:8px;z-index:2147483646;pointer-events:none;' + 'opacity:0;transition:opacity .15s ease';
        this._root.append(n);
        this._streamNotice = n;
      }
      n.textContent = text;
      n.style.opacity = '1';
      if (this._streamNoticeTimer) clearTimeout(this._streamNoticeTimer);
      this._streamNoticeTimer = setTimeout(() => {
        n.style.opacity = '0';
      }, 2600);
    }
    _emitDcOp(op, slide, lock, newIndex) {
      // Mid-stream guard: refuse the gesture outright — no lock, no
      // optimistic index change, no emit, no self-mutation (returning
      // true short-circuits every caller). The host applies the same
      // gate for decks whose committed support.js predates the signal.
      if (this._streamActive()) {
        this._showStreamNotice();
        return true;
      }
      // Slide index (template/script/style filtered — same as
      // _collectSlides). deck-stage is a filtered-index dc-op emitter;
      // the host resolves against findDeckStage().slideTids. Callers
      // already pass `to` as a slide index.
      op.at = this._slides.indexOf(slide);
      op.witness = {
        childCount: this._slides.length
      };
      // dc-runtime wraps an <x-import>-mounted component in a
      // <div class="sc-host-x" data-dc-tpl="N"> host — the stamp is on the
      // WRAPPER, not this element. closest() finds it (or this element's
      // own stamp when directly templated).
      const host = this.closest('[data-dc-tpl]');
      const tid = host && host.getAttribute('data-dc-tpl');
      op.mount = {
        tid: tid !== null ? parseInt(tid, 10) : null,
        tag: 'deck-stage'
      };
      op.emitOnly = !!window.__dcUpdate;
      if (op.emitOnly) {
        if (lock) this._railLock = true;
        if (newIndex != null && newIndex !== this._index) {
          this._indexBeforeEmit = this._index;
          this._index = newIndex;
          try {
            history.replaceState(null, '', '#' + (newIndex + 1));
          } catch (e) {}
        }
      }
      this.dispatchEvent(new CustomEvent('dc-op', {
        detail: op,
        bubbles: true,
        composed: true
      }));
      return op.emitOnly;
    }

    /** Delete a set of slides (pre-op indices). One slide delegates to
     *  _deleteSlide — the plain 'remove' op — so single deletes keep
     *  working against hosts that predate 'removeMany'. A bulk delete is
     *  ONE op: one host write, one undo snapshot, and indices that all
     *  address the same pre-op deck (N acked single ops would each need
     *  a fresh witness). */
    _deleteSlides(list) {
      if (this._railLock || !list) return;
      const indices = [...new Set(list)].filter(i => this._slides[i]).sort((a, b) => a - b);
      if (!indices.length || indices.length >= this._slides.length) return;
      if (indices.length === 1) {
        this._deleteSlide(indices[0]);
        return;
      }
      // Mirrors _duplicateSlide: check the stream gate before doing any
      // work (_emitDcOp re-checks).
      if (this._streamActive()) {
        this._showStreamNotice();
        return;
      }
      const els = indices.map(i => this._slides[i]);
      const del = new Set(indices);
      const cur = this._index;
      // New current index in post-op space: shift the kept slide left by
      // the deletions below it; if the current slide itself is deleted,
      // land on the nearest survivor (after, else before).
      const below = n => indices.reduce((k, x) => k + (x < n ? 1 : 0), 0);
      let ni;
      if (!del.has(cur)) {
        ni = cur - below(cur);
      } else {
        let s = -1;
        for (let j = cur + 1; j < this._slides.length; j++) {
          if (!del.has(j)) {
            s = j;
            break;
          }
        }
        if (s === -1) {
          for (let j = cur - 1; j >= 0; j--) {
            if (!del.has(j)) {
              s = j;
              break;
            }
          }
        }
        ni = s < 0 ? 0 : s - below(s);
      }
      // Emit-path deletes can't refocus until the host re-renders; arm
      // the flag at emit time (never on a refused/no-op path) so
      // ack/slotchange can finish the keyboard flow's focus hand-back.
      // The local path clears it via the caller's _focusCurrentThumb().
      this._pendingRailRefocus = true;
      if (this._emitDcOp({
        op: 'removeMany',
        indices
      }, els[0], true, ni)) return;
      this._index = ni;
      this._squelchSlotChange = true;
      els.forEach(el => el.remove());
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _deleteSlide(i) {
      if (this._railLock) return;
      const slide = this._slides[i];
      if (!slide || this._slides.length <= 1) return;
      const cur = this._index;
      const ni = i < cur || i === cur && i === this._slides.length - 1 ? cur - 1 : cur;
      this._pendingRailRefocus = true;
      if (this._emitDcOp({
        op: 'remove'
      }, slide, true, ni)) return;
      this._index = ni;
      this._squelchSlotChange = true;
      slide.remove();
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _duplicateSlide(i) {
      if (this._railLock) return;
      const slide = this._slides[i];
      if (!slide) return;
      // Mint ids + copy component state BEFORE emitting, so the op can
      // carry the id map — but never mint for an op the stream gate is
      // about to refuse (_emitDcOp re-checks; this avoids orphaned keys).
      if (this._streamActive()) {
        this._showStreamNotice();
        return;
      }
      const copy = slide.cloneNode(true);
      copy.removeAttribute('id');
      const ids = this._remintDuplicateIds(copy);
      const op = {
        op: 'duplicate'
      };
      if (ids) op.ids = ids;
      if (this._emitDcOp(op, slide, true, i + 1)) return;
      this._index = i + 1;
      this._squelchSlotChange = true;
      this.insertBefore(copy, slide.nextSibling);
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }

    /** Duplicate id policy. Plain ids are stripped — two live slides must
     *  not share one id. But a component that KEYS persistent state by id
     *  (image-slot's sidecar photo) would silently lose that state with
     *  its id. Such a component opts out of the strip by exposing a
     *  static cloneSlot(fromId, isFree) that copies its stored state
     *  under a fresh id of its choosing and returns that id. The old→new
     *  map is returned (or null) and rides the dc-op so the host writes
     *  the SAME ids into source — without that, the copy's state would
     *  revert on reload (docs/dc-ops.md). */
    _remintDuplicateIds(copy) {
      const ids = {};
      let found = false;
      const used = new Set();
      const idOk = /^[A-Za-z][\w-]{0,63}$/;
      const isFree = id => idOk.test(id) && !used.has(id) && !document.getElementById(id);
      copy.querySelectorAll('[id]').forEach(el => {
        const tag = el.tagName.toLowerCase();
        const cls = tag.indexOf('-') >= 0 && customElements.get(tag);
        let next = null;
        if (el.id && cls && typeof cls.cloneSlot === 'function') {
          try {
            next = cls.cloneSlot(el.id, isFree);
          } catch (e) {}
        }
        // Re-checked here so a misbehaving static can't smuggle a dupe
        // or an unsafe value into the document / the emitted op.
        if (typeof next === 'string' && isFree(next)) {
          ids[el.id] = next;
          used.add(next);
          el.id = next;
          found = true;
        } else {
          el.removeAttribute('id');
        }
      });
      return found ? ids : null;
    }
    _toggleSkip(i) {
      if (this._railLock) return;
      const slide = this._slides[i];
      if (!slide) return;
      const on = !slide.hasAttribute('data-deck-skip');
      if (this._emitDcOp(on ? {
        op: 'setAttr',
        attr: 'data-deck-skip',
        value: ''
      } : {
        op: 'removeAttr',
        attr: 'data-deck-skip'
      }, slide, false)) return;
      if (on) slide.setAttribute('data-deck-skip', '');else slide.removeAttribute('data-deck-skip');
    }
    _skippedIndices() {
      const out = [];
      for (let i = 0; i < this._slides.length; i++) {
        if (this._slides[i].hasAttribute('data-deck-skip')) out.push(i);
      }
      return out;
    }

    /** Rail numbering, skip-aware: a skipped slide shows no number and the
     *  rest stay contiguous (1..visible), so the labels match the positions
     *  the overlay counter reports. Cheap (text writes are diffed), safe to
     *  call after any reconcile or skip toggle. */
    _renumberRail() {
      let v = 0;
      (this._thumbs || []).forEach(t => {
        const label = t.slide.hasAttribute('data-deck-skip') ? '' : String(++v);
        if (t.num.textContent !== label) t.num.textContent = label;
      });
    }

    /** Skip-aware label for slide i — the same numbering _renumberRail
     *  paints: '' for a skipped slide, else its 1-based position among
     *  non-skipped slides. Display surfaces (e.g. the delete confirm)
     *  use this so they never name a number the rail doesn't show. */
    _slideLabel(i) {
      const s = this._slides[i];
      if (!s || s.hasAttribute('data-deck-skip')) return '';
      let v = 0;
      for (let k = 0; k <= i; k++) {
        if (!this._slides[k].hasAttribute('data-deck-skip')) v++;
      }
      return String(v);
    }

    /** Overlay counter, skip-aware: position among non-skipped slides over
     *  the non-skipped total. A skipped CURRENT slide (reachable by rail
     *  click or deep link, never by _advance) shows '–' — its number is
     *  gone from the rail, so any digit here would lie. */
    _syncCount() {
      if (!this._countEl || !this._totalEl) return;
      // Empty deck: keep the overlay's initial "1 / 1" (it has nothing to
      // count and isn't visible without slides) — the guest fallback for
      // frozen copies leaves empty decks alone for the same rendering.
      if (!this._slides.length) {
        this._countEl.textContent = '1';
        this._totalEl.textContent = '1';
        return;
      }
      let pos = 0,
        total = 0;
      this._slides.forEach((s, i) => {
        if (!s.hasAttribute('data-deck-skip')) {
          total++;
          if (i <= this._index) pos = total;
        }
      });
      const cur = this._slides[this._index];
      const curSkipped = !cur || cur.hasAttribute('data-deck-skip');
      this._countEl.textContent = curSkipped ? '–' : String(pos);
      this._totalEl.textContent = String(total);
    }
    _moveSlide(i, j) {
      if (this._railLock || j < 0 || j >= this._slides.length || j === i) return;
      const cur = this._index;
      const ni = cur === i ? j : i < cur && j >= cur ? cur - 1 : i > cur && j <= cur ? cur + 1 : cur;
      const slide = this._slides[i];
      if (this._emitDcOp({
        op: 'move',
        to: j
      }, slide, true, ni)) return;
      const ref = j < i ? this._slides[j] : this._slides[j].nextSibling;
      this._index = ni;
      this._squelchSlotChange = true;
      this.insertBefore(slide, ref);
      this._collectSlides();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'mutation'
      });
    }

    // Public API ------------------------------------------------------------

    /** Current slide index (0-based). */
    get index() {
      return this._index;
    }
    /** Total slide count. */
    get length() {
      return this._slides.length;
    }
    /** Programmatically navigate. */
    goTo(i) {
      this._go(i, 'api');
    }
    next() {
      this._advance(1, 'api');
    }
    prev() {
      this._advance(-1, 'api');
    }
    reset() {
      this._go(0, 'api');
    }
  }
  if (!customElements.get('deck-stage')) {
    customElements.define('deck-stage', DeckStage);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/deck-stage.js", error: String((e && e.message) || e) }); }

// ui_kits/report/AnalysisPage.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  Matrix2x2,
  BarSeries,
  Tag,
  Divider,
  Badge
} = window.TIANSIGHTDesignSystem_bb63dc;
const CHAPTERS = [{
  key: "渗透率矩阵",
  note: "菜品结构",
  kind: "matrix",
  data: {
    xAxis: "点单渗透率",
    yAxis: "毛利率",
    quadrants: [{
      label: "高毛利低渗透 · 值得推",
      tone: "caution"
    }, {
      label: "招牌担当 · 守住",
      tone: "growth"
    }, {
      label: "低毛利低渗透 · 下架候选",
      tone: "muted"
    }, {
      label: "低毛利高渗透 · 改配方",
      tone: "loss"
    }],
    points: [{
      label: "文思豆腐",
      x: .74,
      y: .78,
      tone: "growth"
    }, {
      label: "蟹粉狮子头",
      x: .28,
      y: .72,
      tone: "caution"
    }, {
      label: "响油鳝糊",
      x: .68,
      y: .22,
      tone: "loss"
    }, {
      label: "冷碟拼盘",
      x: .2,
      y: .18,
      tone: "muted"
    }, {
      label: "松鼠鳜鱼",
      x: .55,
      y: .62,
      tone: "bronze"
    }]
  }
}, {
  key: "商圈供需洞察",
  note: "需求 vs 供给",
  kind: "matrix",
  data: {
    xAxis: "同业供给密度",
    yAxis: "需求强度",
    quadrants: [{
      label: "需求旺 供给稀 · 抢占",
      tone: "growth"
    }, {
      label: "需求旺 供给密 · 打差异",
      tone: "caution"
    }, {
      label: "双低 · 不投入",
      tone: "muted"
    }, {
      label: "需求弱 供给密 · 退出",
      tone: "loss"
    }],
    points: [{
      label: "写字楼午市",
      x: .32,
      y: .82,
      tone: "growth"
    }, {
      label: "住宅晚市",
      x: .58,
      y: .66,
      tone: "bronze"
    }, {
      label: "商场档口",
      x: .84,
      y: .44,
      tone: "loss"
    }]
  }
}, {
  key: "复购分层四象限",
  note: "会员分层",
  kind: "matrix",
  data: {
    xAxis: "复购频次",
    yAxis: "客单价",
    quadrants: [{
      label: "高价低频 · 待激活",
      tone: "caution"
    }, {
      label: "核心客群 · 守住",
      tone: "growth"
    }, {
      label: "低价低频 · 观察",
      tone: "muted"
    }, {
      label: "低价高频 · 提客单",
      tone: "bronze"
    }],
    points: [{
      label: "会员 A 层",
      x: .78,
      y: .74,
      tone: "growth"
    }, {
      label: "会员 B 层",
      x: .26,
      y: .66,
      tone: "caution"
    }, {
      label: "平台新客",
      x: .7,
      y: .26,
      tone: "bronze"
    }, {
      label: "沉睡会员",
      x: .18,
      y: .22,
      tone: "muted"
    }]
  }
}, {
  key: "利润敏感性矩阵",
  note: "1% 变动的利润弹性",
  kind: "bars",
  unit: "万/季",
  bars: [{
    label: "折扣结构",
    value: 4.8,
    tone: "loss"
  }, {
    label: "食材损耗",
    value: 3.1,
    tone: "loss"
  }, {
    label: "套餐结构",
    value: 2.9,
    tone: "growth"
  }, {
    label: "人效排班",
    value: 2.4,
    tone: "caution"
  }, {
    label: "外卖运费",
    value: 1.2,
    tone: "muted"
  }]
}, {
  key: "价格带断层洞察",
  note: "订单量分布",
  kind: "bars",
  unit: "%",
  bars: [{
    label: "38–58 元",
    value: 34,
    tone: "bronze"
  }, {
    label: "58–88 元",
    value: 12,
    tone: "loss"
  }, {
    label: "88–128 元",
    value: 29,
    tone: "growth"
  }, {
    label: "128 元以上",
    value: 8,
    tone: "muted"
  }, {
    label: "38 元以下",
    value: 17,
    tone: "caution"
  }]
}];
function AnalysisPage() {
  const [active, setActive] = React.useState(CHAPTERS[0].key);
  const ch = CHAPTERS.find(c => c.key === active);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(PageHead, {
    eyebrow: "\u5206\u6790\u7EF4\u5EA6 / \u7ECF\u8425\u6D1E\u5BDF\u56FE\u8C31",
    title: "\u589E\u957F\u5728\u4F55\u5904\uFF0C\u5229\u6DA6\u5931\u4E8E\u4F55\u5904",
    subtitle: "\u4E94\u5F20\u56FE\u8C31\u9010\u5C42\u62C6\u89E3\u83DC\u54C1\u3001\u5546\u5708\u3001\u4F1A\u5458\u3001\u5229\u6DA6\u4E0E\u4EF7\u683C\u5E26\uFF1B\u6BCF\u4E2A\u70B9\u4F4D\u90FD\u80FD\u8FFD\u56DE\u539F\u59CB\u51ED\u8BC1\u3002",
    meta: "5 CHARTS \xB7 V4.2"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-2)",
      flexWrap: "wrap"
    }
  }, CHAPTERS.map(c => /*#__PURE__*/React.createElement(Tag, {
    key: c.key,
    active: c.key === active,
    onClick: () => setActive(c.key)
  }, c.key))), /*#__PURE__*/React.createElement(Panel, {
    caption: ch.note,
    title: ch.key,
    note: "\u70B9\u51FB\u4E0A\u65B9\u56FE\u8C31\u5207\u6362"
  }, ch.kind === "matrix" ? /*#__PURE__*/React.createElement(Matrix2x2, _extends({
    height: 340
  }, ch.data)) : /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement(BarSeries, {
    data: ch.bars,
    unit: ch.unit,
    labelWidth: 110,
    height: 18
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3, minmax(0,1fr))",
      gap: "var(--space-6)"
    }
  }, [["证据可追溯", "每个点位可下钻到原始收银与结算凭证。"], ["口径统一", "收银、平台、会员三源对齐同一口径后入图。"], ["不猜不编", "样本不足的维度不出结论，只作标注。"]].map(([t, b]) => /*#__PURE__*/React.createElement("div", {
    key: t,
    style: {
      background: "var(--parchment-100)",
      border: "1px solid var(--line-hairline)",
      borderRadius: "var(--radius-md)",
      padding: "var(--space-5)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: 500,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, t), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "8px 0 0",
      fontSize: "var(--text-xs)",
      color: "var(--text-muted)",
      lineHeight: "var(--leading-normal)"
    }
  }, b)))));
}
Object.assign(window, {
  AnalysisPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/report/AnalysisPage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/report/DecisionPage.jsx
try { (() => {
const {
  Stat,
  MetricRow,
  LedgerTable,
  BarSeries,
  Badge,
  Divider,
  Icon
} = window.TIANSIGHTDesignSystem_bb63dc;
const CONCLUSIONS = [{
  tone: "growth",
  head: "午市套餐结构存在可加码空间",
  evidence: "工作日午市点单集中在 38–58 元价格带，套餐渗透仅 34%。",
  impact: "+18.2 万 / 季",
  action: "重排午市套餐结构，主推高毛利招牌组合",
  kpi: "午市客单 +8%"
}, {
  tone: "loss",
  head: "平台满减与会员券叠加，让利失控",
  evidence: "6 个月内 27% 订单同时使用两类优惠，实收折扣率 6.7%。",
  impact: "-24.6 万 / 季",
  action: "收紧叠加规则，按时段分层投放",
  kpi: "折扣率 ≤ 5.0%"
}, {
  tone: "caution",
  head: "招牌菜出餐时长波动，影响复购",
  evidence: "晚市高峰出餐 P90 为 14.3 分钟，差评关键词首位为「上菜慢」。",
  impact: "不可计算，如实说明",
  action: "招牌菜出餐标准化与备料前置",
  kpi: "出餐时长 ≤ 9 分钟"
}];
function DecisionPage({
  onlyAnomalies
}) {
  const rows = onlyAnomalies ? CONCLUSIONS.filter(c => c.tone !== "growth") : CONCLUSIONS;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(PageHead, {
    eyebrow: "\u7B2C\u4E00\u5C42 \xB7 \u7ECF\u8425\u6D1E\u5BDF / \u51B3\u7B56\u7EF4\u5EA6",
    title: "\u5148\u6539\u54EA\u4E09\u4EF6\uFF0C\u7531\u8C01\u6267\u884C\uFF0C\u4EE5\u4F55\u9A8C\u6536",
    subtitle: "\u6BCF\u6761\u7ED3\u8BBA\u5177\u5907\u56DB\u8981\u7D20\uFF1A\u8BC1\u636E\u3001\u5229\u6DA6\u5F71\u54CD\u3001\u6267\u884C\u52A8\u4F5C\u3001\u9A8C\u6536\u6307\u6807\u3002\u53EF\u8BA1\u7B97\u7684\uFF0C\u91CF\u5316\u5230\u4F4D\uFF1B\u4E0D\u53EF\u8BA1\u7B97\u7684\uFF0C\u5982\u5B9E\u8BF4\u660E\u3002",
    meta: "REPORT V4.2 \xB7 2025-09-04"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: "var(--space-6)",
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderTop: "2px solid var(--bronze-500)",
      borderRadius: "var(--radius-md)",
      padding: "var(--space-6)"
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    label: "\u5B63\u5EA6\u8425\u6536",
    value: "486.2",
    unit: "\u4E07"
  }), /*#__PURE__*/React.createElement(Stat, {
    label: "\u589E\u957F\u673A\u4F1A",
    value: "+31.4",
    unit: "\u4E07",
    tone: "growth",
    note: "\u5348\u5E02\u5957\u9910 + \u4F1A\u5458\u5524\u9192"
  }), /*#__PURE__*/React.createElement(Stat, {
    label: "\u5229\u6DA6\u6D41\u5931",
    value: "-38.9",
    unit: "\u4E07",
    tone: "loss",
    note: "\u6298\u6263\u53E0\u52A0 + \u98DF\u6750\u635F\u8017"
  }), /*#__PURE__*/React.createElement(Stat, {
    label: "\u51C0\u53EF\u4E89\u53D6",
    value: "+70.3",
    unit: "\u4E07",
    tone: "bronze",
    note: "90 \u5929\u5185\u53EF\u843D\u5730\u90E8\u5206"
  })), /*#__PURE__*/React.createElement(Panel, {
    caption: "\u7ED3\u8BBA \xB7 \u56DB\u8981\u7D20",
    title: "先改这 " + rows.length + " 件",
    note: "\u6309\u5229\u6DA6\u5F71\u54CD\u4E0E\u53EF\u6267\u884C\u6027\u6392\u5E8F"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-5)"
    }
  }, rows.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: c.head,
    style: {
      display: "grid",
      gridTemplateColumns: "34px minmax(0,1fr)",
      gap: "var(--space-4)",
      paddingBottom: "var(--space-5)",
      borderBottom: i === rows.length - 1 ? "none" : "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 34,
      height: 34,
      borderRadius: "50%",
      border: "1px solid var(--line-rule)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-display)",
      fontSize: 15,
      color: "var(--bronze-500)"
    }
  }, ["一", "二", "三"][i]), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-xl)",
      fontWeight: 500,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, c.head), /*#__PURE__*/React.createElement(Badge, {
    tone: c.tone
  }, c.tone === "growth" ? "增长机会" : c.tone === "loss" ? "利润流失" : "关注")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: "var(--space-5)"
    }
  }, [["证据", c.evidence], ["利润影响", c.impact], ["执行动作", c.action], ["验收指标", c.kpi]].map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-3xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      lineHeight: "var(--leading-normal)",
      color: k === "利润影响" ? c.tone === "loss" ? "var(--loss-500)" : c.tone === "growth" ? "var(--growth-500)" : "var(--ink-700)" : "var(--ink-700)",
      fontFamily: k === "利润影响" ? "var(--font-numeral)" : "var(--font-body)"
    }
  }, v))))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) minmax(0,1fr)",
      gap: "var(--space-6)"
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    caption: "\u9010\u5E97\u5BF9\u6BD4",
    title: "\u95E8\u5E97\u7ECF\u8425\u6307\u6570",
    note: "7 \u5BB6\u95E8\u5E97 \xB7 \u8FD1 6 \u4E2A\u6708"
  }, /*#__PURE__*/React.createElement(BarSeries, {
    labelWidth: 96,
    unit: "",
    data: [{
      label: "旗舰店",
      value: 92,
      tone: "growth"
    }, {
      label: "河西店",
      value: 78,
      tone: "bronze"
    }, {
      label: "湖东店",
      value: 71,
      tone: "bronze"
    }, {
      label: "城北店",
      value: 54,
      tone: "caution"
    }, {
      label: "老门东店",
      value: 38,
      tone: "loss"
    }]
  })), /*#__PURE__*/React.createElement(Panel, {
    caption: "\u5173\u952E\u6307\u6807",
    title: "\u4E00\u672C\u8D26"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(MetricRow, {
    dense: true,
    label: "\u5348\u5E02\u5BA2\u6D41",
    sublabel: "\u65D7\u8230\u5E97 \xB7 \u5DE5\u4F5C\u65E5",
    value: "132",
    unit: "\u4EBA/\u65E5",
    delta: "+8.4%",
    deltaTone: "growth"
  }), /*#__PURE__*/React.createElement(MetricRow, {
    dense: true,
    label: "\u665A\u5E02\u7FFB\u53F0",
    value: "2.6",
    unit: "\u6B21",
    delta: "+0.2",
    deltaTone: "growth"
  }), /*#__PURE__*/React.createElement(MetricRow, {
    dense: true,
    label: "\u6298\u6263\u8BA9\u5229",
    value: "6.7",
    unit: "%",
    delta: "-2.1pt",
    deltaTone: "loss"
  }), /*#__PURE__*/React.createElement(MetricRow, {
    dense: true,
    label: "\u98DF\u6750\u635F\u8017",
    value: "4.3",
    unit: "%",
    delta: "-0.6pt",
    deltaTone: "loss"
  }), /*#__PURE__*/React.createElement(MetricRow, {
    dense: true,
    label: "\u4F1A\u5458\u590D\u8D2D",
    value: "31.2",
    unit: "%",
    delta: "+1.4pt",
    deltaTone: "growth"
  })))), /*#__PURE__*/React.createElement(Panel, {
    caption: "\u884C\u52A8",
    title: "90 \u5929\u884C\u52A8\u6E05\u5355",
    note: "\u52A8\u4F5C \xB7 \u8D23\u4EFB\u4EBA \xB7 \u671F\u9650 \xB7 \u9A8C\u6536\u6307\u6807"
  }, /*#__PURE__*/React.createElement(LedgerTable, {
    columns: [{
      key: "action",
      label: "动作"
    }, {
      key: "owner",
      label: "责任人"
    }, {
      key: "due",
      label: "期限",
      mono: true
    }, {
      key: "kpi",
      label: "验收指标"
    }, {
      key: "impact",
      label: "利润影响",
      mono: true,
      align: "right"
    }],
    rows: [{
      action: "重排午市套餐结构",
      owner: "运营总监",
      due: "D+30",
      kpi: "午市客单 +8%",
      impact: "+18.2 万",
      impactTone: "growth"
    }, {
      action: "收紧平台满减叠加",
      owner: "渠道经理",
      due: "D+45",
      kpi: "折扣率 ≤ 5.0%",
      impact: "+24.6 万",
      impactTone: "growth"
    }, {
      action: "招牌菜出餐标准化",
      owner: "行政总厨",
      due: "D+60",
      kpi: "出餐时长 ≤ 9 分钟",
      impact: "如实说明",
      impactTone: "muted"
    }, {
      action: "会员 B 层唤醒计划",
      owner: "会员运营",
      due: "D+90",
      kpi: "B 层复购 +6pt",
      impact: "+13.1 万",
      impactTone: "growth"
    }, {
      action: "城北店备料流程复核",
      owner: "店长 · 城北",
      due: "D+30",
      kpi: "损耗率 ≤ 3.5%",
      impact: "+6.4 万",
      impactTone: "growth"
    }],
    footer: "\u6570\u636E\u6765\u6E90\uFF1A\u6536\u94F6\u6D41\u6C34 2025-03 \u81F3 2025-08\uFF0C\u542B\u5E73\u53F0\u7ED3\u7B97\u5355\u4E0E\u4F1A\u5458\u6D88\u8D39\u660E\u7EC6\u3002\u4E0D\u53EF\u8BA1\u7B97\u9879\u5DF2\u5982\u5B9E\u6807\u6CE8\u3002"
  })));
}
Object.assign(window, {
  DecisionPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/report/DecisionPage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/report/MethodMapPage.jsx
try { (() => {
const {
  StepMarker,
  Divider,
  Icon,
  Badge
} = window.TIANSIGHTDesignSystem_bb63dc;
const STEPS = [{
  label: "现状",
  q: "现在怎样",
  aim: "看清经营",
  body: "汇总收银、平台、会员与门店数据",
  out: "一本账 · 口径对齐表"
}, {
  label: "机会",
  q: "机会在哪",
  aim: "发现增长点",
  body: "增长机会与利润流失，一并定位",
  out: "五张经营洞察图谱"
}, {
  label: "优先",
  q: "先做什么",
  aim: "判断顺序",
  body: "按利润影响与可执行性排序",
  out: "先改哪三件"
}, {
  label: "行动",
  q: "谁来负责",
  aim: "形成清单",
  body: "明确动作、责任人、期限与指标",
  out: "90 天行动清单"
}, {
  label: "结果",
  q: "是否有效",
  aim: "验证结果",
  body: "对比执行前后变化，继续校正",
  out: "验收记录 · 门店标准"
}];
const LAYERS = [{
  tier: "第一层 · 经营洞察",
  name: "先把问题看清",
  got: "增长机会、利润流失点与 90 天行动清单"
}, {
  tier: "第二层 · 第二大脑共建",
  name: "让系统昼夜守望经营",
  mode: "深度共建",
  got: "打通收银、平台与会员数据：自动对账、异常预警、决策看板"
}, {
  tier: "第三层 · AI 原生品牌",
  name: "从零构建一个新品牌",
  mode: "从 0 共创",
  got: "品牌、组织、数字底座一体化设计，数据资产归品牌自己"
}, {
  tier: "X · 增值专项",
  name: "随层挂接，按需启用",
  mode: "按需挂接",
  got: "企业大学、菜单结构、运营托管、成本优化、专家顾问团支持"
}];
function MethodMapPage() {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(PageHead, {
    eyebrow: "\u65B9\u6CD5\u8BBA\u5730\u56FE / \u7ECF\u8425\u4E94\u95EE",
    title: "\u4E94\u95EE\u65E2\u660E\uFF0C\u767E\u4E8B\u53EF\u51B3",
    subtitle: "\u9010\u5E97\u9010\u6708\uFF0C\u5FAA\u73AF\u63A8\u6F14\u3002\u7ECF\u9A8C\u8BC1\u6709\u6548\u7684\u505A\u6CD5\u6C89\u6DC0\u4E3A\u95E8\u5E97\u6807\u51C6\uFF0C\u4E0D\u968F\u4EBA\u5458\u6D41\u52A8\u800C\u6D41\u5931\u3002",
    meta: "METHOD MAP \xB7 V4.2"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-sm)",
      padding: "var(--space-7)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(5, minmax(0,1fr))",
      gap: "var(--space-4)",
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      left: 20,
      right: 20,
      top: 22,
      height: 1,
      background: "var(--line-hairline)"
    }
  }), STEPS.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: s.label,
    style: {
      position: "relative",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement(StepMarker, {
    index: i + 1,
    active: i === 0,
    size: 44
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-xl)",
      fontWeight: 500,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, s.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-accent)"
    }
  }, s.q + " · " + s.aim), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: "var(--text-body)",
      lineHeight: "var(--leading-normal)"
    }
  }, s.body), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 6,
      paddingTop: 8,
      borderTop: "1px solid var(--line-hairline)",
      fontFamily: "var(--font-numeral)",
      fontSize: "var(--text-3xs)",
      color: "var(--text-muted)"
    }
  }, s.out))))), /*#__PURE__*/React.createElement(Divider, {
    spacing: "var(--space-7)"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      fontSize: "var(--text-sm)",
      color: "var(--ink-700)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--bronze-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "check",
    size: 17
  })), "\u7ECF\u9A8C\u8BC1\u6709\u6548\u7684\u505A\u6CD5\u6C89\u6DC0\u4E3A\u95E8\u5E97\u6807\u51C6\uFF0C\u4E0D\u968F\u4EBA\u5458\u6D41\u52A8\u800C\u6D41\u5931\u3002")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, "\u5C42\u5C42\u9012\u8FDB\uFF0C\u5929\u5929\u6709\u6570"), LAYERS.map((l, i) => /*#__PURE__*/React.createElement("div", {
    key: l.tier,
    style: {
      display: "grid",
      gridTemplateColumns: "38px minmax(0,240px) minmax(0,1fr) auto",
      gap: "var(--space-5)",
      alignItems: "center",
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderLeft: "2px solid " + (i === 1 ? "var(--bronze-500)" : "var(--line-hairline)"),
      borderRadius: "var(--radius-md)",
      padding: "var(--space-5) var(--space-6)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 34,
      height: 34,
      borderRadius: "50%",
      border: "1px solid var(--line-rule)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "var(--font-display)",
      fontSize: 15,
      color: "var(--bronze-500)"
    }
  }, ["一", "二", "三", "X"][i]), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, l.tier), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: 500,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, l.name)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: "var(--text-body)",
      lineHeight: "var(--leading-normal)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-3xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-muted)",
      marginRight: 8
    }
  }, "\u8001\u677F\u5F97\u5230"), l.got), l.mode ? /*#__PURE__*/React.createElement(Badge, {
    tone: i === 1 ? "bronze" : "neutral",
    variant: i === 1 ? "solid" : "soft"
  }, l.mode) : /*#__PURE__*/React.createElement("span", null)))));
}
Object.assign(window, {
  MethodMapPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/report/MethodMapPage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/report/ReportShell.jsx
try { (() => {
const {
  Seal,
  Badge,
  Button,
  Icon,
  Switch,
  Divider
} = window.TIANSIGHTDesignSystem_bb63dc;
const PAGES = [{
  key: "decision",
  label: "决策维度",
  en: "DECISION"
}, {
  key: "analysis",
  label: "分析维度",
  en: "ANALYSIS"
}, {
  key: "trend",
  label: "趋势与归因",
  en: "TREND"
}, {
  key: "method",
  label: "方法论地图",
  en: "METHOD MAP"
}];
function ReportShell({
  page,
  onPage,
  onlyAnomalies,
  onToggle,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100vh",
      display: "grid",
      gridTemplateColumns: "244px minmax(0,1fr)"
    }
  }, /*#__PURE__*/React.createElement("aside", {
    style: {
      borderRight: "1px solid var(--line-hairline)",
      background: "var(--parchment-100)",
      padding: "22px 20px",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-7)",
      position: "sticky",
      top: 0,
      height: "100vh"
    }
  }, /*#__PURE__*/React.createElement(Seal, {
    size: 32,
    wordmark: true,
    subtitle: "\u62A5\u544A\u6837\u5F20",
    src: "../../assets/logo-seal.png"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 2
    }
  }, PAGES.map(p => {
    const on = p.key === page;
    return /*#__PURE__*/React.createElement("div", {
      key: p.key,
      onClick: () => onPage(p.key),
      style: {
        cursor: "pointer",
        padding: "10px 12px",
        borderRadius: "var(--radius-sm)",
        borderLeft: "1.5px solid " + (on ? "var(--line-strong)" : "transparent"),
        background: on ? "var(--surface-card)" : "transparent",
        display: "flex",
        flexDirection: "column",
        gap: 2,
        transition: "all var(--dur-fast) var(--ease-standard)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: "var(--font-display)",
        fontSize: "var(--text-md)",
        fontWeight: on ? 500 : 400,
        letterSpacing: "var(--tracking-cjk-display)",
        color: on ? "var(--bronze-600)" : "var(--ink-700)"
      }
    }, p.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: "var(--font-quote)",
        fontSize: "var(--text-3xs)",
        letterSpacing: "var(--tracking-caps)",
        color: "var(--text-muted)"
      }
    }, p.en));
  })), /*#__PURE__*/React.createElement(Divider, {
    spacing: "0"
  }), /*#__PURE__*/React.createElement(Switch, {
    label: "\u4EC5\u770B\u5F02\u5E38\u9879",
    description: "\u9690\u85CF\u5728\u9608\u503C\u5185\u7684\u6307\u6807",
    checked: onlyAnomalies,
    onChange: e => onToggle(e.target.checked)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "auto",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement(Badge, {
    tone: "bronze",
    variant: "solid"
  }, "\u771F\u5B9E\u62A5\u544A"), /*#__PURE__*/React.createElement(Badge, {
    tone: "neutral"
  }, "V4.2")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      fontSize: "var(--text-3xs)",
      color: "var(--text-muted)"
    }
  }, "2025-03 \u2192 2025-08 \xB7 7 \u5BB6\u95E8\u5E97"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "sm",
    fullWidth: true,
    icon: /*#__PURE__*/React.createElement(Icon, {
      name: "file-text",
      size: 14
    }),
    iconPosition: "left"
  }, "\u5BFC\u51FA PDF"))), /*#__PURE__*/React.createElement("main", {
    style: {
      background: "var(--surface-page)",
      padding: "34px 40px 72px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 960,
      margin: "0 auto",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-7)"
    }
  }, children)));
}
function PageHead({
  eyebrow,
  title,
  subtitle,
  meta
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "space-between",
      gap: "var(--space-7)",
      paddingBottom: "var(--space-4)",
      borderBottom: "1.5px solid var(--line-rule)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, eyebrow), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-3xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, title), subtitle ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-sm)",
      color: "var(--text-muted)",
      maxWidth: "34em"
    }
  }, subtitle) : null), meta ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-numeral)",
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, meta) : null);
}
function Panel({
  caption,
  title,
  note,
  children,
  span
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      gridColumn: span ? "1 / -1" : "auto",
      background: "var(--surface-card)",
      border: "1px solid var(--line-hairline)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--shadow-sm)",
      padding: "var(--space-6)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      justifyContent: "space-between",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 4
    }
  }, caption ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-3xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, caption) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, title)), note ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-3xs)",
      color: "var(--text-muted)"
    }
  }, note) : null), children);
}
Object.assign(window, {
  ReportShell,
  PageHead,
  Panel,
  REPORT_PAGES: PAGES
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/report/ReportShell.jsx", error: String((e && e.message) || e) }); }

// ui_kits/report/TrendPage.jsx
try { (() => {
const {
  TrendLine,
  StackedBars,
  Waterfall,
  Heatmap,
  Donut,
  Gauge,
  Funnel,
  Sparkline,
  MetricRow,
  Badge
} = window.TIANSIGHTDesignSystem_bb63dc;
const M = ["03", "04", "05", "06", "07", "08"];
const mk = function (a) {
  return M.map(function (m, i) {
    return {
      label: m,
      value: a[i]
    };
  });
};
function TrendPage() {
  const rows = ["旗舰店", "河西店", "湖东店", "城北店", "老门东店"];
  const seed = [4.2, 2.1, -1.4, 3.8, 5.1, 6.2, 1.2, 0.4, -2.2, 1.9, 2.4, 3.1, 0.8, 1.1, 0.2, -0.6, 1.4, 2.0, -3.4, -4.1, -2.8, -1.2, 0.6, 1.4, -5.1, -4.4, -3.9, -4.8, -2.2, -1.6];
  const vals = {};
  rows.forEach(function (r, ri) {
    M.forEach(function (c, ci) {
      vals[r + "|" + c] = seed[ri * 6 + ci];
    });
  });
  return React.createElement(React.Fragment, null, React.createElement(PageHead, {
    eyebrow: "分析维度 / 趋势与归因",
    title: "逐月走势，逐店对比",
    subtitle: "同一口径下的走势、结构、归因与达成；每张图都可下钻到原始凭证。",
    meta: "D3 CHARTS · V4.2"
  }), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) minmax(0,1fr)",
      gap: "var(--space-6)"
    }
  }, React.createElement(Panel, {
    caption: "走势",
    title: "逐月营收",
    note: "本店 vs 同业均值"
  }, React.createElement(TrendLine, {
    height: 240,
    unit: "万",
    series: [{
      name: "本店",
      tone: "growth",
      area: true,
      emphasis: true,
      points: mk([72, 78, 74, 83, 89, 92])
    }, {
      name: "同业均值",
      tone: "muted",
      dashed: true,
      points: mk([70, 71, 73, 74, 75, 76])
    }]
  })), React.createElement(Panel, {
    caption: "结构",
    title: "渠道结构占比",
    note: "归一化至 100%"
  }, React.createElement(StackedBars, {
    height: 240,
    normalize: true,
    keys: ["堂食", "外卖", "团购"],
    tones: {
      堂食: "growth",
      外卖: "caution",
      团购: "loss"
    },
    data: M.map(function (m, i) {
      return {
        label: m,
        堂食: 52 + i,
        外卖: 30 - i * .5,
        团购: 18 - i * .5
      };
    })
  })), React.createElement(Panel, {
    caption: "归因",
    title: "利润流失归因",
    note: "单位：万 / 季",
    span: true
  }, React.createElement(Waterfall, {
    height: 290,
    start: 486,
    unit: "万",
    steps: [{
      label: "折扣叠加",
      value: -24.6
    }, {
      label: "食材损耗",
      value: -12.1
    }, {
      label: "人效排班",
      value: -6.4
    }, {
      label: "套餐结构",
      value: 18.2
    }, {
      label: "会员唤醒",
      value: 13.1
    }]
  })), React.createElement(Panel, {
    caption: "逐店逐月",
    title: "同比变化",
    note: "绿为改善，红为恶化",
    span: true
  }, React.createElement(Heatmap, {
    diverging: true,
    unit: "%",
    rows: rows,
    columns: M,
    values: vals
  })), React.createElement(Panel, {
    caption: "客群",
    title: "构成与转化"
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-6)"
    }
  }, React.createElement(Donut, {
    size: 170,
    thickness: 24,
    centerValue: "31.2%",
    centerLabel: "会员占比",
    data: [{
      label: "会员",
      value: 31.2,
      tone: "growth"
    }, {
      label: "平台新客",
      value: 44.1,
      tone: "caution"
    }, {
      label: "散客",
      value: 24.7,
      tone: "muted"
    }]
  }), React.createElement(Funnel, {
    stages: [{
      label: "曝光",
      value: 12400
    }, {
      label: "进店",
      value: 3180
    }, {
      label: "下单",
      value: 2260
    }, {
      label: "复购",
      value: 705
    }]
  }))), React.createElement(Panel, {
    caption: "达成",
    title: "关键指标达成"
  }, React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-5)"
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-6)",
      flexWrap: "wrap"
    }
  }, React.createElement(Gauge, {
    size: 160,
    value: 92,
    target: 100,
    unit: "%",
    label: "午市客单"
  }), React.createElement(Gauge, {
    size: 160,
    value: 104,
    target: 100,
    unit: "%",
    label: "折扣控制"
  })), React.createElement("div", null, React.createElement(MetricRow, {
    dense: true,
    label: "午市客流",
    sublabel: "旗舰店 · 工作日",
    value: "132",
    unit: "人/日",
    delta: "+8.4%",
    deltaTone: "growth",
    note: React.createElement(Sparkline, {
      values: [112, 118, 109, 124, 131, 132],
      toneName: "growth"
    })
  }), React.createElement(MetricRow, {
    dense: true,
    label: "折扣让利",
    value: "4.9",
    unit: "%",
    delta: "-1.8pt",
    deltaTone: "loss",
    note: React.createElement(Sparkline, {
      values: [6.7, 6.4, 6.1, 5.6, 5.2, 4.9],
      toneName: "loss"
    })
  }))))));
}
Object.assign(window, {
  TrendPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/report/TrendPage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ClosingCTA.jsx
try { (() => {
const {
  Seal,
  Wordmark,
  Button,
  Input,
  Select,
  Checkbox,
  Icon,
  Eyebrow
} = window.TIANSIGHTDesignSystem_bb63dc;
function ClosingCTA() {
  const [sent, setSent] = React.useState(false);
  return /*#__PURE__*/React.createElement("section", {
    id: "contact",
    style: {
      background: "var(--surface-inverse)",
      color: "var(--text-on-inverse)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px",
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) minmax(0,420px)",
      gap: "var(--space-10)",
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Eyebrow, {
    tone: "inverse"
  }, "TIANSIGHT \xB7 TABLE AI ALLIANCE"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: "22px 0 0",
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-5xl)",
      fontWeight: "var(--weight-regular)",
      lineHeight: 1.18,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-on-inverse)"
    }
  }, "\u643A\u624B\u4F8D\u5929\uFF0C", /*#__PURE__*/React.createElement("br", null), "\u6301\u7EED\u9886\u5148\u3002"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "24px 0 0",
      maxWidth: "28em",
      fontSize: "var(--text-md)",
      lineHeight: "var(--leading-normal)",
      color: "rgba(239,230,210,.72)"
    }
  }, "\u4EE5\u6700\u8FD1 6 \u4E2A\u6708\u7ECF\u8425\u6570\u636E\u4E3A\u59CB\uFF0C7 \u65E5\u5185\u4EA4\u4ED8\u7ECF\u8425\u8BCA\u65AD\uFF1A\u589E\u957F\u673A\u4F1A\u3001\u5229\u6DA6\u6D41\u5931\u3001\u884C\u52A8\u6B21\u5E8F\u3001\u9A8C\u6536\u6807\u51C6\u3002"), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-9)",
      display: "flex",
      alignItems: "center",
      gap: "var(--space-6)"
    }
  }, /*#__PURE__*/React.createElement(Seal, {
    size: 92,
    inverse: true,
    src: "../../assets/logo-seal.png"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, /*#__PURE__*/React.createElement(Wordmark, {
    fontSize: "var(--text-md)",
    inverse: true
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: "rgba(239,230,210,.6)"
    }
  }, "\u5FAE\u4FE1\u626B\u7801\u8054\u7CFB\u4F8D\u5929")))), /*#__PURE__*/React.createElement("div", {
    style: {
      background: "var(--surface-card)",
      borderRadius: "var(--radius-md)",
      padding: "var(--space-7)",
      boxShadow: "var(--shadow-lg)"
    }
  }, sent ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-4)",
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--growth-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "check",
    size: 26
  })), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-xl)",
      fontWeight: "var(--weight-medium)",
      color: "var(--text-display)"
    }
  }, "\u5DF2\u6536\u5230\uFF0C7 \u65E5\u5185\u56DE\u590D"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-sm)",
      color: "var(--text-muted)"
    }
  }, "\u4F8D\u5929\u987E\u95EE\u5C06\u4E0E\u4F60\u786E\u8BA4\u6570\u636E\u53E3\u5F84\u4E0E\u4EA4\u4ED8\u8282\u594F\u3002"), /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm",
    onClick: () => setSent(false)
  }, "\u518D\u586B\u4E00\u4EFD")) : /*#__PURE__*/React.createElement("form", {
    onSubmit: e => {
      e.preventDefault();
      setSent(true);
    },
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, "\u7ECF\u8425\u8BCA\u65AD\u7533\u8BF7"), /*#__PURE__*/React.createElement(Input, {
    label: "\u54C1\u724C\u540D\u79F0",
    placeholder: "\u4F8B\uFF1A\u97F5 1980 \u65B0\u6D3E\u6DEE\u626C\u83DC",
    required: true
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement(Input, {
    label: "\u95E8\u5E97\u6570",
    suffix: "\u5BB6",
    defaultValue: "7"
  }), /*#__PURE__*/React.createElement(Select, {
    label: "\u4E1A\u6001",
    options: ["新派淮扬菜", "潮汕牛肉", "河鲜小馆", "老字号茶庄"]
  })), /*#__PURE__*/React.createElement(Input, {
    label: "\u8054\u7CFB\u65B9\u5F0F",
    placeholder: "\u624B\u673A\u53F7\u6216\u5FAE\u4FE1\u53F7",
    required: true
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-3)",
      paddingTop: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement(Checkbox, {
    label: "\u6536\u94F6\u6570\u636E",
    description: "\u8FD1 6 \u4E2A\u6708\u6D41\u6C34\u4E0E\u6298\u6263\u660E\u7EC6",
    defaultChecked: true
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "\u5E73\u53F0\u6570\u636E",
    description: "\u5916\u5356\u4E0E\u56E2\u8D2D\u7ED3\u7B97\u5355",
    defaultChecked: true
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "\u4F1A\u5458\u6570\u636E",
    description: "\u4F1A\u5458\u6D88\u8D39\u4E0E\u590D\u8D2D\u660E\u7EC6"
  })), /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    fullWidth: true,
    type: "submit",
    icon: /*#__PURE__*/React.createElement(Icon, {
      name: "arrow-right",
      size: 16
    })
  }, "\u63D0\u4EA4\uFF0C\u53D6\u5F97\u7ECF\u8425\u8BCA\u65AD")))), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--line-inverse)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "26px 32px",
      display: "flex",
      justifyContent: "space-between",
      gap: "var(--space-6)",
      flexWrap: "wrap",
      fontSize: "var(--text-2xs)",
      color: "rgba(239,230,210,.55)"
    }
  }, /*#__PURE__*/React.createElement("span", null, "\u4F8D\u5929 / Table AI Alliance"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-quote)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase"
    }
  }, "Second Brain / Decision System / Restaurant Growth"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-quote)",
      fontStyle: "italic"
    }
  }, "Time is money. With TIANSIGHT, lose neither."))));
}
Object.assign(window, {
  ClosingCTA
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ClosingCTA.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/FiveQuestions.jsx
try { (() => {
const {
  SectionHeading,
  StepMarker,
  Card,
  Divider,
  Icon
} = window.TIANSIGHTDesignSystem_bb63dc;
const STEPS = [{
  label: "现状",
  q: "现在怎样",
  aim: "看清经营",
  body: "汇总收银、平台、会员与门店数据"
}, {
  label: "机会",
  q: "机会在哪",
  aim: "发现增长点",
  body: "增长机会与利润流失，一并定位"
}, {
  label: "优先",
  q: "先做什么",
  aim: "判断顺序",
  body: "按利润影响与可执行性排序"
}, {
  label: "行动",
  q: "谁来负责",
  aim: "形成清单",
  body: "明确动作、责任人、期限与指标"
}, {
  label: "结果",
  q: "是否有效",
  aim: "验证结果",
  body: "对比执行前后变化，继续校正"
}];
const THREE = [{
  t: "看得清",
  b: "收银、平台、会员数据合成一本账，每个数字有出处，异常能追溯。"
}, {
  t: "改得动",
  b: "问题进入行动清单，明确优先级、责任人和验证周期。"
}, {
  t: "留得下",
  b: "有效的做法沉淀成店里的标准动作，换店长、开新店都不走样。"
}];
function FiveQuestions() {
  const [i, setI] = React.useState(0);
  const s = STEPS[i];
  return /*#__PURE__*/React.createElement("section", {
    id: "five",
    style: {
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, /*#__PURE__*/React.createElement(SectionHeading, {
    eyebrow: "\u7ECF\u8425\u4E94\u95EE",
    title: "\u9010\u5E97\u9010\u6708\uFF0C\u5FAA\u73AF\u63A8\u6F14",
    subtitle: "\u4E94\u95EE\u65E2\u660E\uFF0C\u767E\u4E8B\u53EF\u51B3\u3002"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,300px) minmax(0,1fr)",
      gap: "var(--space-9)",
      marginTop: "var(--space-8)",
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)"
    }
  }, STEPS.map((st, n) => /*#__PURE__*/React.createElement("div", {
    key: st.label,
    onClick: () => setI(n),
    style: {
      cursor: "pointer",
      padding: "10px 12px",
      borderRadius: "var(--radius-sm)",
      background: n === i ? "var(--parchment-100)" : "transparent",
      borderLeft: "1.5px solid " + (n === i ? "var(--line-strong)" : "transparent"),
      transition: "all var(--dur-fast) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement(StepMarker, {
    index: n + 1,
    label: st.label,
    sublabel: st.q + " · " + st.aim,
    active: n === i,
    size: 38
  })))), /*#__PURE__*/React.createElement(Card, {
    padding: "lg",
    style: {
      minHeight: 300,
      display: "flex",
      flexDirection: "column"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)"
    }
  }, "第 " + (i + 1) + " 问 · " + s.q), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "14px 0 0",
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-3xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, s.aim), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "16px 0 0",
      fontSize: "var(--text-lg)",
      color: "var(--text-body)",
      maxWidth: "28em"
    }
  }, s.body), /*#__PURE__*/React.createElement(Divider, {
    spacing: "var(--space-7)"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3, minmax(0,1fr))",
      gap: "var(--space-6)",
      marginTop: "auto"
    }
  }, THREE.map(t => /*#__PURE__*/React.createElement("div", {
    key: t.t,
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--bronze-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "circle-dot",
    size: 15
  })), t.t), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-muted)"
    }
  }, t.b)))))), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "var(--space-7) 0 0",
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      color: "var(--ink-700)",
      letterSpacing: "var(--tracking-cjk-display)"
    }
  }, "\u7ECF\u9A8C\u8BC1\u6709\u6548\u7684\u505A\u6CD5\u6C89\u6DC0\u4E3A\u95E8\u5E97\u6807\u51C6\uFF0C\u4E0D\u968F\u4EBA\u5458\u6D41\u52A8\u800C\u6D41\u5931\u3002")));
}
Object.assign(window, {
  FiveQuestions
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/FiveQuestions.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/HomeHero.jsx
try { (() => {
const {
  Button,
  Badge,
  Stat,
  Eyebrow,
  Icon,
  Divider
} = window.TIANSIGHTDesignSystem_bb63dc;
const SHEETS = [{
  name: "侍天决策维度报告",
  tag: "决策维度"
}, {
  name: "侍天分析维度报告",
  tag: "分析维度"
}, {
  name: "侍天方法论地图",
  tag: "方法论地图"
}];
function SheetThumb({
  sheet,
  onOpen
}) {
  const [h, setH] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    onClick: onOpen,
    onMouseEnter: () => setH(true),
    onMouseLeave: () => setH(false),
    style: {
      cursor: "pointer",
      background: "var(--surface-card)",
      border: "1px solid " + (h ? "var(--line-rule)" : "var(--line-hairline)"),
      borderRadius: "var(--radius-md)",
      boxShadow: h ? "var(--shadow-md)" : "var(--shadow-sm)",
      overflow: "hidden",
      transform: h ? "translateY(-2px)" : "none",
      transition: "all var(--dur-base) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 132,
      padding: "14px 14px 0",
      display: "flex",
      flexDirection: "column",
      gap: 7,
      background: "var(--parchment-50)",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 5,
      width: "42%",
      background: "var(--bronze-400)",
      borderRadius: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 3,
      width: "78%",
      background: "var(--parchment-400)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 3,
      width: "64%",
      background: "var(--parchment-400)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 6,
      marginTop: 6,
      alignItems: "flex-end",
      height: 56
    }
  }, [.5, .78, .36, .92, .62].map((v, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      height: v * 100 + "%",
      background: i === 3 ? "var(--growth-500)" : i === 2 ? "var(--loss-500)" : "var(--parchment-300)",
      borderRadius: "1px 1px 0 0"
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "10px 14px",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-xs)",
      color: "var(--ink-800)"
    }
  }, sheet.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--bronze-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "arrow-up-right",
    size: 14
  }))));
}
function HomeHero({
  onNav
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: "top",
    className: "ts-paper",
    style: {
      position: "relative",
      overflow: "hidden",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      right: -180,
      top: -120,
      width: 620,
      height: 620,
      borderRadius: "50%",
      border: "1.5px solid rgba(118,85,31,.09)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      right: -60,
      top: 40,
      width: 380,
      height: 380,
      borderRadius: "50%",
      border: "1px solid rgba(118,85,31,.06)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "88px 32px 72px",
      display: "grid",
      gridTemplateColumns: "minmax(0,1.05fr) minmax(0,.95fr)",
      gap: "var(--space-10)",
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Eyebrow, null, "\u4F8D\u5929 TIANSIGHT \xB7 \u9910\u996E\u7B2C\u4E8C\u5927\u8111"), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: "20px 0 0",
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-6xl)",
      fontWeight: "var(--weight-medium)",
      lineHeight: 1.12,
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, "\u62CD\u677F\u4E4B\u524D\uFF0C", /*#__PURE__*/React.createElement("br", null), "\u95EE\u4F8D\u5929"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "26px 0 0",
      maxWidth: "30em",
      fontSize: "var(--text-lg)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-body)"
    }
  }, "\u4EA4 6 \u4E2A\u6708\u7ECF\u8425\u6570\u636E\uFF0C7 \u65E5\u5185\u53D6\u5F97\u7ECF\u8425\u8BCA\u65AD\uFF1A\u589E\u957F\u5728\u4F55\u5904\u3001\u5229\u6DA6\u5931\u4E8E\u4F55\u5904\u3001\u5148\u6539\u54EA\u4E09\u4EF6\u3001\u7531\u8C01\u6267\u884C\u3001\u4EE5\u4F55\u9A8C\u6536\u3002"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-4)",
      marginTop: "var(--space-8)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    icon: /*#__PURE__*/React.createElement(Icon, {
      name: "arrow-right",
      size: 16
    }),
    onClick: () => onNav("contact")
  }, "\u8FDB\u5165\u5E73\u53F0"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "lg",
    onClick: () => onNav("ladder")
  }, "\u670D\u52A1\u4E0E\u4EF7\u683C")), /*#__PURE__*/React.createElement(Divider, {
    spacing: "var(--space-8)"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-9)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    label: "\u4EA4\u4ED8\u5468\u671F",
    value: "7",
    unit: "\u65E5\u5185",
    tone: "bronze"
  }), /*#__PURE__*/React.createElement(Stat, {
    label: "\u6570\u636E\u7A97\u53E3",
    value: "6",
    unit: "\u4E2A\u6708"
  }), /*#__PURE__*/React.createElement(Stat, {
    label: "\u884C\u52A8\u6E05\u5355",
    value: "90",
    unit: "\u5929"
  }))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginBottom: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement(Badge, {
    tone: "bronze",
    variant: "solid"
  }, "\u771F\u5B9E\u62A5\u544A"), /*#__PURE__*/React.createElement(Badge, {
    tone: "neutral"
  }, "V4.2"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase"
    }
  }, "\u62A5\u544A\u6837\u5F20")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gap: "var(--space-4)"
    }
  }, SHEETS.map(s => /*#__PURE__*/React.createElement(SheetThumb, {
    key: s.name,
    sheet: s,
    onOpen: () => onNav("proof")
  }))))));
}
Object.assign(window, {
  HomeHero,
  SheetThumb
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/HomeHero.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Partners.jsx
try { (() => {
const {
  SectionHeading,
  Card,
  Quote,
  Tag,
  Button
} = window.TIANSIGHTDesignSystem_bb63dc;
const PARTNERS = ["苏帮袁", "清水亭", "3699 河鲜小馆", "游园京梦", "吴裕泰", "韵 1980 新派淮扬菜", "潮发潮汕牛肉"];
const VOICES = [{
  by: "王总",
  text: "太高效，也太透彻了。以前一个月才能完成的深度分析和落地辅导，现在一周就能完成，还能持续跟进。"
}, {
  by: "张总",
  text: "报告分析得非常细，最关键的是结论有证据，不靠猜，也没有 AI 幻觉。大力推荐。"
}, {
  by: "李总",
  text: "不仅把问题讲清楚，还把优先级、责任人和验收指标都排好了，团队拿到就能执行。"
}];
function Partners() {
  return /*#__PURE__*/React.createElement("section", {
    id: "partners",
    style: {
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, /*#__PURE__*/React.createElement(SectionHeading, {
    eyebrow: "\u4F19\u4F34\u80CC\u4E66",
    title: "\u8FD9\u4E9B\u54C1\u724C\uFF0C\u5DF2\u4E0E\u4F8D\u5929\u540C\u884C",
    subtitle: "\u4ECE\u5355\u5E97\u5230\u8FDE\u9501\uFF0C\u4ECE\u65B0\u6D3E\u6DEE\u626C\u83DC\u5230\u8001\u5B57\u53F7\u3002"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-3)",
      flexWrap: "wrap",
      marginTop: "var(--space-7)",
      alignItems: "center"
    }
  }, PARTNERS.map(p => /*#__PURE__*/React.createElement(Tag, {
    key: p
  }, p)), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm"
  }, "\u66F4\u591A\u4F19\u4F34"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, "\u6B63\u5728\u7B7E\u7EA6\u4E2D"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3, minmax(0,1fr))",
      gap: "var(--space-5)",
      marginTop: "var(--space-8)"
    }
  }, VOICES.map(v => /*#__PURE__*/React.createElement(Card, {
    key: v.by,
    padding: "lg"
  }, /*#__PURE__*/React.createElement(Quote, {
    author: v.by,
    role: "\u9910\u996E\u8001\u677F",
    size: "sm"
  }, v.text))))));
}
Object.assign(window, {
  Partners
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Partners.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/PartnersPage.jsx
try { (() => {
const {
  SectionHeading,
  Card,
  Tag,
  Button,
  Icon,
  Divider,
  ExpertCard,
  PartnerCase,
  Badge
} = window.TIANSIGHTDesignSystem_bb63dc;
const PARTNERS = [{
  brand: "韵 1980 新派淮扬菜",
  kind: "新派淮扬菜",
  stores: 7,
  since: "2024-09",
  summary: "午市结构与折扣叠加同时改动，两个季度内利润率回到目标区间。",
  metrics: [{
    label: "午市客单",
    before: "68 元",
    after: "74 元",
    delta: "+8.8%"
  }, {
    label: "折扣率",
    before: "6.7%",
    after: "4.9%",
    delta: "-1.8pt",
    tone: "loss"
  }, {
    label: "会员复购",
    before: "29.8%",
    after: "31.2%",
    delta: "+1.4pt"
  }],
  actions: ["重排午市套餐结构，主推高毛利招牌组合", "收紧平台满减与会员券叠加规则", "招牌菜出餐标准化，备料前置"],
  quote: {
    text: "不仅把问题讲清楚，还把优先级、责任人和验收指标都排好了，团队拿到就能执行。",
    by: "李总",
    role: "餐饮老板"
  }
}, {
  brand: "3699 河鲜小馆",
  kind: "河鲜小馆",
  stores: 3,
  since: "2025-01",
  summary: "河鲜损耗与价格带断层同时处理，主力价格带重新连上。",
  metrics: [{
    label: "食材损耗",
    before: "5.4%",
    after: "3.6%",
    delta: "-1.8pt",
    tone: "loss"
  }, {
    label: "主力价格带占比",
    before: "12%",
    after: "26%",
    delta: "+14pt"
  }],
  actions: ["按到货批次调整每日主推", "补齐 58–88 元价格带缺口"],
  quote: {
    text: "报告分析得非常细，最关键的是结论有证据，不靠猜，也没有 AI 幻觉。",
    by: "张总",
    role: "餐饮老板"
  }
}];
const EXPERTS = [{
  name: "专家姓名",
  title: "淮扬菜出品顾问",
  field: "菜品结构 · 出餐标准",
  tags: ["出餐标准", "菜单结构"]
}, {
  name: "专家姓名",
  title: "连锁运营顾问",
  field: "门店标准 · 人效",
  tags: ["门店标准", "人效"]
}, {
  name: "专家姓名",
  title: "供应链顾问",
  field: "成本优化 · 损耗",
  tags: ["成本优化", "损耗"]
}, {
  name: "专家姓名",
  title: "会员增长顾问",
  field: "复购分层 · 渠道",
  tags: ["复购分层", "渠道"]
}];
const ROSTER = ["苏帮袁", "清水亭", "3699 河鲜小馆", "游园京梦", "吴裕泰", "韵 1980 新派淮扬菜", "潮发潮汕牛肉"];
function PartnersPage({
  onNav
}) {
  const [active, setActive] = React.useState(PARTNERS[0].brand);
  const p = PARTNERS.find(function (x) {
    return x.brand === active;
  }) || PARTNERS[0];
  return React.createElement("div", null, React.createElement("section", {
    style: {
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, React.createElement(SectionHeading, {
    eyebrow: "伙伴背书",
    title: "这些品牌，已与侍天同行",
    subtitle: "从单店到连锁，从新派淮扬菜到老字号。",
    action: React.createElement(Button, {
      variant: "secondary",
      onClick: function () {
        onNav && onNav("contact");
      }
    }, "成为伙伴")
  }), React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-3)",
      flexWrap: "wrap",
      marginTop: "var(--space-7)",
      alignItems: "center"
    }
  }, ROSTER.map(function (b) {
    const has = PARTNERS.some(function (x) {
      return x.brand === b;
    });
    return React.createElement(Tag, {
      key: b,
      active: b === active,
      onClick: has ? function () {
        setActive(b);
      } : undefined
    }, b, has ? null : null);
  }), React.createElement("span", {
    style: {
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, "更多伙伴正在签约中")), React.createElement("div", {
    style: {
      marginTop: "var(--space-8)"
    }
  }, React.createElement(PartnerCase, p)), React.createElement("p", {
    style: {
      margin: "var(--space-5) 0 0",
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, "案例数据经伙伴确认后发布；未确认的品牌只列名称，不列数字。"))), React.createElement("section", {
    style: {
      background: "var(--parchment-100)"
    }
  }, React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, React.createElement(SectionHeading, {
    eyebrow: "X · 增值专项",
    title: "专家顾问团，随层挂接",
    subtitle: "行业专家与侍天系统并行：判断有人背书，动作有人带教。"
  }), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: "var(--space-8)",
      marginTop: "var(--space-8)"
    }
  }, EXPERTS.map(function (e, i) {
    return React.createElement(ExpertCard, Object.assign({
      key: i,
      size: 120
    }, e));
  })), React.createElement("p", {
    style: {
      margin: "var(--space-7) 0 0",
      fontSize: "var(--text-2xs)",
      color: "var(--text-muted)"
    }
  }, "顾问头像与实名待品牌方确认后补充。"))));
}
Object.assign(window, {
  PartnersPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/PartnersPage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ReportProof.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  SectionHeading,
  Card,
  Tag,
  Matrix2x2,
  BarSeries,
  LedgerTable,
  Icon,
  Badge
} = window.TIANSIGHTDesignSystem_bb63dc;
const CHAPTERS = [{
  key: "菜品结构",
  title: "渗透率矩阵",
  kind: "matrix",
  matrix: {
    xAxis: "点单渗透率",
    yAxis: "毛利率",
    quadrants: [{
      label: "高毛利低渗透 · 值得推",
      tone: "caution"
    }, {
      label: "招牌担当 · 守住",
      tone: "growth"
    }, {
      label: "低毛利低渗透 · 下架候选",
      tone: "muted"
    }, {
      label: "低毛利高渗透 · 改配方",
      tone: "loss"
    }],
    points: [{
      label: "文思豆腐",
      x: .74,
      y: .78,
      tone: "growth"
    }, {
      label: "蟹粉狮子头",
      x: .28,
      y: .71,
      tone: "caution"
    }, {
      label: "响油鳝糊",
      x: .68,
      y: .22,
      tone: "loss"
    }, {
      label: "冷碟拼盘",
      x: .22,
      y: .2,
      tone: "muted"
    }]
  }
}, {
  key: "商圈供需",
  title: "商圈供需洞察",
  kind: "bars",
  bars: [{
    label: "写字楼午市",
    value: 82,
    tone: "growth"
  }, {
    label: "周边住宅晚市",
    value: 64,
    tone: "bronze"
  }, {
    label: "商场客流",
    value: 41,
    tone: "caution"
  }, {
    label: "同业供给密度",
    value: 88,
    tone: "loss"
  }]
}, {
  key: "复购四象限",
  title: "复购分层四象限",
  kind: "matrix",
  matrix: {
    xAxis: "复购频次",
    yAxis: "客单价",
    quadrants: [{
      label: "高价低频 · 待激活",
      tone: "caution"
    }, {
      label: "核心客群 · 守住",
      tone: "growth"
    }, {
      label: "低价低频 · 观察",
      tone: "muted"
    }, {
      label: "低价高频 · 提客单",
      tone: "bronze"
    }],
    points: [{
      label: "会员 A 层",
      x: .78,
      y: .74,
      tone: "growth"
    }, {
      label: "会员 B 层",
      x: .26,
      y: .66,
      tone: "caution"
    }, {
      label: "平台新客",
      x: .7,
      y: .26,
      tone: "bronze"
    }]
  }
}, {
  key: "利润敏感性",
  title: "利润敏感性矩阵",
  kind: "bars",
  bars: [{
    label: "折扣结构",
    value: 4.8,
    tone: "loss"
  }, {
    label: "食材损耗",
    value: 3.1,
    tone: "loss"
  }, {
    label: "人效排班",
    value: 2.4,
    tone: "caution"
  }, {
    label: "套餐结构",
    value: 2.9,
    tone: "growth"
  }]
}, {
  key: "价格带洞察",
  title: "价格带断层洞察",
  kind: "bars",
  bars: [{
    label: "38–58 元",
    value: 34,
    tone: "bronze"
  }, {
    label: "58–88 元",
    value: 12,
    tone: "loss"
  }, {
    label: "88–128 元",
    value: 29,
    tone: "growth"
  }, {
    label: "128 元以上",
    value: 8,
    tone: "muted"
  }]
}];
const PROMISES = [{
  icon: "target",
  label: "哪里最值得先改"
}, {
  icon: "users",
  label: "谁负责，何时完成"
}, {
  icon: "shield",
  label: "用什么指标验收"
}];
function ReportProof() {
  const [active, setActive] = React.useState(CHAPTERS[0].key);
  const ch = CHAPTERS.find(c => c.key === active);
  return /*#__PURE__*/React.createElement("section", {
    id: "proof",
    style: {
      background: "var(--parchment-100)",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, /*#__PURE__*/React.createElement(SectionHeading, {
    eyebrow: "\u4EE5\u6837\u5F20\u4E3A\u8BC1",
    title: "\u4E00\u4EFD\u62A5\u544A\uFF0C\u5C31\u662F\u4E00\u4EFD\u51B3\u7B56\u6587\u4EF6",
    subtitle: "\u6BCF\u6761\u7ED3\u8BBA\u5177\u5907\u56DB\u8981\u7D20\uFF1A\u8BC1\u636E\u3001\u5229\u6DA6\u5F71\u54CD\u3001\u6267\u884C\u52A8\u4F5C\u3001\u9A8C\u6536\u6307\u6807\u3002\u53EF\u8BA1\u7B97\u7684\uFF0C\u91CF\u5316\u5230\u4F4D\uFF1B\u4E0D\u53EF\u8BA1\u7B97\u7684\uFF0C\u5982\u5B9E\u8BF4\u660E\u3002"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-7)",
      marginTop: "var(--space-6)",
      flexWrap: "wrap"
    }
  }, PROMISES.map(p => /*#__PURE__*/React.createElement("span", {
    key: p.label,
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      fontSize: "var(--text-sm)",
      color: "var(--ink-700)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--bronze-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: p.icon,
    size: 18
  })), p.label))), /*#__PURE__*/React.createElement(Card, {
    padding: "lg",
    style: {
      marginTop: "var(--space-8)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-5)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-4)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-xl)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)",
      color: "var(--text-display)"
    }
  }, "\u7ECF\u8425\u6D1E\u5BDF\u56FE\u8C31"), /*#__PURE__*/React.createElement(Badge, {
    tone: "neutral"
  }, "V4.2")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-2)",
      flexWrap: "wrap"
    }
  }, CHAPTERS.map(c => /*#__PURE__*/React.createElement(Tag, {
    key: c.key,
    active: c.key === active,
    onClick: () => setActive(c.key)
  }, c.key)))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 1,
      background: "var(--line-hairline)",
      margin: "var(--space-6) 0"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) minmax(0,1fr)",
      gap: "var(--space-9)",
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "var(--text-2xs)",
      letterSpacing: "var(--tracking-caps)",
      textTransform: "uppercase",
      color: "var(--text-accent)",
      marginBottom: "var(--space-4)"
    }
  }, ch.title), ch.kind === "matrix" ? /*#__PURE__*/React.createElement(Matrix2x2, _extends({
    height: 260
  }, ch.matrix)) : /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement(BarSeries, {
    data: ch.bars,
    unit: "%",
    labelWidth: 104,
    height: 16
  }))), /*#__PURE__*/React.createElement(LedgerTable, {
    caption: "90 \u5929\u884C\u52A8\u6E05\u5355",
    columns: [{
      key: "action",
      label: "动作"
    }, {
      key: "owner",
      label: "责任人"
    }, {
      key: "due",
      label: "期限",
      mono: true
    }, {
      key: "kpi",
      label: "验收指标"
    }],
    rows: [{
      action: "重排午市套餐结构",
      owner: "运营总监",
      due: "D+30",
      kpi: "午市客单 +8%"
    }, {
      action: "收紧平台满减叠加",
      owner: "渠道经理",
      due: "D+45",
      kpi: "折扣率 ≤ 5.0%"
    }, {
      action: "招牌菜出餐标准化",
      owner: "行政总厨",
      due: "D+60",
      kpi: "出餐时长 ≤ 9 分钟"
    }, {
      action: "会员 B 层唤醒计划",
      owner: "会员运营",
      due: "D+90",
      kpi: "B 层复购 +6pt"
    }],
    footer: "\u6570\u636E\u6765\u6E90\uFF1A\u6536\u94F6\u6D41\u6C34 2025-03 \u81F3 2025-08\uFF0C\u542B\u5E73\u53F0\u7ED3\u7B97\u5355\u4E0E\u4F1A\u5458\u6D88\u8D39\u660E\u7EC6\u3002"
  })))));
}
Object.assign(window, {
  ReportProof
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ReportProof.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ServiceLadder.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  SectionHeading,
  PlanCard,
  Button,
  Card,
  Icon,
  Divider
} = window.TIANSIGHTDesignSystem_bb63dc;
const TIERS = [{
  tier: "第一层 · 经营洞察",
  name: "先把问题看清",
  deliverable: "增长机会、利润流失点与 90 天行动清单"
}, {
  tier: "第二层 · 第二大脑共建",
  name: "让系统昼夜守望经营",
  mode: "深度共建",
  emphasized: true,
  deliverable: "打通收银、平台与会员数据：自动对账、异常预警、决策看板"
}, {
  tier: "第三层 · AI 原生品牌",
  name: "从零构建一个新品牌",
  mode: "从 0 共创",
  deliverable: "品牌、组织、数字底座一体化设计，数据资产归品牌自己"
}, {
  tier: "X · 增值专项",
  name: "随层挂接，按需启用",
  mode: "按需挂接",
  deliverable: "企业大学、菜单结构、运营托管、成本优化、专家顾问团支持"
}];
const OUTCOMES = [{
  icon: "trending-up",
  t: "看见增长机会",
  b: "找到值得加码的门店、菜品、时段与渠道。"
}, {
  icon: "trending-down",
  t: "守住利润空间",
  b: "减少折扣、损耗、人效与结构性浪费。"
}, {
  icon: "target",
  t: "聚焦关键投入",
  b: "把时间和预算集中到最有影响的经营动作。"
}, {
  icon: "layers",
  t: "复制有效经验",
  b: "一家店验证有效的做法，直接搬到其他店用。"
}];
function ServiceLadder({
  onNav
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: "ladder",
    style: {
      background: "var(--parchment-100)",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "var(--section-y) 32px"
    }
  }, /*#__PURE__*/React.createElement(SectionHeading, {
    eyebrow: "\u670D\u52A1\u4E0E\u4EF7\u683C",
    title: "\u5C42\u5C42\u9012\u8FDB\uFF0C\u5929\u5929\u6709\u6570",
    subtitle: "\u7B2C\u4E00\u5C42\u51FA\u6D1E\u5BDF\uFF0C\u7B2C\u4E8C\u5C42\u5EFA\u5927\u8111\uFF0C\u7B2C\u4E09\u5C42\u7ACB AI \u539F\u751F\u54C1\u724C\uFF1AX \u4E3A\u589E\u503C\u4E13\u9879\uFF0C\u6BCF\u4E00\u5C42\u7686\u53EF\u6302\u63A5\u3002",
    action: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: () => onNav("contact")
    }, "\u8054\u7CFB\u4F8D\u5929")
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: "var(--space-5)",
      marginTop: "var(--space-8)"
    }
  }, TIERS.map(t => /*#__PURE__*/React.createElement(PlanCard, _extends({
    key: t.tier
  }, t)))), /*#__PURE__*/React.createElement(Divider, {
    spacing: "var(--section-y-tight)"
  }), /*#__PURE__*/React.createElement(SectionHeading, {
    size: "md",
    eyebrow: "\u589E\u957F\u4E0E\u6B62\u635F\uFF0C\u7686\u53EF\u91CF\u5316",
    title: "\u628A\u8D44\u6E90\u6295\u5411\u6700\u503C\u5F97\u505A\u7684\u4E8B",
    subtitle: "\u770B\u89C1\u673A\u4F1A\uFF0C\u5B88\u4F4F\u5229\u6DA6\uFF0C\u8BA9\u6709\u6548\u7ECF\u9A8C\u5728\u6BCF\u5BB6\u95E8\u5E97\u6301\u7EED\u590D\u5236\u3002",
    rule: false
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4, minmax(0,1fr))",
      gap: "var(--space-5)",
      marginTop: "var(--space-7)"
    }
  }, OUTCOMES.map(o => /*#__PURE__*/React.createElement(Card, {
    key: o.t,
    padding: "md",
    interactive: true
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--bronze-500)",
      display: "flex"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: o.icon,
    size: 22
  })), /*#__PURE__*/React.createElement("h4", {
    style: {
      margin: "14px 0 8px",
      fontFamily: "var(--font-display)",
      fontSize: "var(--text-lg)",
      fontWeight: "var(--weight-medium)",
      letterSpacing: "var(--tracking-cjk-display)"
    }
  }, o.t), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: "var(--text-xs)",
      lineHeight: "var(--leading-normal)",
      color: "var(--text-muted)"
    }
  }, o.b))))));
}
Object.assign(window, {
  ServiceLadder
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ServiceLadder.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/SiteHeader.jsx
try { (() => {
const {
  Seal,
  Button,
  Icon
} = window.TIANSIGHTDesignSystem_bb63dc;
function NavLink({
  id,
  children,
  onNav,
  active
}) {
  const [h, setH] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", {
    href: "#" + id,
    onClick: e => {
      e.preventDefault();
      onNav(id);
    },
    onMouseEnter: () => setH(true),
    onMouseLeave: () => setH(false),
    style: {
      fontSize: "var(--text-sm)",
      color: h || active ? "var(--bronze-600)" : "var(--ink-700)",
      borderBottom: "1px solid " + (active ? "var(--line-strong)" : "transparent"),
      paddingBottom: 2,
      letterSpacing: "var(--tracking-cjk-body)",
      transition: "all var(--dur-fast) var(--ease-standard)"
    }
  }, children);
}
function SiteHeader({
  onNav,
  active
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      position: "sticky",
      top: 0,
      zIndex: 100,
      background: "rgba(239,230,210,.88)",
      backdropFilter: "blur(10px)",
      WebkitBackdropFilter: "blur(10px)",
      borderBottom: "1px solid var(--line-hairline)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--container-max)",
      margin: "0 auto",
      padding: "14px 32px",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-7)"
    }
  }, /*#__PURE__*/React.createElement("a", {
    href: "#top",
    onClick: e => {
      e.preventDefault();
      onNav("top");
    },
    style: {
      borderBottom: "none"
    }
  }, /*#__PURE__*/React.createElement(Seal, {
    size: 34,
    wordmark: true,
    subtitle: "\u667A\u6167\u9910\u996E",
    src: "../../assets/logo-seal.png"
  })), /*#__PURE__*/React.createElement("nav", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-7)"
    }
  }, /*#__PURE__*/React.createElement(NavLink, {
    id: "proof",
    onNav: onNav,
    active: active === "proof"
  }, "\u62A5\u544A\u6837\u5F20"), /*#__PURE__*/React.createElement(NavLink, {
    id: "five",
    onNav: onNav,
    active: active === "five"
  }, "\u7ECF\u8425\u4E94\u95EE"), /*#__PURE__*/React.createElement(NavLink, {
    id: "ladder",
    onNav: onNav,
    active: active === "ladder"
  }, "\u670D\u52A1\u4E0E\u4EF7\u683C"), /*#__PURE__*/React.createElement("a", {
    href: "partners.html",
    style: {
      fontSize: "var(--text-sm)",
      color: active === "partners" ? "var(--bronze-600)" : "var(--ink-700)",
      borderBottom: "1px solid " + (active === "partners" ? "var(--line-strong)" : "transparent"),
      paddingBottom: 2,
      letterSpacing: "var(--tracking-cjk-body)"
    }
  }, "\u4F19\u4F34\u4E0E\u4E13\u5BB6"), /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "sm",
    icon: /*#__PURE__*/React.createElement(Icon, {
      name: "arrow-right",
      size: 14
    }),
    onClick: () => onNav("contact")
  }, "\u8FDB\u5165\u5E73\u53F0"))));
}
Object.assign(window, {
  SiteHeader,
  NavLink
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/SiteHeader.jsx", error: String((e && e.message) || e) }); }

__ds_ns.Donut = __ds_scope.Donut;

__ds_ns.Funnel = __ds_scope.Funnel;

__ds_ns.Gauge = __ds_scope.Gauge;

__ds_ns.Heatmap = __ds_scope.Heatmap;

__ds_ns.Sparkline = __ds_scope.Sparkline;

__ds_ns.StackedBars = __ds_scope.StackedBars;

__ds_ns.TrendLine = __ds_scope.TrendLine;

__ds_ns.Waterfall = __ds_scope.Waterfall;

__ds_ns.CHART_TONES = __ds_scope.CHART_TONES;

__ds_ns.CHART_HEX = __ds_scope.CHART_HEX;

__ds_ns.CHART_MARGIN = __ds_scope.CHART_MARGIN;

__ds_ns.AXIS_LABEL = __ds_scope.AXIS_LABEL;

__ds_ns.VALUE_LABEL = __ds_scope.VALUE_LABEL;

__ds_ns.ChartFrame = __ds_scope.ChartFrame;

__ds_ns.ChartLegend = __ds_scope.ChartLegend;

__ds_ns.Axes = __ds_scope.Axes;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Card = __ds_scope.Card;

__ds_ns.Divider = __ds_scope.Divider;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.PlanCard = __ds_scope.PlanCard;

__ds_ns.Quote = __ds_scope.Quote;

__ds_ns.Wordmark = __ds_scope.Wordmark;

__ds_ns.Seal = __ds_scope.Seal;

__ds_ns.SectionHeading = __ds_scope.SectionHeading;

__ds_ns.Stat = __ds_scope.Stat;

__ds_ns.StepMarker = __ds_scope.StepMarker;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.BarSeries = __ds_scope.BarSeries;

__ds_ns.LedgerTable = __ds_scope.LedgerTable;

__ds_ns.Matrix2x2 = __ds_scope.Matrix2x2;

__ds_ns.MetricRow = __ds_scope.MetricRow;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.Radio = __ds_scope.Radio;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.ExpertCard = __ds_scope.ExpertCard;

__ds_ns.PartnerCase = __ds_scope.PartnerCase;

__ds_ns.ChartSlide = __ds_scope.ChartSlide;

__ds_ns.ContactSlide = __ds_scope.ContactSlide;

__ds_ns.ExpertTeamSlide = __ds_scope.ExpertTeamSlide;

__ds_ns.FiveQuestionsSlide = __ds_scope.FiveQuestionsSlide;

__ds_ns.LadderSlide = __ds_scope.LadderSlide;

__ds_ns.PartnerWallSlide = __ds_scope.PartnerWallSlide;

__ds_ns.PromiseSlide = __ds_scope.PromiseSlide;

__ds_ns.ProofSlide = __ds_scope.ProofSlide;

__ds_ns.SectionSlide = __ds_scope.SectionSlide;

__ds_ns.SlideFrame = __ds_scope.SlideFrame;

__ds_ns.SlideTitle = __ds_scope.SlideTitle;

__ds_ns.SlidePlaceholder = __ds_scope.SlidePlaceholder;

__ds_ns.TitleSlide = __ds_scope.TitleSlide;

})();
